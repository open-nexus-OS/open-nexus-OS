[package]
name = "gfx-backend"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "GPU backend abstraction: GfxBackend trait + CpuMockBackend"

[lib]
name = "gfx_backend"
path = "src/lib.rs"

[dependencies]
nexus-gfx = { path = "../nexus-gfx" }

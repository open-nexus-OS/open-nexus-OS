[package]
name = "nexus-metrics"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[features]
default = ["std"]
std = []
os-lite = ["dep:nexus-ipc", "nexus-ipc/os-lite", "nexus-ipc/kernel-ipc"]

[dependencies]
nexus-ipc = { path = "../nexus-ipc", optional = true, default-features = false }

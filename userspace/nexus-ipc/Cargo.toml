[package]
name = "nexus-ipc"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["Open Nexus OS Contributors"]

[features]
default = ["std"]
std = []
os-lite = []
kernel-ipc = []

[dependencies]
nexus-abi = { path = "../../source/libs/nexus-abi" }
nexus-sync = { path = "../../source/libs/nexus-sync" }

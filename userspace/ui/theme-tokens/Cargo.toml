[package]
name = "nexus-theme-tokens"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "nexus_theme_tokens"
path = "src/lib.rs"

[dependencies]
nexus-layout-types = { path = "../layout-types" }

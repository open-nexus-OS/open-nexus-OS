/* @ds-bundle: {"format":4,"namespace":"AIVAOSDesignSystem_afec52","components":[{"name":"DatePicker","sourcePath":"components/controls/DatePicker.jsx"},{"name":"GlassCheckbox","sourcePath":"components/controls/GlassCheckbox.jsx"},{"name":"GlassRadioGroup","sourcePath":"components/controls/GlassRadioGroup.jsx"},{"name":"Rating","sourcePath":"components/controls/Rating.jsx"},{"name":"Segment","sourcePath":"components/controls/Segment.jsx"},{"name":"Select","sourcePath":"components/controls/Select.jsx"},{"name":"Slider","sourcePath":"components/controls/Slider.jsx"},{"name":"Stepper","sourcePath":"components/controls/Stepper.jsx"},{"name":"WheelPicker","sourcePath":"components/controls/WheelPicker.jsx"},{"name":"AppIcon","sourcePath":"components/core/AppIcon.jsx"},{"name":"Badge","sourcePath":"components/core/Badge.jsx"},{"name":"GlassButton","sourcePath":"components/core/GlassButton.jsx"},{"name":"GlassCard","sourcePath":"components/core/GlassCard.jsx"},{"name":"GlassToggle","sourcePath":"components/core/GlassToggle.jsx"},{"name":"Banner","sourcePath":"components/feedback/Banner.jsx"},{"name":"ProgressBar","sourcePath":"components/feedback/ProgressBar.jsx"},{"name":"Refresher","sourcePath":"components/feedback/Refresher.jsx"},{"name":"Skeleton","sourcePath":"components/feedback/Skeleton.jsx"},{"name":"SkeletonText","sourcePath":"components/feedback/Skeleton.jsx"},{"name":"Spinner","sourcePath":"components/feedback/Spinner.jsx"},{"name":"Toast","sourcePath":"components/feedback/Toast.jsx"},{"name":"SearchBar","sourcePath":"components/inputs/SearchBar.jsx"},{"name":"TextArea","sourcePath":"components/inputs/TextArea.jsx"},{"name":"TextField","sourcePath":"components/inputs/TextField.jsx"},{"name":"KeyboardKey","sourcePath":"components/keyboard/KeyboardKey.jsx"},{"name":"OnScreenKeyboard","sourcePath":"components/keyboard/OnScreenKeyboard.jsx"},{"name":"LANGS","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"LETTER_ROWS","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"HANGUL_SHIFT","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"NUMBER_ROW","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"SYMBOL_LAYERS","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"KANA_KEYS","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"Accordion","sourcePath":"components/navigation/Accordion.jsx"},{"name":"Avatar","sourcePath":"components/navigation/Avatar.jsx"},{"name":"Breadcrumbs","sourcePath":"components/navigation/Breadcrumbs.jsx"},{"name":"Chip","sourcePath":"components/navigation/Chip.jsx"},{"name":"ListItem","sourcePath":"components/navigation/ListItem.jsx"},{"name":"List","sourcePath":"components/navigation/ListItem.jsx"},{"name":"Pagination","sourcePath":"components/navigation/Pagination.jsx"},{"name":"Sidebar","sourcePath":"components/navigation/Sidebar.jsx"},{"name":"SplitView","sourcePath":"components/navigation/Sidebar.jsx"},{"name":"SubHeader","sourcePath":"components/navigation/SubHeader.jsx"},{"name":"TabBar","sourcePath":"components/navigation/TabBar.jsx"},{"name":"Toolbar","sourcePath":"components/navigation/Toolbar.jsx"},{"name":"TreeView","sourcePath":"components/navigation/TreeView.jsx"},{"name":"ActionSheet","sourcePath":"components/overlays/ActionSheet.jsx"},{"name":"Alert","sourcePath":"components/overlays/Alert.jsx"},{"name":"FAB","sourcePath":"components/overlays/FAB.jsx"},{"name":"Menu","sourcePath":"components/overlays/Menu.jsx"},{"name":"ContextMenu","sourcePath":"components/overlays/Menu.jsx"},{"name":"Modal","sourcePath":"components/overlays/Modal.jsx"},{"name":"Popover","sourcePath":"components/overlays/Popover.jsx"},{"name":"PopoverItem","sourcePath":"components/overlays/Popover.jsx"},{"name":"Tooltip","sourcePath":"components/overlays/Tooltip.jsx"},{"name":"AppWindow","sourcePath":"components/window/AppWindow.jsx"},{"name":"Icon","sourcePath":"components/window/Icon.jsx"},{"name":"Window","sourcePath":"components/window/Window.jsx"},{"name":"WindowActionBar","sourcePath":"components/window/WindowActionBar.jsx"},{"name":"WindowButton","sourcePath":"components/window/WindowButton.jsx"},{"name":"WindowControls","sourcePath":"components/window/WindowControls.jsx"},{"name":"WindowPane","sourcePath":"components/window/WindowPane.jsx"}],"sourceHashes":{"card-shell.js":"1f1a98aa60b1","components/controls/DatePicker.jsx":"50433d7af94e","components/controls/GlassCheckbox.jsx":"8cfa101fddc7","components/controls/GlassRadioGroup.jsx":"e5f0720f8892","components/controls/Rating.jsx":"a40d13b68788","components/controls/Segment.jsx":"4abe6060b9c0","components/controls/Select.jsx":"1534ea35e74c","components/controls/Slider.jsx":"d618b34a6c60","components/controls/Stepper.jsx":"961d0379f14e","components/controls/WheelPicker.jsx":"097e08f28535","components/core/AppIcon.jsx":"e97abd629032","components/core/Badge.jsx":"e7d5df410c06","components/core/GlassButton.jsx":"5ef52d74d5cb","components/core/GlassCard.jsx":"e6ab79f60e4c","components/core/GlassToggle.jsx":"1a252d7a0759","components/feedback/Banner.jsx":"98f2fb4d8443","components/feedback/ProgressBar.jsx":"2a7d55e9e5cb","components/feedback/Refresher.jsx":"e4b44725771d","components/feedback/Skeleton.jsx":"182f51682619","components/feedback/Spinner.jsx":"07f327c59212","components/feedback/Toast.jsx":"5e7ae1f967ad","components/inputs/SearchBar.jsx":"c747468608a8","components/inputs/TextArea.jsx":"421a00e53d97","components/inputs/TextField.jsx":"868b3635221b","components/keyboard/KeyboardKey.jsx":"d6697b8c056e","components/keyboard/OnScreenKeyboard.jsx":"8035539b8561","components/keyboard/keyboard-engine.js":"ba32a3c011bb","components/navigation/Accordion.jsx":"f80f01838144","components/navigation/Avatar.jsx":"bb05a1b45d3f","components/navigation/Breadcrumbs.jsx":"16c0a6e3e744","components/navigation/Chip.jsx":"c84e40c3ecdc","components/navigation/ListItem.jsx":"ba5989fc6a96","components/navigation/Pagination.jsx":"24c0858aceac","components/navigation/Sidebar.jsx":"2f06bc2426ea","components/navigation/SubHeader.jsx":"505dc345a9e0","components/navigation/TabBar.jsx":"af3c4d4789f5","components/navigation/Toolbar.jsx":"e0143c3137a8","components/navigation/TreeView.jsx":"497b1dbee814","components/overlays/ActionSheet.jsx":"892152c349d4","components/overlays/Alert.jsx":"fa1984dc532f","components/overlays/FAB.jsx":"f5c6a2cdbcd4","components/overlays/Menu.jsx":"5c38cf5ab441","components/overlays/Modal.jsx":"b084d458cf95","components/overlays/Popover.jsx":"61ce13d0551b","components/overlays/Tooltip.jsx":"96a3b0743038","components/window/AppWindow.jsx":"f430a7e665c7","components/window/Icon.jsx":"0d9e3b38b582","components/window/Window.jsx":"7835d3aaf51c","components/window/WindowActionBar.jsx":"557e6091bc30","components/window/WindowButton.jsx":"ff213f20eaa6","components/window/WindowControls.jsx":"98f8bf870a41","components/window/WindowPane.jsx":"0b0f4c05e8af","design_handoff_files_window/source/ds-base.js":"20ae0857bffb","design_handoff_os_settings_window/source/ds-base.js":"dba2883b7673"},"inlinedExternals":[],"unexposedExports":[{"name":"hangulBack","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"hangulPress","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"hangulText","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"imeCandidates","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"isJungseong","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"kanaModify","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"predictWords","sourcePath":"components/keyboard/keyboard-engine.js"},{"name":"romajiToKana","sourcePath":"components/keyboard/keyboard-engine.js"}]} */

(() => {

const __ds_ns = (window.AIVAOSDesignSystem_afec52 = window.AIVAOSDesignSystem_afec52 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// card-shell.js
try { (() => {
// Open Nexus OS — shared Light/Dark toggle for design-system cards.
// Toggles html.dark and dispatches a 'cardtheme' CustomEvent ('light'|'dark')
// so React demos (e.g. Window theme prop) can follow along.
(function () {
  function setTheme(dark) {
    document.documentElement.classList.toggle('dark', dark);
    try {
      window.dispatchEvent(new CustomEvent('cardtheme', {
        detail: dark ? 'dark' : 'light'
      }));
    } catch (e) {/* noop */}
    var btns = document.querySelectorAll('.card-theme-toggle button');
    if (btns.length === 2) {
      btns[0].classList.toggle('active', !dark);
      btns[1].classList.toggle('active', dark);
    }
  }
  window.cardShellSetTheme = setTheme;
  function init() {
    if (document.querySelector('.card-theme-toggle')) return;
    var bar = document.createElement('div');
    bar.className = 'card-theme-toggle';
    ['Light', 'Dark'].forEach(function (label, i) {
      var b = document.createElement('button');
      b.type = 'button';
      b.textContent = label;
      b.addEventListener('click', function () {
        setTheme(i === 1);
      });
      bar.appendChild(b);
    });
    document.body.appendChild(bar);
    setTheme(document.documentElement.classList.contains('dark'));
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "card-shell.js", error: String((e && e.message) || e) }); }

// components/controls/GlassCheckbox.jsx
try { (() => {
const {
  useState
} = React;
/**
 * GlassCheckbox — rounded-square checkbox for AIVA OS.
 * Unchecked: glass outline. Checked: blue fill with a white checkmark.
 */
function GlassCheckbox({
  checked,
  defaultChecked = false,
  onChange,
  label,
  disabled = false,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultChecked);
  const isOn = checked !== undefined ? checked : internal;
  const toggle = () => {
    if (disabled) return;
    const next = !isOn;
    if (checked === undefined) setInternal(next);
    onChange && onChange(next);
  };
  return /*#__PURE__*/React.createElement("div", {
    onClick: toggle,
    role: "checkbox",
    "aria-checked": isOn,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: label ? 10 : 0,
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.45 : 1,
      userSelect: 'none',
      WebkitTapHighlightColor: 'transparent',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 22,
      height: 22,
      flexShrink: 0,
      borderRadius: 7,
      background: isOn ? 'var(--glass-toggle-on-bg)' : 'var(--glass-card-bg)',
      border: `1px solid ${isOn ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)'}`,
      backdropFilter: 'blur(8px)',
      WebkitBackdropFilter: 'blur(8px)',
      boxShadow: isOn ? '0 2px 8px var(--glass-toggle-on-shadow)' : 'inset 0 1px 1px rgba(255,255,255,0.2)',
      transition: 'background 0.18s ease, border-color 0.18s ease',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    style: {
      opacity: isOn ? 1 : 0,
      transform: isOn ? 'scale(1)' : 'scale(0.6)',
      transition: 'opacity 0.18s ease, transform 0.18s cubic-bezier(0.34,1.56,0.64,1)'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4 12.5l5 5L20 6.5",
    stroke: "#fff",
    strokeWidth: "3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      fontFamily: 'var(--font-sans)',
      fontWeight: 500,
      color: 'var(--glass-text-primary)'
    }
  }, label));
}
Object.assign(__ds_scope, { GlassCheckbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/GlassCheckbox.jsx", error: String((e && e.message) || e) }); }

// components/controls/GlassRadioGroup.jsx
try { (() => {
const {
  useState
} = React;
/**
 * GlassRadioGroup — single-select radio list for AIVA OS.
 * Each option is a glass row; the selected option shows a blue
 * filled radio dot. Controlled or uncontrolled.
 */
function GlassRadioGroup({
  options = [],
  value,
  defaultValue,
  onChange,
  disabled = false,
  style = {}
}) {
  const norm = options.map(o => typeof o === 'string' ? {
    label: o,
    value: o
  } : o);
  const [internal, setInternal] = useState(defaultValue ?? norm[0]?.value);
  const active = value !== undefined ? value : internal;
  const select = v => {
    if (disabled) return;
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      opacity: disabled ? 0.45 : 1,
      ...style
    }
  }, norm.map(o => {
    const isOn = o.value === active;
    return /*#__PURE__*/React.createElement("div", {
      key: o.value,
      onClick: () => select(o.value),
      role: "radio",
      "aria-checked": isOn,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 11,
        padding: '10px 12px',
        borderRadius: 'var(--radius-xl)',
        cursor: disabled ? 'not-allowed' : 'pointer',
        background: isOn ? 'var(--glass-card-bg)' : 'transparent',
        border: `1px solid ${isOn ? 'var(--glass-card-border)' : 'transparent'}`,
        transition: 'background 0.15s ease, border-color 0.15s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 20,
        height: 20,
        flexShrink: 0,
        borderRadius: '50%',
        background: isOn ? 'var(--glass-toggle-on-bg)' : 'var(--glass-card-bg)',
        border: `1px solid ${isOn ? 'var(--glass-toggle-on-border)' : 'var(--glass-divider)'}`,
        boxShadow: isOn ? '0 2px 8px var(--glass-toggle-on-shadow)' : 'inset 0 1px 1px rgba(255,255,255,0.2)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        transition: 'background 0.18s ease, border-color 0.18s ease'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 7,
        height: 7,
        borderRadius: '50%',
        background: '#fff',
        opacity: isOn ? 1 : 0,
        transform: isOn ? 'scale(1)' : 'scale(0.4)',
        transition: 'opacity 0.18s ease, transform 0.18s cubic-bezier(0.34,1.56,0.64,1)'
      }
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 14,
        fontFamily: 'var(--font-sans)',
        fontWeight: 500,
        color: 'var(--glass-text-primary)'
      }
    }, o.label));
  }));
}
Object.assign(__ds_scope, { GlassRadioGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/GlassRadioGroup.jsx", error: String((e && e.message) || e) }); }

// components/controls/Rating.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Rating — star rating control for AIVA OS.
 * Interactive (click/hover to set) or read-only display. Supports half values
 * in read-only mode. Stars use the accent color when filled.
 */
function Rating({
  value,
  defaultValue = 0,
  max = 5,
  onChange,
  size = 22,
  readOnly = false,
  color,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const [hover, setHover] = useState(null);
  const val = value !== undefined ? value : internal;
  const shown = hover != null ? hover : val;
  const fill = color || 'var(--color-warning)';
  const set = v => {
    if (readOnly) return;
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    role: "slider",
    "aria-valuenow": val,
    "aria-valuemax": max,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 3,
      ...style
    },
    onMouseLeave: () => setHover(null)
  }, Array.from({
    length: max
  }).map((_, i) => {
    const idx = i + 1;
    const ratio = Math.max(0, Math.min(1, shown - i)); // 0..1 fill for this star
    return /*#__PURE__*/React.createElement("span", {
      key: i,
      onClick: () => set(idx),
      onMouseEnter: () => !readOnly && setHover(idx),
      style: {
        position: 'relative',
        display: 'inline-flex',
        cursor: readOnly ? 'default' : 'pointer',
        lineHeight: 0,
        WebkitTapHighlightColor: 'transparent'
      }
    }, /*#__PURE__*/React.createElement("svg", {
      width: size,
      height: size,
      viewBox: "0 0 24 24",
      fill: "none"
    }, /*#__PURE__*/React.createElement("path", {
      d: "M12 2.5l2.9 6 6.6.9-4.8 4.6 1.2 6.5L12 18.9 6.1 21.5l1.2-6.5L2.5 9.4l6.6-.9z",
      fill: "var(--glass-card-bg)",
      stroke: "var(--glass-divider)",
      strokeWidth: "1.2",
      strokeLinejoin: "round"
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        inset: 0,
        width: `${ratio * 100}%`,
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement("svg", {
      width: size,
      height: size,
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        display: 'block'
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M12 2.5l2.9 6 6.6.9-4.8 4.6 1.2 6.5L12 18.9 6.1 21.5l1.2-6.5L2.5 9.4l6.6-.9z",
      fill: fill,
      stroke: fill,
      strokeWidth: "1.2",
      strokeLinejoin: "round"
    }))));
  }));
}
Object.assign(__ds_scope, { Rating });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Rating.jsx", error: String((e && e.message) || e) }); }

// components/controls/Segment.jsx
try { (() => {
const {
  useState,
  useRef,
  useLayoutEffect
} = React;
/**
 * Segment — iOS-style segmented control for AIVA OS.
 * A glass pill track with a sliding white "thumb" behind the active segment.
 * Used for view switching (Tag/Woche/Monat), filters, and mode pickers.
 */
function Segment({
  options = [],
  value,
  defaultValue,
  onChange,
  size = 'md',
  style = {}
}) {
  const norm = options.map(o => typeof o === 'string' ? {
    label: o,
    value: o
  } : o);
  const [internal, setInternal] = useState(defaultValue ?? norm[0]?.value);
  const active = value !== undefined ? value : internal;
  const activeIndex = Math.max(0, norm.findIndex(o => o.value === active));
  const trackRef = useRef(null);
  const [thumb, setThumb] = useState({
    left: 0,
    width: 0
  });
  useLayoutEffect(() => {
    const track = trackRef.current;
    if (!track) return;
    const btn = track.children[activeIndex + 1]; // +1: thumb is child 0
    if (btn) setThumb({
      left: btn.offsetLeft,
      width: btn.offsetWidth
    });
  }, [activeIndex, norm.length, size]);
  const s = size === 'sm' ? {
    h: 30,
    fs: 12,
    pad: 3,
    gap: 2
  } : size === 'lg' ? {
    h: 42,
    fs: 15,
    pad: 4,
    gap: 3
  } : {
    h: 36,
    fs: 13,
    pad: 3,
    gap: 2
  };
  const select = v => {
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    style: {
      position: 'relative',
      display: 'inline-flex',
      alignItems: 'stretch',
      gap: s.gap,
      height: s.h,
      padding: s.pad,
      borderRadius: 'var(--radius-full)',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: 'inset 0 1px 1px rgba(255,255,255,0.20)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: s.pad,
      bottom: s.pad,
      left: thumb.left,
      width: thumb.width,
      borderRadius: 'var(--radius-full)',
      background: 'var(--glass-panel-border)',
      boxShadow: '0 1px 4px rgba(0,0,0,0.18), 0 0 0 0.5px rgba(255,255,255,0.30)',
      transition: 'left 0.3s cubic-bezier(0.34,1.4,0.5,1), width 0.3s cubic-bezier(0.34,1.4,0.5,1)'
    }
  }), norm.map(o => {
    const isActive = o.value === active;
    return /*#__PURE__*/React.createElement("button", {
      key: o.value,
      onClick: () => select(o.value),
      style: {
        position: 'relative',
        zIndex: 1,
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 5,
        padding: '0 14px',
        border: 'none',
        background: 'transparent',
        cursor: 'pointer',
        fontFamily: 'var(--font-sans)',
        fontSize: s.fs,
        fontWeight: isActive ? 600 : 500,
        letterSpacing: '-0.01em',
        color: isActive ? 'var(--glass-text-strong)' : 'var(--glass-text-secondary)',
        whiteSpace: 'nowrap',
        transition: 'color 0.2s ease, font-weight 0.2s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, o.icon && /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex'
      }
    }, o.icon), o.label);
  }));
}
Object.assign(__ds_scope, { Segment });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Segment.jsx", error: String((e && e.message) || e) }); }

// components/controls/Select.jsx
try { (() => {
const {
  useState,
  useRef,
  useEffect
} = React;
/**
 * Select — glass dropdown select for AIVA OS.
 * A frosted trigger pill that opens a liquid-glass option panel (same
 * surface language as Popover) — never the native OS list. The selected
 * option is highlighted with an accent checkmark. Controlled or uncontrolled.
 */
function Select({
  options = [],
  value,
  defaultValue,
  onChange,
  placeholder = 'Auswählen',
  disabled = false,
  size = 'md',
  style = {}
}) {
  const norm = options.map(o => typeof o === 'string' ? {
    label: o,
    value: o
  } : o);
  const [internal, setInternal] = useState(defaultValue ?? '');
  const val = value !== undefined ? value : internal;
  const selected = norm.find(o => o.value === val);
  const [open, setOpen] = useState(false);
  const [hover, setHover] = useState(false);
  const wrapRef = useRef(null);
  const s = size === 'sm' ? {
    h: 30,
    fs: 12,
    pad: '0 10px'
  } : size === 'lg' ? {
    h: 42,
    fs: 15,
    pad: '0 16px'
  } : {
    h: 36,
    fs: 14,
    pad: '0 13px'
  };
  useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (wrapRef.current && !wrapRef.current.contains(e.target)) setOpen(false);
    };
    const onKey = e => {
      if (e.key === 'Escape') setOpen(false);
    };
    document.addEventListener('mousedown', onDoc);
    document.addEventListener('keydown', onKey);
    return () => {
      document.removeEventListener('mousedown', onDoc);
      document.removeEventListener('keydown', onKey);
    };
  }, [open]);
  const pick = v => {
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
    setOpen(false);
  };
  const Item = ({
    o
  }) => {
    const [h, setH] = useState(false);
    const isSel = o.value === val;
    return /*#__PURE__*/React.createElement("button", {
      onClick: () => pick(o.value),
      onMouseEnter: () => setH(true),
      onMouseLeave: () => setH(false),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        width: '100%',
        padding: '9px 11px',
        border: 'none',
        cursor: 'pointer',
        textAlign: 'left',
        borderRadius: 'var(--radius-md)',
        background: h ? 'var(--glass-hover-bg)' : 'transparent',
        fontFamily: 'var(--font-sans)',
        fontSize: s.fs,
        fontWeight: isSel ? 600 : 500,
        color: 'var(--glass-text-strong)',
        transition: 'background 0.12s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, o.label), /*#__PURE__*/React.createElement("svg", {
      width: "14",
      height: "14",
      viewBox: "0 0 24 24",
      fill: "none",
      "aria-hidden": true,
      style: {
        flexShrink: 0,
        opacity: isSel ? 1 : 0,
        color: 'var(--color-info)'
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M4 12.5l5 5L20 6.5",
      stroke: "currentColor",
      strokeWidth: "2.6",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    })));
  };
  return /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    style: {
      position: 'relative',
      display: 'inline-flex',
      opacity: disabled ? 0.45 : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: disabled,
    onClick: () => setOpen(v => !v),
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    "aria-haspopup": "listbox",
    "aria-expanded": open,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      height: s.h,
      padding: s.pad,
      minWidth: 120,
      width: '100%',
      fontFamily: 'var(--font-sans)',
      fontSize: s.fs,
      fontWeight: 500,
      color: selected ? 'var(--glass-text-strong)' : 'var(--glass-text-secondary)',
      background: (hover || open) && !disabled ? 'var(--glass-hover-bg)' : 'var(--glass-card-bg)',
      border: `1px solid ${open ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)'}`,
      borderRadius: 'var(--radius-lg)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      cursor: disabled ? 'not-allowed' : 'pointer',
      outline: 'none',
      boxShadow: 'inset 0 1px 1px rgba(255,255,255,0.18)',
      transition: 'background 0.15s ease, border-color 0.15s ease',
      WebkitTapHighlightColor: 'transparent'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      textAlign: 'left',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, selected ? selected.label : placeholder), /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    "aria-hidden": true,
    style: {
      flexShrink: 0,
      color: 'var(--glass-text-secondary)',
      transform: open ? 'rotate(180deg)' : 'rotate(0deg)',
      transition: 'transform 0.2s ease'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 9l6 6 6-6",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), open && /*#__PURE__*/React.createElement("div", {
    role: "listbox",
    style: {
      position: 'absolute',
      top: `calc(100% + 6px)`,
      left: 0,
      zIndex: 'var(--z-overlay, 20)',
      width: '100%',
      minWidth: 160,
      maxHeight: 264,
      overflowY: 'auto',
      padding: 6,
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-xl)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)'
    }
  }, norm.map(o => /*#__PURE__*/React.createElement(Item, {
    key: o.value,
    o: o
  }))));
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Select.jsx", error: String((e && e.message) || e) }); }

// components/controls/Slider.jsx
try { (() => {
const {
  useState,
  useRef,
  useCallback
} = React;
/**
 * Slider — iOS-style range slider for AIVA OS (maps to ion-range).
 * Glass track with a blue filled portion and a white draggable thumb.
 * Used for volume, brightness, and any continuous value in Control Center / Settings.
 */
function Slider({
  value,
  defaultValue = 50,
  min = 0,
  max = 100,
  step = 1,
  onChange,
  disabled = false,
  icon,
  iconEnd,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const val = value !== undefined ? value : internal;
  const pct = (val - min) / (max - min) * 100;
  const trackRef = useRef(null);
  const dragging = useRef(false);
  const [isDragging, setIsDragging] = useState(false);
  const setFromClientX = useCallback(clientX => {
    const track = trackRef.current;
    if (!track) return;
    const rect = track.getBoundingClientRect();
    let p = (clientX - rect.left) / rect.width;
    p = Math.max(0, Math.min(1, p));
    let next = min + p * (max - min);
    next = Math.round(next / step) * step;
    next = Math.max(min, Math.min(max, next));
    if (value === undefined) setInternal(next);
    onChange && onChange(next);
  }, [min, max, step, onChange, value]);
  const onPointerDown = e => {
    if (disabled) return;
    dragging.current = true;
    setIsDragging(true);
    e.currentTarget.setPointerCapture?.(e.pointerId);
    setFromClientX(e.clientX);
  };
  const onPointerMove = e => {
    if (!dragging.current) return;
    setFromClientX(e.clientX);
  };
  const onPointerUp = () => {
    dragging.current = false;
    setIsDragging(false);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      width: '100%',
      opacity: disabled ? 0.45 : 1,
      ...style
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      color: 'var(--glass-text-secondary)',
      flexShrink: 0
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    onPointerDown: onPointerDown,
    onPointerMove: onPointerMove,
    onPointerUp: onPointerUp,
    style: {
      position: 'relative',
      flex: 1,
      height: 28,
      display: 'flex',
      alignItems: 'center',
      cursor: disabled ? 'not-allowed' : 'pointer',
      touchAction: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      height: 6,
      borderRadius: 999,
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(8px)',
      WebkitBackdropFilter: 'blur(8px)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      width: `${pct}%`,
      height: 6,
      borderRadius: 999,
      background: 'var(--glass-toggle-on-bg)',
      boxShadow: '0 0 8px var(--glass-toggle-on-shadow)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: `${pct}%`,
      transform: `translateX(-50%) scale(${isDragging ? 1.25 : 1})`,
      width: 20,
      height: 20,
      borderRadius: '50%',
      background: 'linear-gradient(to bottom, #ffffff, #f2f3f6)',
      boxShadow: isDragging ? '0 3px 10px rgba(0,0,0,0.35), 0 0 0 6px rgba(255,255,255,0.10)' : '0 1px 5px rgba(0,0,0,0.30), 0 0 0 0.5px rgba(0,0,0,0.04)',
      transition: isDragging ? 'transform var(--motion-swift, 0.16s) ease-out, box-shadow var(--motion-swift, 0.16s) ease' : 'left 0.12s ease, transform var(--motion-quick, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), box-shadow var(--motion-quick, 0.28s) ease'
    }
  })), iconEnd && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      color: 'var(--glass-text-secondary)',
      flexShrink: 0
    }
  }, iconEnd));
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Slider.jsx", error: String((e && e.message) || e) }); }

// components/controls/Stepper.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Stepper — compact −/+ numeric stepper for AIVA OS.
 * A glass pill with two press targets flanking the current value.
 */
function Stepper({
  value,
  defaultValue = 0,
  min = -Infinity,
  max = Infinity,
  step = 1,
  onChange,
  disabled = false,
  formatValue,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const val = value !== undefined ? value : internal;
  const set = next => {
    next = Math.max(min, Math.min(max, next));
    if (value === undefined) setInternal(next);
    onChange && onChange(next);
  };
  const Btn = ({
    children,
    onPress,
    disabledBtn
  }) => {
    const [hover, setHover] = useState(false);
    return /*#__PURE__*/React.createElement("button", {
      onClick: () => !disabledBtn && onPress(),
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      disabled: disabledBtn,
      style: {
        width: 38,
        height: 34,
        border: 'none',
        cursor: disabledBtn ? 'not-allowed' : 'pointer',
        background: hover && !disabledBtn ? 'var(--glass-hover-bg)' : 'transparent',
        color: 'var(--glass-text-strong)',
        fontSize: 20,
        lineHeight: 1,
        fontWeight: 400,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        opacity: disabledBtn ? 0.3 : 1,
        transition: 'background 0.15s ease',
        WebkitTapHighlightColor: 'transparent',
        fontFamily: 'var(--font-sans)'
      }
    }, children);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      height: 34,
      borderRadius: 'var(--radius-full)',
      overflow: 'hidden',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: 'inset 0 1px 1px rgba(255,255,255,0.2)',
      opacity: disabled ? 0.45 : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement(Btn, {
    onPress: () => set(val - step),
    disabledBtn: disabled || val <= min
  }, "\u2212"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 36,
      textAlign: 'center',
      padding: '0 4px',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 600,
      fontVariantNumeric: 'tabular-nums',
      color: 'var(--glass-text-strong)',
      borderLeft: '1px solid var(--glass-divider)',
      borderRight: '1px solid var(--glass-divider)',
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, formatValue ? formatValue(val) : val), /*#__PURE__*/React.createElement(Btn, {
    onPress: () => set(val + step),
    disabledBtn: disabled || val >= max
  }, "+"));
}
Object.assign(__ds_scope, { Stepper });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Stepper.jsx", error: String((e && e.message) || e) }); }

// components/controls/WheelPicker.jsx
try { (() => {
const {
  useRef,
  useEffect,
  useState
} = React;
/**
 * WheelPicker — iOS-style scrolling wheel column for AIVA OS (maps to the
 * picker primitive behind DatePickerComponent). Snap-scrolls options under a
 * centered glass selection band. Controlled or uncontrolled.
 */
function WheelPicker({
  options = [],
  value,
  defaultValue,
  onChange,
  itemHeight = 36,
  visibleCount = 5,
  width = 90,
  style = {}
}) {
  const norm = options.map(o => typeof o === 'string' || typeof o === 'number' ? {
    label: String(o),
    value: o
  } : o);
  const [internal, setInternal] = useState(defaultValue ?? norm[0]?.value);
  const val = value !== undefined ? value : internal;
  const ref = useRef(null);
  const settle = useRef(null);
  const pad = Math.floor(visibleCount / 2);
  const idx = Math.max(0, norm.findIndex(o => o.value === val));
  useEffect(() => {
    const el = ref.current;
    if (el) el.scrollTop = idx * itemHeight;
  }, []); // initial position only

  const onScroll = () => {
    clearTimeout(settle.current);
    settle.current = setTimeout(() => {
      const el = ref.current;
      if (!el) return;
      let i = Math.round(el.scrollTop / itemHeight);
      i = Math.max(0, Math.min(norm.length - 1, i));
      el.scrollTo({
        top: i * itemHeight,
        behavior: 'smooth'
      });
      const v = norm[i]?.value;
      if (v !== val) {
        if (value === undefined) setInternal(v);
        onChange && onChange(v);
      }
    }, 110);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width,
      height: itemHeight * visibleCount,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: pad * itemHeight,
      height: itemHeight,
      pointerEvents: 'none',
      borderTop: '1px solid var(--glass-divider)',
      borderBottom: '1px solid var(--glass-divider)',
      background: 'var(--glass-hover-bg)',
      borderRadius: 8,
      zIndex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      zIndex: 2,
      background: 'linear-gradient(var(--glass-overlay-bg), transparent 30%, transparent 70%, var(--glass-overlay-bg))',
      opacity: 0.55
    }
  }), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    onScroll: onScroll,
    style: {
      height: '100%',
      overflowY: 'scroll',
      scrollSnapType: 'y mandatory',
      scrollbarWidth: 'none',
      msOverflowStyle: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: pad * itemHeight
    }
  }), norm.map(o => {
    const active = o.value === val;
    return /*#__PURE__*/React.createElement("div", {
      key: o.value,
      onClick: () => {
        if (value === undefined) setInternal(o.value);
        onChange && onChange(o.value);
        ref.current.scrollTo({
          top: norm.indexOf(o) * itemHeight,
          behavior: 'smooth'
        });
      },
      style: {
        height: itemHeight,
        scrollSnapAlign: 'center',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        cursor: 'pointer',
        fontFamily: 'var(--font-sans)',
        fontSize: active ? 17 : 15,
        fontWeight: active ? 600 : 500,
        fontVariantNumeric: 'tabular-nums',
        color: active ? 'var(--glass-text-strong)' : 'var(--glass-text-secondary)',
        transition: 'color 0.15s ease, font-size 0.15s ease'
      }
    }, o.label);
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      height: pad * itemHeight
    }
  })), /*#__PURE__*/React.createElement("style", null, `div::-webkit-scrollbar{display:none}`));
}
Object.assign(__ds_scope, { WheelPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/WheelPicker.jsx", error: String((e && e.message) || e) }); }

// components/controls/DatePicker.jsx
try { (() => {
const {
  useState
} = React;
/**
 * DatePicker — glass date wheel for AIVA OS (maps to DatePickerComponent).
 * Three WheelPicker columns (day · month · year) inside a frosted panel.
 * Value is a JS Date; controlled or uncontrolled.
 */
function DatePicker({
  value,
  defaultValue,
  onChange,
  minYear = 1970,
  maxYear = 2035,
  monthLabels = ['Jan', 'Feb', 'Mär', 'Apr', 'Mai', 'Jun', 'Jul', 'Aug', 'Sep', 'Okt', 'Nov', 'Dez'],
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue || new Date());
  const date = value !== undefined ? value : internal;
  const y = date.getFullYear();
  const m = date.getMonth(); // 0-based
  const d = date.getDate();
  const daysInMonth = new Date(y, m + 1, 0).getDate();
  const years = Array.from({
    length: maxYear - minYear + 1
  }, (_, i) => minYear + i);
  const days = Array.from({
    length: daysInMonth
  }, (_, i) => i + 1);
  const update = (ny, nm, nd) => {
    const clampedDay = Math.min(nd, new Date(ny, nm + 1, 0).getDate());
    const next = new Date(ny, nm, clampedDay);
    if (value === undefined) setInternal(next);
    onChange && onChange(next);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 4,
      padding: '8px 14px',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-2xl)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.WheelPicker, {
    width: 54,
    options: days,
    value: d,
    onChange: nd => update(y, m, nd)
  }), /*#__PURE__*/React.createElement(__ds_scope.WheelPicker, {
    width: 70,
    options: monthLabels.map((label, i) => ({
      label,
      value: i
    })),
    value: m,
    onChange: nm => update(y, nm, d)
  }), /*#__PURE__*/React.createElement(__ds_scope.WheelPicker, {
    width: 74,
    options: years,
    value: y,
    onChange: ny => update(ny, m, d)
  }));
}
Object.assign(__ds_scope, { DatePicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/DatePicker.jsx", error: String((e && e.message) || e) }); }

// components/core/AppIcon.jsx
try { (() => {
const {
  useState
} = React;
/**
 * AppIcon — adaptive app icon tile with three rendering modes.
 *
 * variant="native"      → icon fills the rounded tile; for first-party OS apps
 *                         whose icon already has the correct shape & background.
 * variant="wrapped"     → glass panel backing wraps the icon; for sideloaded apps,
 *                         web apps, or anything without a shaped icon.
 * variant="freestanding"→ completely bare, no backing; for special apps like Stash
 *                         and pre-composed special-folder icons (pictures, videos, …).
 *
 * Badge is ALWAYS rendered on the outer icon-area wrapper — never inside the
 * backing's overflow:hidden — so it is never clipped.
 */
function AppIcon({
  src,
  label,
  size = 'md',
  badge,
  active = false,
  variant = 'native',
  style = {},
  onClick
}) {
  const [hovered, setHovered] = useState(false);
  const [pressed, setPressed] = useState(false);
  const sizes = {
    xs: {
      backing: 40,
      radius: 11,
      font: 10,
      gap: 5
    },
    sm: {
      backing: 48,
      radius: 13,
      font: 11,
      gap: 6
    },
    md: {
      backing: 56,
      radius: 16,
      font: 13,
      gap: 8
    },
    lg: {
      backing: 72,
      radius: 20,
      font: 14,
      gap: 10
    },
    xl: {
      backing: 88,
      radius: 24,
      font: 15,
      gap: 12
    }
  };
  const s = sizes[size] || sizes.md;
  const transform = pressed ? 'scale(0.92)' : hovered ? 'scale(1.08)' : 'scale(1)';
  const transition = 'transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.2s ease';

  /* ─── icon visual ─────────────────────────────────────────────────────── */

  let iconVisual;
  if (variant === 'wrapped') {
    /* Glass panel backing — wraps a shapeless / non-standard icon */
    iconVisual = /*#__PURE__*/React.createElement("div", {
      style: {
        width: s.backing,
        height: s.backing,
        borderRadius: s.radius,
        background: 'var(--glass-icon-bg)',
        border: '1px solid var(--glass-icon-border)',
        backdropFilter: 'blur(20px)',
        WebkitBackdropFilter: 'blur(20px)',
        boxShadow: hovered ? 'var(--shadow-icon-hover)' : 'var(--shadow-icon)',
        transform,
        transition,
        overflow: 'hidden',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        position: 'relative'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0,
        background: 'var(--glass-shine)',
        pointerEvents: 'none',
        zIndex: 1
      }
    }), /*#__PURE__*/React.createElement("img", {
      src: src,
      alt: label,
      draggable: false,
      style: {
        width: '65%',
        height: '65%',
        objectFit: 'contain',
        position: 'relative',
        zIndex: 2
      }
    }));
  } else if (variant === 'native') {
    /* Icon IS the visual — rounded clip, no redundant glass */
    iconVisual = /*#__PURE__*/React.createElement("div", {
      style: {
        width: s.backing,
        height: s.backing,
        borderRadius: s.radius,
        overflow: 'hidden',
        boxShadow: hovered ? 'var(--shadow-icon-hover)' : 'var(--shadow-icon)',
        transform,
        transition,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("img", {
      src: src,
      alt: label,
      draggable: false,
      style: {
        width: '100%',
        height: '100%',
        objectFit: 'cover',
        display: 'block'
      }
    }));
  } else {
    /* freestanding — completely bare; the icon IS the visual.
       Special-folder icons (pictures/videos/documents/…) already carry their
       type symbol composed onto the folder face, so they render as-is here. */
    iconVisual = /*#__PURE__*/React.createElement("div", {
      style: {
        width: s.backing,
        height: s.backing,
        transform,
        transition,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("img", {
      src: src,
      alt: label,
      draggable: false,
      style: {
        width: '100%',
        height: '100%',
        objectFit: 'contain',
        display: 'block'
      }
    }));
  }

  /* ─── outer wrapper ───────────────────────────────────────────────────── */

  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: s.gap,
      cursor: 'pointer',
      userSelect: 'none',
      WebkitTapHighlightColor: 'transparent',
      ...style
    },
    onClick: onClick,
    onMouseEnter: () => setHovered(true),
    onMouseLeave: () => {
      setHovered(false);
      setPressed(false);
    },
    onMouseDown: () => setPressed(true),
    onMouseUp: () => setPressed(false)
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: s.backing,
      height: s.backing
    }
  }, iconVisual, badge > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: variant === 'freestanding' ? '12%' : -4,
      right: variant === 'freestanding' ? '2%' : -4,
      minWidth: 18,
      height: 18,
      borderRadius: 9,
      background: 'var(--glass-notif-dot)',
      color: '#fff',
      fontSize: 10,
      fontFamily: 'var(--font-sans)',
      fontWeight: 700,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '0 4px',
      zIndex: 10,
      boxShadow: '0 1px 4px rgba(0,0,0,0.35)'
    }
  }, badge > 99 ? '99+' : badge)), active && /*#__PURE__*/React.createElement("div", {
    style: {
      width: 4,
      height: 4,
      borderRadius: 2,
      background: 'var(--glass-text-primary)',
      opacity: 0.7,
      marginTop: -4,
      flexShrink: 0
    }
  }), label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: s.font,
      fontFamily: 'var(--font-sans)',
      fontWeight: 'var(--font-weight-semibold)',
      color: 'var(--glass-text-primary)',
      textAlign: 'center',
      lineHeight: 1.2,
      maxWidth: s.backing + 16,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      textShadow: 'var(--glass-label-shadow)'
    }
  }, label));
}
Object.assign(__ds_scope, { AppIcon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/AppIcon.jsx", error: String((e && e.message) || e) }); }

// components/core/Badge.jsx
try { (() => {
/**
 * Badge — compact status chip for AIVA OS.
 * Multiple variants cover all OS status contexts.
 */
function Badge({
  children,
  variant = 'default',
  style = {}
}) {
  const variants = {
    default: {
      background: 'var(--color-primary)',
      color: 'var(--color-primary-fg)',
      borderColor: 'transparent'
    },
    secondary: {
      background: 'var(--color-secondary)',
      color: 'var(--color-secondary-fg)',
      borderColor: 'transparent'
    },
    glass: {
      background: 'var(--glass-card-bg)',
      color: 'var(--glass-text-primary)',
      borderColor: 'var(--glass-card-border)',
      backdropFilter: 'blur(12px)',
      WebkitBackdropFilter: 'blur(12px)'
    },
    destructive: {
      background: 'var(--color-destructive)',
      color: 'var(--color-destructive-fg)',
      borderColor: 'transparent'
    },
    success: {
      background: 'rgba(34, 197, 94, 0.18)',
      color: '#16a34a',
      borderColor: 'rgba(34, 197, 94, 0.3)'
    },
    warning: {
      background: 'rgba(245, 158, 11, 0.18)',
      color: '#d97706',
      borderColor: 'rgba(245, 158, 11, 0.3)'
    },
    outline: {
      background: 'transparent',
      color: 'var(--glass-text-primary)',
      borderColor: 'var(--glass-divider)'
    },
    active: {
      background: 'var(--glass-toggle-on-bg)',
      color: '#ffffff',
      borderColor: 'var(--glass-toggle-on-border)'
    }
  };
  const v = variants[variant] || variants.default;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: 22,
      padding: '0 8px',
      fontSize: 11,
      fontFamily: 'var(--font-sans)',
      fontWeight: 'var(--font-weight-medium)',
      lineHeight: 1,
      borderRadius: 6,
      border: '1px solid',
      whiteSpace: 'nowrap',
      flexShrink: 0,
      ...v,
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Badge.jsx", error: String((e && e.message) || e) }); }

// components/core/GlassButton.jsx
try { (() => {
const {
  useState
} = React;
/**
 * GlassButton — liquid-glass styled button for the AIVA OS UI.
 * Reads from --glass-* and --color-* CSS custom properties.
 * Automatically adapts to dark/light mode via the .dark class on an ancestor.
 */
function GlassButton({
  children,
  variant = 'glass',
  size = 'md',
  disabled = false,
  onClick,
  icon,
  style = {}
}) {
  const [hovered, setHovered] = useState(false);
  const [pressed, setPressed] = useState(false);
  const [focused, setFocused] = useState(false);
  const sizes = {
    sm: {
      height: 30,
      padding: '0 10px',
      fontSize: 12,
      gap: 5,
      borderRadius: 8
    },
    md: {
      height: 36,
      padding: '0 16px',
      fontSize: 14,
      gap: 6,
      borderRadius: 10
    },
    lg: {
      height: 42,
      padding: '0 22px',
      fontSize: 15,
      gap: 7,
      borderRadius: 12
    },
    icon: {
      width: 36,
      height: 36,
      padding: 0,
      fontSize: 14,
      gap: 0,
      borderRadius: '50%'
    }
  };
  const s = sizes[size] || sizes.md;
  const variantStyle = {
    default: {
      background: 'var(--color-primary)',
      borderColor: 'transparent',
      color: 'var(--color-primary-fg)',
      backdropFilter: 'none'
    },
    glass: {
      background: hovered ? 'var(--glass-hover-bg)' : 'var(--glass-panel-bg)',
      borderColor: 'var(--glass-panel-border)',
      color: 'var(--glass-text-primary)',
      backdropFilter: 'blur(20px) saturate(1.4)',
      WebkitBackdropFilter: 'blur(20px) saturate(1.4)',
      boxShadow: pressed ? 'inset 0 2px 6px rgba(0,0,0,0.14), inset 0 1px 0 rgba(255,255,255,0.10)' : hovered ? '0 6px 18px rgba(0,0,0,0.16), inset 0 1px 0 rgba(255,255,255,0.30)' : '0 2px 8px rgba(0,0,0,0.10), inset 0 1px 0 rgba(255,255,255,0.22)'
    },
    ghost: {
      background: hovered ? 'var(--glass-hover-bg)' : 'transparent',
      borderColor: 'transparent',
      color: 'var(--glass-text-primary)'
    },
    destructive: {
      background: 'var(--color-destructive)',
      borderColor: 'transparent',
      color: 'var(--color-destructive-fg)'
    },
    active: {
      background: 'var(--glass-toggle-on-bg)',
      borderColor: 'var(--glass-toggle-on-border)',
      color: '#ffffff',
      boxShadow: '0 4px 15px var(--glass-toggle-on-shadow)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)'
    },
    secondary: {
      background: 'var(--color-secondary)',
      borderColor: 'var(--color-border)',
      color: 'var(--color-secondary-fg)'
    }
  };
  const v = variantStyle[variant] || variantStyle.glass;
  return /*#__PURE__*/React.createElement("button", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: s.gap,
      height: s.height,
      width: s.width,
      padding: s.padding,
      fontSize: s.fontSize,
      fontFamily: 'var(--font-sans)',
      fontWeight: 'var(--font-weight-medium)',
      lineHeight: 1,
      letterSpacing: '-0.01em',
      border: '1px solid',
      borderRadius: s.borderRadius,
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.5 : 1,
      transform: pressed ? 'scale(0.95)' : hovered ? 'scale(1.02)' : 'scale(1)',
      // Press hits instantly; release springs back with slight overshoot.
      transition: pressed ? 'transform var(--motion-instant, 0.1s) ease-out, background var(--motion-swift, 0.16s) ease, box-shadow var(--motion-swift, 0.16s) ease' : 'transform var(--motion-quick, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), background var(--motion-swift, 0.16s) ease, box-shadow var(--motion-quick, 0.28s) ease',
      outline: 'none',
      ...(focused && !pressed ? {
        boxShadow: '0 0 0 3px rgba(59,130,246,0.35), 0 2px 8px rgba(0,0,0,0.10)'
      } : null),
      whiteSpace: 'nowrap',
      userSelect: 'none',
      WebkitTapHighlightColor: 'transparent',
      ...v,
      ...style
    },
    disabled: disabled,
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHovered(true),
    onMouseLeave: () => {
      setHovered(false);
      setPressed(false);
    },
    onMouseDown: () => setPressed(true),
    onMouseUp: () => setPressed(false),
    onTouchStart: () => setPressed(true),
    onTouchEnd: () => setPressed(false),
    onFocus: e => setFocused(e.target.matches(':focus-visible')),
    onBlur: () => setFocused(false)
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      flexShrink: 0
    }
  }, icon), children);
}
Object.assign(__ds_scope, { GlassButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/GlassButton.jsx", error: String((e && e.message) || e) }); }

// components/core/GlassCard.jsx
try { (() => {
/**
 * GlassCard — frosted glass container panel for AIVA OS.
 * The foundational surface for panels, dock pills, and nested cards.
 * Reads all styling from --glass-* CSS custom properties.
 */
function GlassCard({
  children,
  variant = 'panel',
  style = {},
  onClick,
  as: Tag = 'div'
}) {
  const blurs = {
    panel: '40px',
    dock: '64px',
    card: '20px',
    subtle: '12px',
    inner: '20px'
  };
  const blur = blurs[variant] || '40px';
  const variantStyles = {
    panel: {
      background: 'var(--glass-panel-bg)',
      border: '1px solid var(--glass-panel-border)',
      boxShadow: 'var(--glass-panel-shadow)',
      borderRadius: 'var(--radius-3xl)'
    },
    dock: {
      background: 'var(--glass-panel-bg)',
      border: '1px solid var(--glass-panel-border)',
      boxShadow: 'var(--glass-panel-shadow)',
      borderRadius: 'var(--radius-3xl)'
    },
    card: {
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      boxShadow: '0 4px 16px rgba(0,0,0,0.10)',
      borderRadius: 'var(--radius-2xl)'
    },
    inner: {
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      boxShadow: 'none',
      borderRadius: 'var(--radius-2xl)'
    },
    subtle: {
      background: 'var(--glass-subtle-bg)',
      border: 'none',
      boxShadow: 'none',
      borderRadius: 'var(--radius-2xl)'
    }
  };
  const v = variantStyles[variant] || variantStyles.panel;
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      backdropFilter: `blur(${blur})`,
      WebkitBackdropFilter: `blur(${blur})`,
      overflow: 'hidden',
      ...v,
      ...style
    },
    onClick: onClick
  }, children);
}
Object.assign(__ds_scope, { GlassCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/GlassCard.jsx", error: String((e && e.message) || e) }); }

// components/core/GlassToggle.jsx
try { (() => {
const {
  useState
} = React;
/**
 * GlassToggle — iOS/macOS-style on/off switch for AIVA OS.
 * Used in Control Center, Settings, and anywhere a binary state toggle is needed.
 */
function GlassToggle({
  checked,
  defaultChecked = false,
  onChange,
  label,
  disabled = false,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultChecked);
  const [pressed, setPressed] = useState(false);
  const isOn = checked !== undefined ? checked : internal;
  // Pressed thumb stretches along the travel axis (liquid feel), anchored to its side.
  const thumbW = pressed ? 26 : 20;
  const toggle = () => {
    if (disabled) return;
    const next = !isOn;
    if (checked === undefined) setInternal(next);
    onChange && onChange(next);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: label ? 10 : 0,
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.45 : 1,
      userSelect: 'none',
      WebkitTapHighlightColor: 'transparent',
      ...style
    },
    onClick: toggle,
    onMouseDown: () => setPressed(true),
    onMouseUp: () => setPressed(false),
    onMouseLeave: () => setPressed(false),
    onTouchStart: () => setPressed(true),
    onTouchEnd: () => setPressed(false),
    role: "switch",
    "aria-checked": isOn
  }, label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      fontFamily: 'var(--font-sans)',
      fontWeight: 'var(--font-weight-medium)',
      color: 'var(--glass-text-primary)',
      flex: 1
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 44,
      height: 26,
      borderRadius: 13,
      background: isOn ? 'var(--glass-toggle-on-bg)' : 'var(--glass-toggle-off-bg)',
      border: `1px solid ${isOn ? 'var(--glass-toggle-on-border)' : 'var(--glass-toggle-off-border)'}`,
      backdropFilter: 'blur(8px) saturate(1.4)',
      WebkitBackdropFilter: 'blur(8px) saturate(1.4)',
      boxShadow: isOn ? '0 2px 8px var(--glass-toggle-on-shadow), inset 0 1px 2px rgba(0,0,0,0.10)' : 'inset 0 1px 3px rgba(0,0,0,0.12), 0 1px 2px rgba(0,0,0,0.06)',
      transition: 'background var(--motion-quick, 0.28s) ease, border-color var(--motion-quick, 0.28s) ease, box-shadow var(--motion-quick, 0.28s) ease',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 2,
      left: isOn ? `calc(100% - ${thumbW + 4}px)` : 2,
      width: thumbW,
      height: 20,
      borderRadius: 10,
      background: 'linear-gradient(to bottom, #ffffff, #f2f3f6)',
      boxShadow: '0 1px 4px rgba(0,0,0,0.28), 0 2px 8px rgba(0,0,0,0.12), inset 0 -1px 1px rgba(0,0,0,0.05)',
      transition: 'left var(--motion-quick, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), width var(--motion-quick, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), border-radius var(--motion-quick, 0.28s) ease'
    }
  })));
}
Object.assign(__ds_scope, { GlassToggle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/GlassToggle.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Banner.jsx
try { (() => {
/**
 * Banner — inline status/notification strip for AIVA OS (maps to ArkUI
 * ExceptionPrompt). A glass bar with a status accent, icon, title/message,
 * optional action, and dismiss button. Render inline (not floating like Toast).
 */
function Banner({
  title,
  message,
  variant = 'info',
  icon,
  action,
  onAction,
  onDismiss,
  style = {}
}) {
  const accent = {
    info: 'var(--color-info)',
    success: 'var(--color-success)',
    warning: 'var(--color-warning)',
    destructive: 'var(--color-destructive)'
  }[variant] || 'var(--color-info)';
  const defaultIcon = {
    info: 'M12 8h.01M11 12h1v4h1',
    success: 'M4 12.5l5 5L20 6.5',
    warning: 'M12 9v4M12 17h.01M10.3 3.9L2.4 18a2 2 0 001.7 3h15.8a2 2 0 001.7-3L13.7 3.9a2 2 0 00-3.4 0z',
    destructive: 'M15 9l-6 6M9 9l6 6'
  }[variant];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      width: '100%',
      padding: '13px 14px',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      borderLeft: `3px solid ${accent}`,
      borderRadius: 'var(--radius-lg)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0,
      color: accent,
      marginTop: 1
    }
  }, icon || /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: defaultIcon,
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, title && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 600,
      color: 'var(--glass-text-strong)'
    }
  }, title), message && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 400,
      color: 'var(--glass-text-primary)',
      lineHeight: 1.45
    }
  }, message), action && /*#__PURE__*/React.createElement("button", {
    onClick: onAction,
    style: {
      alignSelf: 'flex-start',
      marginTop: 5,
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      padding: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 600,
      color: accent,
      WebkitTapHighlightColor: 'transparent'
    }
  }, action)), onDismiss && /*#__PURE__*/React.createElement("button", {
    onClick: onDismiss,
    "aria-label": "Schlie\xDFen",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
      width: 22,
      height: 22,
      borderRadius: '50%',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      background: 'transparent',
      color: 'var(--glass-text-secondary)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round"
  }))));
}
Object.assign(__ds_scope, { Banner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Banner.jsx", error: String((e && e.message) || e) }); }

// components/feedback/ProgressBar.jsx
try { (() => {
/**
 * ProgressBar — determinate / indeterminate progress for AIVA OS.
 * A glass track with a blue fill; indeterminate mode shows a sliding pip.
 */
function ProgressBar({
  value = 0,
  indeterminate = false,
  height = 6,
  color,
  style = {}
}) {
  const fill = color || 'var(--glass-toggle-on-bg)';
  const pct = Math.max(0, Math.min(100, value));
  const id = React.useId ? React.useId().replace(/:/g, '') : 'pb';
  return /*#__PURE__*/React.createElement("div", {
    role: "progressbar",
    "aria-valuenow": indeterminate ? undefined : pct,
    style: {
      position: 'relative',
      width: '100%',
      height,
      borderRadius: 999,
      overflow: 'hidden',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(8px)',
      WebkitBackdropFilter: 'blur(8px)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, `@keyframes prog-${id}{0%{left:-40%}100%{left:100%}}`), indeterminate ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      width: '40%',
      borderRadius: 999,
      background: fill,
      boxShadow: '0 0 8px var(--glass-toggle-on-shadow)',
      animation: `prog-${id} 1.1s cubic-bezier(0.4,0,0.2,1) infinite`
    }
  }) : /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      width: `${pct}%`,
      borderRadius: 999,
      background: fill,
      boxShadow: '0 0 8px var(--glass-toggle-on-shadow)',
      transition: 'width 0.3s cubic-bezier(0.4,0,0.2,1)'
    }
  }));
}
Object.assign(__ds_scope, { ProgressBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/ProgressBar.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Refresher.jsx
try { (() => {
const {
  useRef,
  useState
} = React;
/**
 * Refresher — pull-to-refresh wrapper for AIVA OS (maps to ArkUI
 * SwipeRefresher). Wrap a scrollable area; pulling down past the top reveals a
 * glass spinner and fires onRefresh. Call the provided done() to end.
 */
function Refresher({
  onRefresh,
  threshold = 70,
  children,
  style = {}
}) {
  const [pull, setPull] = useState(0);
  const [refreshing, setRefreshing] = useState(false);
  const start = useRef(null);
  const scroller = useRef(null);
  const onTouchStart = e => {
    if (scroller.current && scroller.current.scrollTop <= 0) start.current = e.touches[0].clientY;else start.current = null;
  };
  const onTouchMove = e => {
    if (start.current == null || refreshing) return;
    const dy = e.touches[0].clientY - start.current;
    if (dy > 0) {
      setPull(Math.min(dy * 0.5, threshold * 1.5));
    }
  };
  const onTouchEnd = () => {
    if (pull >= threshold && !refreshing) {
      setRefreshing(true);
      setPull(threshold);
      const done = () => {
        setRefreshing(false);
        setPull(0);
      };
      const r = onRefresh && onRefresh(done);
      if (r && typeof r.then === 'function') r.then(done);
    } else {
      setPull(0);
    }
    start.current = null;
  };
  const progress = Math.min(1, pull / threshold);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: '100%',
      overflow: 'hidden',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: threshold,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      transform: `translateY(${pull - threshold}px)`,
      transition: start.current ? 'none' : 'transform 0.25s ease',
      pointerEvents: 'none',
      zIndex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 32,
      height: 32,
      borderRadius: '50%',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "17",
    height: "17",
    viewBox: "0 0 24 24",
    fill: "none",
    style: {
      color: 'var(--glass-text-primary)',
      transform: `rotate(${progress * 270}deg)`,
      animation: refreshing ? 'refresh-spin 0.8s linear infinite' : 'none'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 12a9 9 0 11-2.6-6.4M21 3v6h-6",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })))), /*#__PURE__*/React.createElement("style", null, `@keyframes refresh-spin{to{transform:rotate(360deg)}}`), /*#__PURE__*/React.createElement("div", {
    ref: scroller,
    onTouchStart: onTouchStart,
    onTouchMove: onTouchMove,
    onTouchEnd: onTouchEnd,
    style: {
      height: '100%',
      overflowY: 'auto',
      transform: `translateY(${pull}px)`,
      transition: start.current ? 'none' : 'transform 0.25s ease'
    }
  }, children));
}
Object.assign(__ds_scope, { Refresher });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Refresher.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Skeleton.jsx
try { (() => {
/**
 * Skeleton — shimmering glass placeholder for loading states.
 * Render in the shape of the content that's loading (lines, blocks, circles).
 */
function Skeleton({
  width = '100%',
  height = 14,
  radius,
  circle = false,
  style = {}
}) {
  const id = React.useId ? React.useId().replace(/:/g, '') : 'sk';
  const r = circle ? '50%' : radius != null ? radius : 'var(--radius-md)';
  const dim = circle ? typeof height === 'number' ? height : 40 : null;
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": true,
    style: {
      display: 'inline-block',
      width: circle ? dim || height : width,
      height: circle ? dim || height : height,
      borderRadius: r,
      position: 'relative',
      overflow: 'hidden',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, `@keyframes shimmer-${id}{100%{transform:translateX(100%)}}`), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      transform: 'translateX(-100%)',
      background: 'linear-gradient(90deg, transparent, var(--glass-hover-bg), transparent)',
      animation: `shimmer-${id} 1.4s ease-in-out infinite`
    }
  }));
}

/**
 * SkeletonText — a stack of skeleton lines (last line shorter), for paragraphs.
 */
function SkeletonText({
  lines = 3,
  gap = 8,
  style = {}
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap,
      width: '100%',
      ...style
    }
  }, Array.from({
    length: lines
  }).map((_, i) => /*#__PURE__*/React.createElement(Skeleton, {
    key: i,
    height: 12,
    width: i === lines - 1 ? '60%' : '100%'
  })));
}
Object.assign(__ds_scope, { Skeleton, SkeletonText });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Skeleton.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Spinner.jsx
try { (() => {
/**
 * Spinner — iOS-style activity indicator for AIVA OS (maps to ion-loading).
 * 12 tapered spokes fading around a circle. Pure CSS animation.
 */
function Spinner({
  size = 28,
  color,
  style = {}
}) {
  const spokeColor = color || 'var(--glass-text-primary)';
  const spokes = Array.from({
    length: 12
  });
  const id = React.useId ? React.useId().replace(/:/g, '') : 'sp';
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    "aria-label": "L\xE4dt",
    style: {
      position: 'relative',
      width: size,
      height: size,
      display: 'inline-block',
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, `@keyframes spin-${id}{to{transform:rotate(360deg)}}`), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      animation: `spin-${id} 1s steps(12) infinite`
    }
  }, spokes.map((_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      width: Math.max(2, size * 0.075),
      height: size * 0.26,
      marginLeft: -Math.max(2, size * 0.075) / 2,
      borderRadius: 999,
      background: spokeColor,
      opacity: (i + 1) / 12,
      transform: `rotate(${i * 30}deg) translateY(-${size * 0.34}px)`,
      transformOrigin: `center ${size * 0.34}px`
    }
  }))));
}
Object.assign(__ds_scope, { Spinner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Spinner.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Toast.jsx
try { (() => {
const {
  useEffect
} = React;
/**
 * Toast — transient glass notification for AIVA OS (maps to ion-toast).
 * A floating frosted pill with optional icon and action button.
 * Render conditionally on `open`; auto-dismisses after `duration` ms.
 */
function Toast({
  open = true,
  message,
  icon,
  action,
  onAction,
  onClose,
  duration = 4000,
  position = 'bottom',
  variant = 'default',
  style = {}
}) {
  useEffect(() => {
    if (!open || !duration) return;
    const t = setTimeout(() => onClose && onClose(), duration);
    return () => clearTimeout(t);
  }, [open, duration, onClose]);
  if (!open) return null;
  const accent = variant === 'success' ? 'var(--color-success)' : variant === 'destructive' ? 'var(--color-destructive)' : variant === 'warning' ? 'var(--color-warning)' : null;
  const pos = position === 'top' ? {
    top: 16
  } : position === 'center' ? {
    top: '50%',
    transform: 'translate(-50%, -50%)'
  } : {
    bottom: 16
  };
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      position: 'fixed',
      left: '50%',
      transform: pos.transform || 'translateX(-50%)',
      ...(pos.top !== undefined ? {
        top: pos.top
      } : {
        bottom: pos.bottom
      }),
      zIndex: 'var(--z-modal, 30)',
      display: 'flex',
      alignItems: 'center',
      gap: 11,
      maxWidth: 'min(440px, calc(100vw - 32px))',
      padding: '12px 16px',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-full)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      animation: position === 'center' ? 'nx-scrim-in var(--motion-quick, 0.28s) ease both' : `${position === 'top' ? 'nx-toast-drop' : 'nx-toast-rise'} var(--motion-slow, 0.5s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both`,
      ...style
    }
  }, accent && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: '50%',
      background: accent,
      flexShrink: 0,
      boxShadow: `0 0 8px ${accent}`
    }
  }), icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      color: 'var(--glass-text-primary)',
      flexShrink: 0
    }
  }, icon), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 500,
      color: 'var(--glass-text-strong)',
      lineHeight: 1.35
    }
  }, message), action && /*#__PURE__*/React.createElement("button", {
    onClick: () => {
      onAction && onAction();
    },
    style: {
      flexShrink: 0,
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 600,
      color: 'var(--color-info)',
      padding: '2px 4px',
      WebkitTapHighlightColor: 'transparent'
    }
  }, action));
}
Object.assign(__ds_scope, { Toast });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Toast.jsx", error: String((e && e.message) || e) }); }

// components/inputs/SearchBar.jsx
try { (() => {
const {
  useState
} = React;
/**
 * SearchBar — iOS-style glass search field for AIVA OS (maps to ion-searchbar).
 * Rounded glass pill with a leading magnifier and a clear (✕) button.
 */
function SearchBar({
  value,
  defaultValue = '',
  onChange,
  onSubmit,
  placeholder = 'Suchen',
  disabled = false,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const val = value !== undefined ? value : internal;
  const [focused, setFocused] = useState(false);
  const set = v => {
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      height: 38,
      padding: '0 12px',
      width: '100%',
      background: 'var(--glass-card-bg)',
      border: `1px solid ${focused ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)'}`,
      borderRadius: 'var(--radius-full)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: focused ? '0 0 0 3px var(--glass-toggle-on-shadow)' : 'inset 0 1px 1px rgba(255,255,255,0.18)',
      transition: 'border-color 0.15s ease, box-shadow 0.15s ease',
      opacity: disabled ? 0.5 : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    style: {
      color: 'var(--glass-text-secondary)',
      flexShrink: 0
    },
    "aria-hidden": true
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7",
    stroke: "currentColor",
    strokeWidth: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M20 20l-3.2-3.2",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  })), /*#__PURE__*/React.createElement("input", {
    value: val,
    disabled: disabled,
    placeholder: placeholder,
    onChange: e => set(e.target.value),
    onFocus: () => setFocused(true),
    onBlur: () => setFocused(false),
    onKeyDown: e => {
      if (e.key === 'Enter' && onSubmit) onSubmit(val);
    },
    style: {
      flex: 1,
      minWidth: 0,
      border: 'none',
      outline: 'none',
      background: 'transparent',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 500,
      color: 'var(--glass-text-strong)',
      padding: 0
    }
  }), val && /*#__PURE__*/React.createElement("button", {
    onClick: () => set(''),
    "aria-label": "L\xF6schen",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
      width: 18,
      height: 18,
      borderRadius: '50%',
      border: 'none',
      cursor: 'pointer',
      background: 'var(--glass-text-secondary)',
      color: 'var(--glass-card-bg)',
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "10",
    height: "10",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18",
    stroke: "currentColor",
    strokeWidth: "3",
    strokeLinecap: "round"
  }))));
}
Object.assign(__ds_scope, { SearchBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/inputs/SearchBar.jsx", error: String((e && e.message) || e) }); }

// components/inputs/TextArea.jsx
try { (() => {
const {
  useState
} = React;
/**
 * TextArea — multiline glass text input for AIVA OS.
 * Frosted surface with optional label, helper/error text, char counter,
 * and auto-grow. Controlled or uncontrolled.
 */
function TextArea({
  value,
  defaultValue = '',
  onChange,
  placeholder,
  label,
  rows = 4,
  maxLength,
  showCount = false,
  autoGrow = false,
  error,
  helper,
  disabled = false,
  style = {},
  textareaStyle = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const val = value !== undefined ? value : internal;
  const [focused, setFocused] = useState(false);
  const ref = React.useRef(null);
  const handle = e => {
    const v = e.target.value;
    if (maxLength != null && v.length > maxLength) return;
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  React.useEffect(() => {
    if (autoGrow && ref.current) {
      ref.current.style.height = 'auto';
      ref.current.style.height = ref.current.scrollHeight + 'px';
    }
  }, [val, autoGrow]);
  const borderColor = error ? 'var(--color-destructive)' : focused ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)';
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      opacity: disabled ? 0.5 : 1,
      ...style
    }
  }, label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      color: 'var(--glass-text-secondary)',
      letterSpacing: '0.01em',
      paddingLeft: 2
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 13px',
      background: 'var(--glass-card-bg)',
      border: `1px solid ${borderColor}`,
      borderRadius: 'var(--radius-lg)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: focused ? '0 0 0 3px var(--glass-toggle-on-shadow)' : 'inset 0 1px 1px rgba(255,255,255,0.18)',
      transition: 'border-color 0.15s ease, box-shadow 0.15s ease'
    }
  }, /*#__PURE__*/React.createElement("textarea", {
    ref: ref,
    value: val,
    onChange: handle,
    placeholder: placeholder,
    disabled: disabled,
    rows: rows,
    onFocus: () => setFocused(true),
    onBlur: () => setFocused(false),
    style: {
      width: '100%',
      border: 'none',
      outline: 'none',
      background: 'transparent',
      resize: autoGrow ? 'none' : 'vertical',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 500,
      lineHeight: 1.5,
      color: 'var(--glass-text-strong)',
      display: 'block',
      padding: 0,
      ...textareaStyle
    }
  })), (error || helper || showCount && maxLength != null) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      gap: 8,
      paddingLeft: 2,
      fontSize: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: error ? 'var(--color-destructive)' : 'var(--glass-text-secondary)'
    }
  }, error || helper), showCount && maxLength != null && /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--glass-text-secondary)',
      fontVariantNumeric: 'tabular-nums',
      flexShrink: 0,
      marginLeft: 'auto'
    }
  }, val.length, "/", maxLength)));
}
Object.assign(__ds_scope, { TextArea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/inputs/TextArea.jsx", error: String((e && e.message) || e) }); }

// components/inputs/TextField.jsx
try { (() => {
const {
  useState
} = React;
/**
 * TextField — glass text input for AIVA OS.
 * A frosted input with optional leading icon, label, and helper/error text.
 * Reads --glass-* tokens; works on light and dark wallpapers.
 */
function TextField({
  value,
  defaultValue = '',
  onChange,
  placeholder,
  label,
  type = 'text',
  icon,
  trailing,
  error,
  helper,
  disabled = false,
  size = 'md',
  style = {},
  inputStyle = {}
}) {
  const [internal, setInternal] = useState(defaultValue);
  const val = value !== undefined ? value : internal;
  const [focused, setFocused] = useState(false);
  const s = size === 'sm' ? {
    h: 32,
    fs: 13,
    pad: 11
  } : size === 'lg' ? {
    h: 46,
    fs: 16,
    pad: 16
  } : {
    h: 38,
    fs: 14,
    pad: 13
  };
  const handle = e => {
    if (value === undefined) setInternal(e.target.value);
    onChange && onChange(e.target.value);
  };
  const borderColor = error ? 'var(--color-destructive)' : focused ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)';
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      opacity: disabled ? 0.5 : 1,
      ...style
    }
  }, label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      color: 'var(--glass-text-secondary)',
      letterSpacing: '0.01em',
      paddingLeft: 2
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      height: s.h,
      padding: `0 ${s.pad}px`,
      background: 'var(--glass-card-bg)',
      border: `1px solid ${borderColor}`,
      borderRadius: 'var(--radius-lg)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: focused ? '0 0 0 3px var(--glass-toggle-on-shadow)' : 'inset 0 1px 1px rgba(255,255,255,0.18)',
      transition: 'border-color 0.15s ease, box-shadow 0.15s ease'
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      color: 'var(--glass-text-secondary)',
      flexShrink: 0
    }
  }, icon), /*#__PURE__*/React.createElement("input", {
    type: type,
    value: val,
    onChange: handle,
    placeholder: placeholder,
    disabled: disabled,
    onFocus: () => setFocused(true),
    onBlur: () => setFocused(false),
    style: {
      flex: 1,
      minWidth: 0,
      border: 'none',
      outline: 'none',
      background: 'transparent',
      fontFamily: 'var(--font-sans)',
      fontSize: s.fs,
      fontWeight: 500,
      color: 'var(--glass-text-strong)',
      padding: 0,
      ...inputStyle
    }
  }), trailing && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      color: 'var(--glass-text-secondary)',
      flexShrink: 0
    }
  }, trailing)), (error || helper) && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      paddingLeft: 2,
      color: error ? 'var(--color-destructive)' : 'var(--glass-text-secondary)'
    }
  }, error || helper));
}
Object.assign(__ds_scope, { TextField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/inputs/TextField.jsx", error: String((e && e.message) || e) }); }

// components/keyboard/KeyboardKey.jsx
try { (() => {
const {
  useRef,
  useState
} = React;
/**
 * KeyboardKey — a single glass key for the OnScreenKeyboard.
 * Pointer-driven (never steals focus), springy press feedback, optional auto-repeat.
 */
function KeyboardKey({
  label,
  sub,
  variant = 'key',
  flex = 1,
  height = 44,
  fontSize,
  repeat = false,
  disabled = false,
  ariaLabel,
  onPress,
  style = {},
  children
}) {
  const [down, setDown] = useState(false);
  const timers = useRef({});
  const stop = () => {
    clearTimeout(timers.current.t);
    clearInterval(timers.current.i);
    timers.current = {};
  };
  const start = e => {
    e.preventDefault();
    if (disabled) return;
    setDown(true);
    onPress && onPress();
    if (repeat) {
      timers.current.t = setTimeout(() => {
        timers.current.i = setInterval(() => onPress && onPress(), 60);
      }, 380);
    }
  };
  const end = () => {
    setDown(false);
    stop();
  };
  const skin = {
    key: {
      bg: 'var(--glass-card-bg)',
      border: 'var(--glass-card-border)',
      color: 'var(--glass-text-strong)'
    },
    fn: {
      bg: 'var(--glass-subtle-bg)',
      border: 'transparent',
      color: 'var(--glass-text-primary)'
    },
    accent: {
      bg: 'var(--glass-toggle-on-bg)',
      border: 'var(--glass-toggle-on-border)',
      color: '#fff'
    },
    ghost: {
      bg: 'transparent',
      border: 'transparent',
      color: 'var(--glass-text-secondary)'
    }
  }[variant] || {};
  const fs = fontSize || Math.max(13, Math.min(21, Math.round(height * 0.42)));
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": ariaLabel || (typeof label === 'string' ? label : undefined),
    onPointerDown: start,
    onPointerUp: end,
    onPointerLeave: end,
    onPointerCancel: end,
    onContextMenu: e => e.preventDefault(),
    style: {
      position: 'relative',
      flex: `${flex} 1 0`,
      minWidth: 0,
      height,
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 1,
      background: down && variant !== 'accent' ? 'var(--glass-active-bg)' : skin.bg,
      border: `1px solid ${skin.border}`,
      borderRadius: Math.max(6, Math.round(height * 0.17)),
      color: skin.color,
      fontFamily: 'var(--font-sans)',
      fontSize: fs,
      fontWeight: 500,
      lineHeight: 1,
      boxShadow: variant === 'ghost' ? 'none' : '0 1px 2px rgba(0,0,0,0.18), inset 0 1px 0 rgba(255,255,255,0.14)',
      backdropFilter: 'blur(var(--glass-blur-md))',
      WebkitBackdropFilter: 'blur(var(--glass-blur-md))',
      cursor: disabled ? 'default' : 'pointer',
      padding: 0,
      userSelect: 'none',
      touchAction: 'none',
      opacity: disabled ? 0.4 : 1,
      transform: down ? 'scale(0.94)' : 'scale(1)',
      transition: down ? 'transform 0.08s linear, background 0.08s linear' : 'transform var(--motion-spring-soft-duration, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), background 0.16s ease',
      ...style
    }
  }, children != null ? children : label, sub ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: Math.max(8, fs * 0.5),
      fontWeight: 600,
      opacity: 0.45
    }
  }, sub) : null);
}
Object.assign(__ds_scope, { KeyboardKey });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/keyboard/KeyboardKey.jsx", error: String((e && e.message) || e) }); }

// components/keyboard/keyboard-engine.js
try { (() => {
/* keyboard-engine.js — layouts, dictionaries and IME logic for OnScreenKeyboard.
   Pure data + pure functions, no React. Covers:
   · latin layouts (de QWERTZ / en QWERTY) + symbol layers
   · hangul 2-set (dubeolsik) syllable composition
   · romaji → kana conversion + 12-key kana tap-cycling + kana → kanji candidates
   · pinyin → hanzi candidates
   Dictionaries are intentionally small demo sets — swap for a real IME service. */

const LANGS = {
  de: {
    label: 'Deutsch',
    short: 'DE',
    kind: 'latin',
    space: 'Leerzeichen',
    ret: 'Return'
  },
  en: {
    label: 'English',
    short: 'EN',
    kind: 'latin',
    space: 'space',
    ret: 'return'
  },
  ko: {
    label: '한국어',
    short: 'KO',
    kind: 'hangul',
    space: '스페이스',
    ret: '입력'
  },
  ja: {
    label: '日本語',
    short: 'JA',
    kind: 'kana',
    space: '空白',
    ret: '改行'
  },
  zh: {
    label: '中文',
    short: 'ZH',
    kind: 'pinyin',
    space: '空格',
    ret: '换行'
  }
};
const LETTER_ROWS = {
  de: ['qwertzuiopü', 'asdfghjklöä', 'yxcvbnm'],
  en: ['qwertyuiop', 'asdfghjkl', 'zxcvbnm'],
  ja: ['qwertyuiop', 'asdfghjkl', 'zxcvbnm'],
  zh: ['qwertyuiop', 'asdfghjkl', 'zxcvbnm'],
  ko: ['ㅂㅈㄷㄱㅅㅛㅕㅑㅐㅔ', 'ㅁㄴㅇㄹㅎㅗㅓㅏㅣ', 'ㅋㅌㅊㅍㅠㅜㅡ']
};
const HANGUL_SHIFT = {
  'ㅂ': 'ㅃ',
  'ㅈ': 'ㅉ',
  'ㄷ': 'ㄸ',
  'ㄱ': 'ㄲ',
  'ㅅ': 'ㅆ',
  'ㅐ': 'ㅒ',
  'ㅔ': 'ㅖ'
};
const NUMBER_ROW = '1234567890';
const SYMBOL_LAYERS = {
  num: ['1234567890', '-/:;()€&@"', '.,?!\'ß„“'],
  sym: ['[]{}#%^*+=', '_\\|~<>$£¥•', '.,?!\'§°…']
};

/* ───────────────────────── Hangul (dubeolsik) ───────────────────────── */

const CHO = ['ㄱ', 'ㄲ', 'ㄴ', 'ㄷ', 'ㄸ', 'ㄹ', 'ㅁ', 'ㅂ', 'ㅃ', 'ㅅ', 'ㅆ', 'ㅇ', 'ㅈ', 'ㅉ', 'ㅊ', 'ㅋ', 'ㅌ', 'ㅍ', 'ㅎ'];
const JUNG = ['ㅏ', 'ㅐ', 'ㅑ', 'ㅒ', 'ㅓ', 'ㅔ', 'ㅕ', 'ㅖ', 'ㅗ', 'ㅘ', 'ㅙ', 'ㅚ', 'ㅛ', 'ㅜ', 'ㅝ', 'ㅞ', 'ㅟ', 'ㅠ', 'ㅡ', 'ㅢ', 'ㅣ'];
const JONG = ['', 'ㄱ', 'ㄲ', 'ㄳ', 'ㄴ', 'ㄵ', 'ㄶ', 'ㄷ', 'ㄹ', 'ㄺ', 'ㄻ', 'ㄼ', 'ㄽ', 'ㄾ', 'ㄿ', 'ㅀ', 'ㅁ', 'ㅂ', 'ㅄ', 'ㅅ', 'ㅆ', 'ㅇ', 'ㅈ', 'ㅊ', 'ㅋ', 'ㅌ', 'ㅍ', 'ㅎ'];
const JUNG_COMBO = {
  'ㅗㅏ': 'ㅘ',
  'ㅗㅐ': 'ㅙ',
  'ㅗㅣ': 'ㅚ',
  'ㅜㅓ': 'ㅝ',
  'ㅜㅔ': 'ㅞ',
  'ㅜㅣ': 'ㅟ',
  'ㅡㅣ': 'ㅢ'
};
const JONG_COMBO = {
  'ㄱㅅ': 'ㄳ',
  'ㄴㅈ': 'ㄵ',
  'ㄴㅎ': 'ㄶ',
  'ㄹㄱ': 'ㄺ',
  'ㄹㅁ': 'ㄻ',
  'ㄹㅂ': 'ㄼ',
  'ㄹㅅ': 'ㄽ',
  'ㄹㅌ': 'ㄾ',
  'ㄹㅍ': 'ㄿ',
  'ㄹㅎ': 'ㅀ',
  'ㅂㅅ': 'ㅄ'
};
const split = map => Object.fromEntries(Object.entries(map).map(([k, v]) => [v, [k[0], k[1]]]));
const JUNG_SPLIT = split(JUNG_COMBO);
const JONG_SPLIT = split(JONG_COMBO);
const isJungseong = j => JUNG.indexOf(j) >= 0;

/** Render a composition buffer {cho,jung,jong} as text (a real syllable when possible). */
function hangulText(b) {
  if (!b) return '';
  const {
    cho,
    jung,
    jong
  } = b;
  if (cho && jung) {
    const ci = CHO.indexOf(cho),
      vi = JUNG.indexOf(jung),
      ji = jong ? JONG.indexOf(jong) : 0;
    if (ci >= 0 && vi >= 0 && ji >= 0) return String.fromCharCode(0xac00 + ci * 588 + vi * 28 + ji);
  }
  return (cho || '') + (jung || '') + (jong || '');
}

/** Feed one jamo. Returns { commit, buf } — commit is text that leaves the composition. */
function hangulPress(b, j) {
  const fresh = o => ({
    cho: null,
    jung: null,
    jong: null,
    ...o
  });
  if (isJungseong(j)) {
    if (b && b.cho && !b.jung) return {
      commit: '',
      buf: {
        ...b,
        jung: j
      }
    };
    if (b && b.jung && !b.jong) {
      const c = JUNG_COMBO[b.jung + j];
      if (c) return {
        commit: '',
        buf: {
          ...b,
          jung: c
        }
      };
      return {
        commit: hangulText(b),
        buf: fresh({
          jung: j
        })
      };
    }
    if (b && b.jong) {
      const sp = JONG_SPLIT[b.jong];
      const moved = sp ? sp[1] : b.jong;
      const rest = sp ? sp[0] : null;
      return {
        commit: hangulText({
          ...b,
          jong: rest
        }),
        buf: fresh({
          cho: moved,
          jung: j
        })
      };
    }
    return {
      commit: b ? hangulText(b) : '',
      buf: fresh({
        jung: j
      })
    };
  }
  if (!b) return {
    commit: '',
    buf: fresh({
      cho: j
    })
  };
  if (b.cho && !b.jung) return {
    commit: hangulText(b),
    buf: fresh({
      cho: j
    })
  };
  if (b.jung && !b.jong) {
    if (b.cho && JONG.indexOf(j) > 0) return {
      commit: '',
      buf: {
        ...b,
        jong: j
      }
    };
    return {
      commit: hangulText(b),
      buf: fresh({
        cho: j
      })
    };
  }
  if (b.jong) {
    const c = JONG_COMBO[b.jong + j];
    if (c) return {
      commit: '',
      buf: {
        ...b,
        jong: c
      }
    };
    return {
      commit: hangulText(b),
      buf: fresh({
        cho: j
      })
    };
  }
  return {
    commit: '',
    buf: fresh({
      cho: j
    })
  };
}

/** Delete the last jamo from a composition buffer. Returns a buffer or null. */
function hangulBack(b) {
  if (!b) return null;
  if (b.jong) {
    const sp = JONG_SPLIT[b.jong];
    return {
      ...b,
      jong: sp ? sp[0] : null
    };
  }
  if (b.jung) {
    const sp = JUNG_SPLIT[b.jung];
    if (sp) return {
      ...b,
      jung: sp[0]
    };
    return b.cho ? {
      ...b,
      jung: null
    } : null;
  }
  return null;
}

/* ───────────────────────── Japanese ───────────────────────── */

const V = ['a', 'i', 'u', 'e', 'o'];
const ROMA = {};
const put = (p, chars) => V.forEach((v, i) => {
  if (chars[i] && chars[i] !== '-') ROMA[p + v] = chars[i];
});
put('', [...'あいうえお']);
put('k', [...'かきくけこ']);
put('g', [...'がぎぐげご']);
put('s', [...'さしすせそ']);
put('z', [...'ざじずぜぞ']);
put('t', [...'たちつてと']);
put('d', [...'だぢづでど']);
put('n', [...'なにぬねの']);
put('h', [...'はひふへほ']);
put('b', [...'ばびぶべぼ']);
put('p', [...'ぱぴぷぺぽ']);
put('m', [...'まみむめも']);
put('r', [...'らりるれろ']);
put('y', ['や', '-', 'ゆ', '-', 'よ']);
put('w', ['わ', '-', '-', '-', 'を']);
put('f', ['ふぁ', 'ふぃ', 'ふ', 'ふぇ', 'ふぉ']);
const YO = ['ゃ', 'ぃ', 'ゅ', 'ぇ', 'ょ'];
const YBASE = {
  ky: 'き',
  gy: 'ぎ',
  sh: 'し',
  sy: 'し',
  j: 'じ',
  jy: 'じ',
  ch: 'ち',
  ty: 'ち',
  ny: 'に',
  hy: 'ひ',
  by: 'び',
  py: 'ぴ',
  my: 'み',
  ry: 'り'
};
Object.entries(YBASE).forEach(([p, c]) => V.forEach((v, i) => {
  ROMA[p + v] = i === 1 ? c + 'ぃ' : c + YO[i];
}));
Object.assign(ROMA, {
  shi: 'し',
  si: 'し',
  chi: 'ち',
  ci: 'ち',
  tsu: 'つ',
  tu: 'つ',
  fu: 'ふ',
  hu: 'ふ',
  ji: 'じ',
  zi: 'じ',
  di: 'ぢ',
  du: 'づ',
  '-': 'ー'
});

/** Convert a romaji buffer to kana. Returns { kana, rest } — rest is the un-convertible tail. */
function romajiToKana(s) {
  let out = '',
    i = 0;
  while (i < s.length) {
    if (s[i] === 'n' && (s[i + 1] === 'n' || s[i + 1] === "'")) {
      out += 'ん';
      i += 2;
      continue;
    }
    let m = null;
    for (let L = 3; L >= 1; L--) {
      const t = s.slice(i, i + L);
      if (ROMA[t]) {
        m = t;
        break;
      }
    }
    if (m) {
      out += ROMA[m];
      i += m.length;
      continue;
    }
    if (s[i] === s[i + 1] && V.indexOf(s[i]) < 0 && s[i] !== 'n') {
      out += 'っ';
      i += 1;
      continue;
    }
    if (s[i] === 'n' && s[i + 1] && V.indexOf(s[i + 1]) < 0 && s[i + 1] !== 'y') {
      out += 'ん';
      i += 1;
      continue;
    }
    break;
  }
  return {
    kana: out,
    rest: s.slice(i)
  };
}

/** 12-key kana pad: each key cycles through its column on repeated taps. */
const KANA_KEYS = [{
  label: 'あ',
  chars: 'あいうえお'
}, {
  label: 'か',
  chars: 'かきくけこ'
}, {
  label: 'さ',
  chars: 'さしすせそ'
}, {
  label: 'た',
  chars: 'たちつてと'
}, {
  label: 'な',
  chars: 'なにぬねの'
}, {
  label: 'は',
  chars: 'はひふへほ'
}, {
  label: 'ま',
  chars: 'まみむめも'
}, {
  label: 'や',
  chars: 'やゆよ'
}, {
  label: 'ら',
  chars: 'らりるれろ'
}, {
  label: 'わ',
  chars: 'わをんー'
}, {
  label: '、。?',
  chars: '、。？！…'
}];
const MOD_CYCLES = ['かが', 'きぎ', 'くぐ', 'けげ', 'こご', 'さざ', 'しじ', 'すず', 'せぜ', 'そぞ', 'ただ', 'ちぢ', 'つっづ', 'てで', 'とど', 'はばぱ', 'ひびぴ', 'ふぶぷ', 'へべぺ', 'ほぼぽ', 'あぁ', 'いぃ', 'うぅゔ', 'えぇ', 'おぉ', 'やゃ', 'ゆゅ', 'よょ', 'わゎ'];
const KANA_MOD = {};
MOD_CYCLES.forEach(cyc => [...cyc].forEach((c, i, arr) => {
  KANA_MOD[c] = arr[(i + 1) % arr.length];
}));
delete KANA_MOD['め'];
/** ゛小゜ key: cycle a kana through its voiced / small forms. */
const kanaModify = c => KANA_MOD[c] || c;
const KATA = s => s.replace(/[ぁ-ゖ]/g, c => String.fromCharCode(c.charCodeAt(0) + 0x60));
const JA_DICT = {
  'にほん': ['日本'],
  'にほんご': ['日本語'],
  'かんじ': ['漢字', '感じ', '幹事'],
  'こんにちは': ['今日は'],
  'ありがとう': ['有難う'],
  'かいぎ': ['会議'],
  'せってい': ['設定'],
  'あした': ['明日'],
  'きょう': ['今日', '京'],
  'でんわ': ['電話'],
  'しごと': ['仕事'],
  'わたし': ['私'],
  'たのしい': ['楽しい'],
  'よろしく': ['宜しく'],
  'ふぁいる': ['ファイル'],
  'きーぼーど': ['キーボード'],
  'めっせーじ': ['メッセージ'],
  'にゅうりょく': ['入力'],
  'へんかん': ['変換'],
  'かくてい': ['確定'],
  'ごめん': ['御免'],
  'おはよう': ['お早う']
};

/* ───────────────────────── Chinese (pinyin) ───────────────────────── */

const ZH_DICT = {
  ni: ['你', '尼', '泥'],
  hao: ['好', '号', '毫'],
  nihao: ['你好'],
  wo: ['我', '握'],
  shi: ['是', '时', '事', '十', '使'],
  de: ['的', '得', '地'],
  zhong: ['中', '种', '重'],
  guo: ['国', '过', '果'],
  zhongguo: ['中国'],
  xie: ['谢', '写', '些'],
  xiexie: ['谢谢'],
  zai: ['在', '再', '载'],
  jian: ['见', '件', '建', '简'],
  zaijian: ['再见'],
  she: ['设', '社', '射'],
  zhi: ['置', '只', '知', '直'],
  shezhi: ['设置'],
  wen: ['文', '问', '温'],
  wenjian: ['文件'],
  jianpan: ['键盘'],
  pan: ['盘', '判'],
  hui: ['会', '回', '汇'],
  yi: ['议', '一', '以', '已'],
  huiyi: ['会议'],
  ming: ['明', '名'],
  tian: ['天', '田', '甜'],
  mingtian: ['明天'],
  jin: ['今', '进', '金'],
  jintian: ['今天'],
  xiao: ['小', '消', '笑'],
  xi: ['息', '西', '系'],
  xiaoxi: ['消息'],
  shu: ['输', '书', '数'],
  ru: ['入', '如'],
  shuru: ['输入'],
  ping: ['屏', '平'],
  mu: ['幕', '木'],
  pingmu: ['屏幕']
};

/* ───────────────────────── Word prediction ───────────────────────── */

const WORDS = {
  de: ['der', 'die', 'das', 'und', 'ich', 'nicht', 'mit', 'auf', 'für', 'ist', 'haben', 'werden', 'können', 'sehr', 'gut', 'danke', 'bitte', 'heute', 'morgen', 'Nachrichten', 'Einstellungen', 'Dateien', 'Termin', 'Besprechung', 'Grüße', 'Guten', 'Hallo', 'vielen', 'gerne', 'schnell', 'später', 'Projekt', 'Entwurf', 'fertig', 'öffnen', 'speichern', 'löschen', 'Mitteilungen', 'Tastatur', 'Deutsch', 'Kalender', 'Notiz', 'senden', 'schreiben', 'Bildschirm'],
  en: ['the', 'and', 'you', 'that', 'with', 'have', 'this', 'from', 'they', 'what', 'about', 'thanks', 'please', 'today', 'tomorrow', 'meeting', 'message', 'settings', 'files', 'project', 'draft', 'keyboard', 'English', 'quick', 'later', 'done', 'open', 'save', 'delete', 'notification', 'calendar', 'screen', 'window', 'send', 'write'],
  ko: ['안녕하세요', '감사합니다', '네', '아니요', '내일', '오늘', '회의', '파일', '설정', '키보드', '한국어', '안녕', '좋아요', '알겠습니다', '메시지', '프로젝트', '화면', '보내기'],
  ja: [],
  zh: []
};
const cap = (word, typed) => typed && typed[0] === typed[0].toUpperCase() && typed[0] !== typed[0].toLowerCase() ? word[0].toUpperCase() + word.slice(1) : word;

/** Latin / hangul word prediction for the current partial word. */
function predictWords(lang, partial) {
  const list = WORDS[lang] || [];
  if (!partial) return list.slice(0, 3);
  const p = partial.toLowerCase();
  const hits = list.filter(w => w.toLowerCase().startsWith(p) && w.toLowerCase() !== p);
  return hits.slice(0, 3).map(w => cap(w, partial));
}

/** IME candidates for a composing buffer. kana for ja, pinyin for zh. */
function imeCandidates(lang, composing) {
  if (!composing) return [];
  if (lang === 'ja') {
    const out = [composing];
    const kata = KATA(composing);
    Object.keys(JA_DICT).forEach(k => {
      if (k === composing) out.push(...JA_DICT[k]);
    });
    Object.keys(JA_DICT).forEach(k => {
      if (k !== composing && k.startsWith(composing)) out.push(...JA_DICT[k]);
    });
    if (kata !== composing) out.push(kata);
    return [...new Set(out)].slice(0, 12);
  }
  if (lang === 'zh') {
    const p = composing.toLowerCase();
    const out = [];
    if (ZH_DICT[p]) out.push(...ZH_DICT[p]);
    Object.keys(ZH_DICT).forEach(k => {
      if (k !== p && k.startsWith(p)) out.push(...ZH_DICT[k]);
    });
    return [...new Set(out)].slice(0, 12);
  }
  return [];
}
Object.assign(__ds_scope, { LANGS, LETTER_ROWS, HANGUL_SHIFT, NUMBER_ROW, SYMBOL_LAYERS, isJungseong, hangulText, hangulPress, hangulBack, romajiToKana, KANA_KEYS, kanaModify, predictWords, imeCandidates });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/keyboard/keyboard-engine.js", error: String((e && e.message) || e) }); }

// components/keyboard/OnScreenKeyboard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  useEffect,
  useLayoutEffect,
  useRef,
  useState
} = React;
/**
 * OnScreenKeyboard — the AIVA OS on-screen keyboard (OSK).
 * Responsive across every aspect ratio, five languages with real IME behaviour,
 * a suggestion / candidate bar, an input preview and a close button.
 */
function OnScreenKeyboard({
  language,
  defaultLanguage = 'de',
  languages = ['de', 'en', 'ko', 'ja', 'zh'],
  value,
  defaultValue = '',
  onChange,
  onSubmit,
  onClose,
  onLanguageChange,
  showPreview = true,
  clearOnSubmit = false,
  placeholder = '',
  numberRow = 'auto',
  dock = false,
  style = {}
}) {
  const rootRef = useRef(null);
  const [w, setW] = useState(760);
  const [vh, setVh] = useState(typeof window === 'undefined' ? 800 : window.innerHeight);
  const [langState, setLangState] = useState(defaultLanguage);
  const lang = language !== undefined ? language : langState;
  const kind = (__ds_scope.LANGS[lang] || __ds_scope.LANGS.en).kind;
  const [inner, setInner] = useState(defaultValue);
  const text = value !== undefined ? value : inner;
  const setText = t => {
    if (value === undefined) setInner(t);
    onChange && onChange(t);
  };
  const [layer, setLayer] = useState('letters');
  const [shift, setShift] = useState(0);
  const [hbuf, setHbuf] = useState(null);
  const [comp, setComp] = useState({
    kana: '',
    rest: '',
    raw: ''
  });
  const [jaPadPref, setJaPadPref] = useState(null);
  const cycle = useRef({
    i: -1,
    pos: 0,
    t: 0
  });
  const shiftTap = useRef(0);
  useLayoutEffect(() => {
    const el = rootRef.current;
    if (!el) return;
    const measure = () => {
      setW(el.clientWidth || 760);
      setVh(window.innerHeight);
    };
    measure();
    const ro = new ResizeObserver(measure);
    ro.observe(el);
    window.addEventListener('resize', measure);
    return () => {
      ro.disconnect();
      window.removeEventListener('resize', measure);
    };
  }, []);
  const mode = w < 560 ? 'compact' : w < 980 ? 'tablet' : 'wide';
  const showNums = numberRow === true || numberRow === 'auto' && mode !== 'compact';
  const jaPad = jaPadPref || (mode === 'wide' ? 'romaji' : 'kana');
  const keyH = Math.round(Math.max(30, Math.min(w * 0.058, vh * 0.082, 58)));
  const gap = Math.max(3, Math.round(keyH * 0.13));
  const pad = Math.max(4, Math.round(keyH * 0.16));

  /* ── composition ───────────────────────────────────────── */
  const composing = kind === 'hangul' ? __ds_scope.hangulText(hbuf) : lang === 'ja' ? comp.kana + comp.rest : lang === 'zh' ? comp.raw : '';
  const clearComp = () => {
    setHbuf(null);
    setComp({
      kana: '',
      rest: '',
      raw: ''
    });
    cycle.current = {
      i: -1,
      pos: 0,
      t: 0
    };
  };
  const confirmed = (extra = '') => {
    const t = text + composing + extra;
    clearComp();
    setText(t);
    return t;
  };
  const partial = (() => {
    const m = (text + composing).match(/[\p{L}\p{M}'’-]+$/u);
    return m ? m[0] : '';
  })();
  const suggestions = kind === 'latin' || kind === 'hangul' ? __ds_scope.predictWords(lang, partial) : __ds_scope.imeCandidates(lang, composing);

  /* ── input actions ─────────────────────────────────────── */
  const typeChar = raw => {
    const ch = shift && kind === 'latin' ? raw.toUpperCase() : raw;
    if (shift === 1) setShift(0);
    if (kind === 'hangul' && layer === 'letters') {
      const jamo = shift ? __ds_scope.HANGUL_SHIFT[raw] || raw : raw;
      const r = __ds_scope.hangulPress(hbuf, jamo);
      if (r.commit) setText(text + r.commit);
      setHbuf(r.buf);
      if (shift === 1) setShift(0);
      return;
    }
    if (lang === 'ja' && layer === 'letters' && jaPad === 'romaji' && /^[a-z-]$/i.test(raw)) {
      const nextRaw = comp.rest + raw.toLowerCase();
      const {
        kana,
        rest
      } = __ds_scope.romajiToKana(nextRaw);
      setComp({
        kana: comp.kana + kana,
        rest,
        raw: ''
      });
      cycle.current = {
        i: -1,
        pos: 0,
        t: 0
      };
      return;
    }
    if (lang === 'zh' && layer === 'letters' && /^[a-z]$/i.test(raw)) {
      setComp({
        kana: '',
        rest: '',
        raw: comp.raw + raw.toLowerCase()
      });
      return;
    }
    setText(text + composing + ch);
    clearComp();
  };
  const pressKana = i => {
    const chars = [...__ds_scope.KANA_KEYS[i].chars];
    const now = Date.now();
    const c = cycle.current;
    if (c.i === i && now - c.t < 1400 && comp.kana.endsWith(chars[c.pos])) {
      const pos = (c.pos + 1) % chars.length;
      cycle.current = {
        i,
        pos,
        t: now
      };
      setComp({
        ...comp,
        kana: comp.kana.slice(0, -1) + chars[pos],
        rest: ''
      });
    } else {
      cycle.current = {
        i,
        pos: 0,
        t: now
      };
      setComp({
        ...comp,
        kana: comp.kana + chars[0],
        rest: ''
      });
    }
  };
  const pressMod = () => {
    if (comp.kana) {
      setComp({
        ...comp,
        kana: comp.kana.slice(0, -1) + __ds_scope.kanaModify(comp.kana.slice(-1))
      });
      cycle.current = {
        i: -1,
        pos: 0,
        t: 0
      };
    } else if (text) {
      setText(text.slice(0, -1) + __ds_scope.kanaModify(text.slice(-1)));
    }
  };
  const backspace = () => {
    if (kind === 'hangul' && hbuf) {
      setHbuf(__ds_scope.hangulBack(hbuf));
      return;
    }
    if (lang === 'ja' && (comp.rest || comp.kana)) {
      if (comp.rest) {
        const r = comp.rest.slice(0, -1);
        setComp({
          ...comp,
          rest: r
        });
      } else setComp({
        ...comp,
        kana: comp.kana.slice(0, -1)
      });
      cycle.current = {
        i: -1,
        pos: 0,
        t: 0
      };
      return;
    }
    if (lang === 'zh' && comp.raw) {
      setComp({
        ...comp,
        raw: comp.raw.slice(0, -1)
      });
      return;
    }
    setText(text.slice(0, -1));
  };
  const pressSpace = () => {
    if (composing && (lang === 'ja' || lang === 'zh')) {
      const first = suggestions[0];
      setText(text + (first || composing));
      clearComp();
      return;
    }
    confirmed(' ');
  };
  const pressReturn = () => {
    if (composing) {
      confirmed();
      return;
    }
    onSubmit && onSubmit(text);
    if (clearOnSubmit) setText('');
  };
  const pickSuggestion = s => {
    if (kind === 'latin' || kind === 'hangul') {
      const base = text + composing;
      setText(base.slice(0, base.length - partial.length) + s + (kind === 'latin' ? ' ' : ''));
      clearComp();
    } else {
      setText(text + s);
      clearComp();
    }
  };
  const cycleLang = () => {
    const t = confirmed();
    const i = languages.indexOf(lang);
    const next = languages[(i + 1) % languages.length];
    setLayer('letters');
    setShift(0);
    if (language === undefined) setLangState(next);
    onLanguageChange && onLanguageChange(next, t);
  };
  const pressShift = () => {
    const now = Date.now();
    if (now - shiftTap.current < 320) {
      setShift(2);
      shiftTap.current = 0;
      return;
    }
    shiftTap.current = now;
    setShift(shift === 0 ? 1 : 0);
  };
  useEffect(() => {
    setLayer('letters');
    clearComp();
  }, [lang]);

  /* ── rows ──────────────────────────────────────────────── */
  const rows = layer === 'letters' ? __ds_scope.LETTER_ROWS[lang] || __ds_scope.LETTER_ROWS.en : __ds_scope.SYMBOL_LAYERS[layer];
  const isKana = lang === 'ja' && jaPad === 'kana' && layer === 'letters';
  const label = ch => {
    if (layer !== 'letters') return ch;
    if (kind === 'hangul') return shift ? __ds_scope.HANGUL_SHIFT[ch] || ch : ch;
    return shift ? ch.toUpperCase() : ch;
  };
  const fnFont = Math.max(10, Math.round(keyH * 0.28));
  const Ic = ({
    d,
    size = 18
  }) => /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": true
  }, d);
  const globeIcon = /*#__PURE__*/React.createElement(Ic, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "9"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M3 12h18M12 3c2.6 3 2.6 15 0 18M12 3c-2.6 3-2.6 15 0 18"
    })),
    size: Math.round(keyH * 0.42)
  });
  const backIcon = /*#__PURE__*/React.createElement(Ic, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M20 6H9.5L3 12l6.5 6H20a1 1 0 001-1V7a1 1 0 00-1-1z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 10l-4 4M12 10l4 4"
    })),
    size: Math.round(keyH * 0.44)
  });
  const returnIcon = /*#__PURE__*/React.createElement(Ic, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M20 5v6a3 3 0 01-3 3H5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M9 10l-4 4 4 4"
    })),
    size: Math.round(keyH * 0.44)
  });
  const shiftIcon = /*#__PURE__*/React.createElement(Ic, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M12 4l8 8h-4v6H8v-6H4l8-8z",
      fill: shift ? 'currentColor' : 'none'
    })),
    size: Math.round(keyH * 0.44)
  });
  const rowStyle = {
    display: 'flex',
    gap
  };
  const letterRows = rows.map((row, ri) => {
    const chars = [...row];
    const last = ri === rows.length - 1;
    return /*#__PURE__*/React.createElement("div", {
      key: ri,
      style: rowStyle
    }, last && layer === 'letters' && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      variant: shift ? 'accent' : 'fn',
      flex: 1.5,
      height: keyH,
      repeat: false,
      ariaLabel: "Shift",
      onPress: pressShift
    }, shiftIcon), last && layer !== 'letters' && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      variant: "fn",
      flex: 1.5,
      height: keyH,
      fontSize: fnFont,
      label: layer === 'num' ? '#+=' : '123',
      onPress: () => setLayer(layer === 'num' ? 'sym' : 'num')
    }), chars.map((ch, i) => /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      key: ch + i,
      label: label(ch),
      height: keyH,
      onPress: () => typeChar(ch)
    })), last && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      variant: "fn",
      flex: 1.5,
      height: keyH,
      repeat: true,
      ariaLabel: "L\xF6schen",
      onPress: backspace
    }, backIcon));
  });
  const kanaGrid = (() => {
    const g = i => /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      key: i,
      label: __ds_scope.KANA_KEYS[i].label,
      height: keyH,
      onPress: () => pressKana(i)
    });
    const fn = props => /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, _extends({
      variant: "fn",
      height: keyH,
      fontSize: fnFont
    }, props));
    return [/*#__PURE__*/React.createElement("div", {
      key: "k1",
      style: rowStyle
    }, fn({
      label: '記号',
      flex: 1.1,
      onPress: () => setLayer('num')
    }), g(0), g(1), g(2), fn({
      flex: 1.1,
      repeat: true,
      ariaLabel: 'Löschen',
      onPress: backspace,
      children: backIcon
    })), /*#__PURE__*/React.createElement("div", {
      key: "k2",
      style: rowStyle
    }, fn({
      label: 'ABC',
      flex: 1.1,
      onPress: () => setJaPadPref('romaji')
    }), g(3), g(4), g(5), fn({
      label: __ds_scope.LANGS.ja.space,
      flex: 1.1,
      onPress: pressSpace
    })), /*#__PURE__*/React.createElement("div", {
      key: "k3",
      style: rowStyle
    }, fn({
      flex: 1.1,
      ariaLabel: 'Sprache',
      onPress: cycleLang,
      children: globeIcon
    }), g(6), g(7), g(8), fn({
      flex: 1.1,
      ariaLabel: __ds_scope.LANGS.ja.ret,
      onPress: pressReturn,
      children: returnIcon
    })), /*#__PURE__*/React.createElement("div", {
      key: "k4",
      style: rowStyle
    }, fn({
      label: '゛小゜',
      flex: 1.1,
      onPress: pressMod
    }), g(9), g(10), /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      label: "\u30FC",
      height: keyH,
      onPress: () => typeChar('ー')
    }), /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
      variant: composing ? 'accent' : 'fn',
      flex: 1.1,
      height: keyH,
      fontSize: fnFont,
      label: "\u78BA\u5B9A",
      onPress: () => composing ? confirmed() : pressReturn()
    }))];
  })();
  const bottomRow = /*#__PURE__*/React.createElement("div", {
    style: rowStyle
  }, /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    variant: "fn",
    flex: mode === 'compact' ? 1.5 : 1.3,
    height: keyH,
    fontSize: fnFont,
    label: layer === 'letters' ? '123' : 'ABC',
    onPress: () => setLayer(layer === 'letters' ? 'num' : 'letters')
  }), languages.length > 1 && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    variant: "fn",
    flex: 1.15,
    height: keyH,
    ariaLabel: "Sprache wechseln",
    onPress: cycleLang
  }, globeIcon), lang === 'ja' && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    variant: "fn",
    flex: 1.15,
    height: keyH,
    fontSize: fnFont,
    label: "\u304B\u306A",
    onPress: () => {
      confirmed();
      setJaPadPref('kana');
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    flex: mode === 'compact' ? 4.6 : 5.6,
    height: keyH,
    fontSize: fnFont,
    label: mode === 'compact' ? '' : (__ds_scope.LANGS[lang] || __ds_scope.LANGS.en).space,
    ariaLabel: "Leerzeichen",
    onPress: pressSpace
  }), mode !== 'compact' && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    label: ".",
    height: keyH,
    onPress: () => typeChar('.')
  }), /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    variant: "accent",
    flex: mode === 'compact' ? 1.9 : 2.2,
    height: keyH,
    fontSize: fnFont,
    label: mode === 'compact' ? null : composing ? '確定' : (__ds_scope.LANGS[lang] || __ds_scope.LANGS.en).ret,
    ariaLabel: "Return",
    onPress: pressReturn
  }, mode === 'compact' ? returnIcon : undefined));

  /* ── toolbar: language chip · suggestions · close ───────── */
  const chipFont = Math.max(10, Math.round(keyH * 0.26));
  const barH = Math.max(28, Math.round(keyH * 0.78));
  const toolbar = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap,
      height: barH,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 5,
      height: '100%',
      padding: '0 9px',
      flexShrink: 0,
      background: 'var(--glass-subtle-bg)',
      borderRadius: 'var(--radius-full)',
      fontSize: chipFont,
      fontWeight: 600,
      letterSpacing: '0.04em',
      color: 'var(--glass-text-secondary)'
    }
  }, (__ds_scope.LANGS[lang] || __ds_scope.LANGS.en).short, composing ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--glass-text-strong)',
      letterSpacing: 0
    }
  }, composing) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      gap: kind === 'latin' || kind === 'hangul' ? 0 : gap,
      overflowX: 'auto',
      overflowY: 'hidden',
      scrollbarWidth: 'none'
    }
  }, suggestions.length === 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: chipFont,
      color: 'var(--glass-text-secondary)',
      paddingLeft: 4
    }
  }, kind === 'latin' || kind === 'hangul' ? '' : lang === 'zh' ? '拼音 eingeben' : 'ローマ字 / かな'), suggestions.map((s, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: s + i
  }, i > 0 && (kind === 'latin' || kind === 'hangul') && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: '48%',
      background: 'var(--glass-divider)',
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onPointerDown: e => {
      e.preventDefault();
      pickSuggestion(s);
    },
    style: {
      flex: kind === 'latin' || kind === 'hangul' ? '1 1 0' : '0 0 auto',
      minWidth: 0,
      height: '100%',
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      padding: kind === 'latin' || kind === 'hangul' ? '0 6px' : '0 11px',
      fontFamily: 'var(--font-sans)',
      fontSize: Math.max(13, Math.round(keyH * 0.36)),
      fontWeight: 500,
      color: i === 0 && (kind === 'kana' || kind === 'pinyin') ? 'var(--glass-text-strong)' : 'var(--glass-text-primary)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      borderRadius: 8
    }
  }, s)))), onClose && /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    variant: "ghost",
    flex: 0,
    height: barH,
    ariaLabel: "Tastatur schlie\xDFen",
    style: {
      width: barH,
      flex: `0 0 ${barH}px`
    },
    onPress: () => {
      confirmed();
      onClose();
    }
  }, /*#__PURE__*/React.createElement(Ic, {
    d: /*#__PURE__*/React.createElement("path", {
      d: "M6 6l12 12M18 6L6 18"
    }),
    size: Math.round(barH * 0.5)
  })));
  const preview = showPreview && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 2,
      flexWrap: 'wrap',
      minHeight: Math.round(keyH * 0.95),
      padding: `${Math.round(keyH * 0.2)}px ${Math.round(keyH * 0.3)}px`,
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      borderRadius: 'var(--radius-lg, 12px)',
      flexShrink: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: Math.max(14, Math.round(keyH * 0.4)),
      color: 'var(--glass-text-strong)',
      lineHeight: 1.45,
      wordBreak: 'break-word'
    }
  }, text ? /*#__PURE__*/React.createElement("span", null, text) : composing ? null : /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--glass-text-secondary)'
    }
  }, placeholder), composing ? /*#__PURE__*/React.createElement("span", {
    style: {
      background: 'var(--glass-toggle-on-shadow)',
      borderBottom: '2px solid var(--glass-toggle-on-bg)',
      borderRadius: '3px 3px 0 0',
      padding: '0 1px'
    }
  }, composing) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-block',
      width: 2,
      alignSelf: 'stretch',
      minHeight: '1.1em',
      background: 'var(--glass-toggle-on-bg)',
      animation: 'aiva-osk-caret 1.05s steps(1,end) infinite'
    }
  }));
  return /*#__PURE__*/React.createElement("div", {
    ref: rootRef,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap,
      width: '100%',
      boxSizing: 'border-box',
      padding: dock ? `${pad}px ${pad}px ${Math.round(pad * 1.6)}px` : pad,
      background: 'var(--glass-panel-bg)',
      border: dock ? 'none' : '1px solid var(--glass-panel-border)',
      borderTop: dock ? '1px solid var(--glass-panel-border)' : undefined,
      borderRadius: dock ? 'var(--radius-3xl, 24px) var(--radius-3xl, 24px) 0 0' : 'var(--radius-3xl, 24px)',
      boxShadow: 'var(--glass-panel-shadow)',
      backdropFilter: 'blur(var(--glass-blur-xl))',
      WebkitBackdropFilter: 'blur(var(--glass-blur-xl))',
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes aiva-osk-caret{0%,49%{opacity:1}50%,100%{opacity:0}}'), preview, toolbar, showNums && layer === 'letters' && !isKana && /*#__PURE__*/React.createElement("div", {
    style: rowStyle
  }, [...__ds_scope.NUMBER_ROW].map(n => /*#__PURE__*/React.createElement(__ds_scope.KeyboardKey, {
    key: n,
    label: n,
    height: keyH,
    onPress: () => typeChar(n)
  }))), isKana ? kanaGrid : letterRows, !isKana && bottomRow);
}
Object.assign(__ds_scope, { OnScreenKeyboard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/keyboard/OnScreenKeyboard.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Accordion.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Accordion — collapsible disclosure group for AIVA OS.
 * A glass container of expandable sections. Single or multiple open at once.
 */
function Accordion({
  items = [],
  defaultOpen = [],
  multiple = false,
  style = {}
}) {
  const [open, setOpen] = useState(new Set(defaultOpen));
  const toggle = i => {
    setOpen(prev => {
      const next = new Set(multiple ? prev : []);
      if (prev.has(i)) next.delete(i);else next.add(i);
      return next;
    });
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 'var(--radius-2xl)',
      overflow: 'hidden',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      ...style
    }
  }, items.map((it, i) => {
    const isOpen = open.has(i);
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        borderTop: i === 0 ? 'none' : '1px solid var(--glass-divider)'
      }
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => toggle(i),
      "aria-expanded": isOpen,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 11,
        width: '100%',
        padding: '14px 15px',
        border: 'none',
        cursor: 'pointer',
        background: 'transparent',
        textAlign: 'left',
        WebkitTapHighlightColor: 'transparent'
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        flexShrink: 0,
        color: 'var(--glass-text-primary)'
      }
    }, it.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        fontFamily: 'var(--font-sans)',
        fontSize: 14,
        fontWeight: 600,
        color: 'var(--glass-text-strong)'
      }
    }, it.title), /*#__PURE__*/React.createElement("svg", {
      width: "16",
      height: "16",
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        flexShrink: 0,
        color: 'var(--glass-text-secondary)',
        transform: isOpen ? 'rotate(180deg)' : 'rotate(0deg)',
        transition: 'transform 0.25s ease'
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M6 9l6 6 6-6",
      stroke: "currentColor",
      strokeWidth: "2.2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }))), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateRows: isOpen ? '1fr' : '0fr',
        transition: 'grid-template-rows 0.28s cubic-bezier(0.4,0,0.2,1)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '0 15px 15px',
        fontFamily: 'var(--font-sans)',
        fontSize: 14,
        fontWeight: 400,
        color: 'var(--glass-text-primary)',
        lineHeight: 1.5
      }
    }, it.content))));
  }));
}
Object.assign(__ds_scope, { Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Accordion.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Avatar.jsx
try { (() => {
/**
 * Avatar — user/contact avatar for AIVA OS.
 * Shows an image, or initials on a glass backing. Optional status dot.
 */
function Avatar({
  src,
  alt = '',
  initials,
  size = 40,
  status,
  square = false,
  style = {}
}) {
  const statusColor = {
    online: 'var(--color-success)',
    busy: 'var(--color-destructive)',
    away: 'var(--color-warning)',
    offline: 'var(--glass-text-secondary)'
  }[status];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: size,
      height: size,
      flexShrink: 0,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: '100%',
      overflow: 'hidden',
      borderRadius: square ? Math.round(size * 0.28) : '50%',
      background: 'var(--glass-card-bg)',
      border: '1px solid var(--glass-card-border)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      boxShadow: 'inset 0 1px 1px rgba(255,255,255,0.2)'
    }
  }, src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontWeight: 600,
      fontSize: size * 0.4,
      color: 'var(--glass-text-strong)',
      letterSpacing: '0.01em'
    }
  }, initials)), statusColor && /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      right: 0,
      bottom: 0,
      width: Math.max(8, size * 0.26),
      height: Math.max(8, size * 0.26),
      borderRadius: '50%',
      background: statusColor,
      border: '2px solid var(--glass-panel-bg)',
      boxSizing: 'border-box'
    }
  }));
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Breadcrumbs.jsx
try { (() => {
/**
 * Breadcrumbs — path navigation trail for AIVA OS.
 * A row of links separated by chevrons; the last crumb is the current page.
 */
function Breadcrumbs({
  items = [],
  onNavigate,
  style = {}
}) {
  return /*#__PURE__*/React.createElement("nav", {
    style: {
      display: 'flex',
      alignItems: 'center',
      flexWrap: 'wrap',
      gap: 4,
      ...style
    }
  }, items.map((it, i) => {
    const last = i === items.length - 1;
    const label = typeof it === 'string' ? it : it.label;
    const val = typeof it === 'string' ? it : it.value;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: i
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => !last && onNavigate && onNavigate(val, i),
      disabled: last,
      style: {
        border: 'none',
        background: 'transparent',
        padding: '2px 4px',
        cursor: last ? 'default' : 'pointer',
        fontFamily: 'var(--font-sans)',
        fontSize: 13,
        fontWeight: last ? 600 : 500,
        color: last ? 'var(--glass-text-strong)' : 'var(--glass-text-secondary)',
        whiteSpace: 'nowrap',
        WebkitTapHighlightColor: 'transparent'
      }
    }, label), !last && /*#__PURE__*/React.createElement("svg", {
      width: "14",
      height: "14",
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        color: 'var(--glass-text-secondary)',
        opacity: 0.5,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M9 6l6 6-6 6",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    })));
  }));
}
Object.assign(__ds_scope, { Breadcrumbs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Breadcrumbs.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Chip.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Chip — compact glass token for AIVA OS: filters, tags, recipients.
 * Larger and more tactile than a Badge; optionally selectable and removable.
 */
function Chip({
  children,
  icon,
  selected = false,
  onClick,
  onRemove,
  disabled = false,
  style = {}
}) {
  const [hover, setHover] = useState(false);
  const interactive = !!onClick;
  return /*#__PURE__*/React.createElement("span", {
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      height: 30,
      padding: onRemove ? '0 6px 0 12px' : '0 12px',
      borderRadius: 'var(--radius-full)',
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 500,
      cursor: disabled ? 'not-allowed' : interactive ? 'pointer' : 'default',
      opacity: disabled ? 0.45 : 1,
      color: selected ? '#fff' : 'var(--glass-text-strong)',
      background: selected ? 'var(--glass-toggle-on-bg)' : interactive && hover ? 'var(--glass-hover-bg)' : 'var(--glass-card-bg)',
      border: `1px solid ${selected ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)'}`,
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: selected ? '0 2px 8px var(--glass-toggle-on-shadow)' : 'none',
      transition: 'background 0.15s ease, color 0.15s ease, border-color 0.15s ease',
      whiteSpace: 'nowrap',
      WebkitTapHighlightColor: 'transparent',
      userSelect: 'none',
      ...style
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0
    }
  }, icon), children, onRemove && /*#__PURE__*/React.createElement("button", {
    onClick: e => {
      e.stopPropagation();
      onRemove();
    },
    "aria-label": "Entfernen",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
      width: 18,
      height: 18,
      borderRadius: '50%',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      background: selected ? 'rgba(255,255,255,0.25)' : 'var(--glass-divider)',
      color: selected ? '#fff' : 'var(--glass-text-primary)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "9",
    height: "9",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18",
    stroke: "currentColor",
    strokeWidth: "3",
    strokeLinecap: "round"
  }))));
}
Object.assign(__ds_scope, { Chip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Chip.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ListItem.jsx
try { (() => {
const {
  useState
} = React;
/**
 * ListItem — a settings/list row for AIVA OS (maps to ion-item).
 * Leading icon/avatar, title + subtitle, trailing control or chevron.
 * Becomes interactive (hover + press) when onClick is provided.
 */
function ListItem({
  title,
  subtitle,
  leading,
  trailing,
  showChevron = false,
  onClick,
  destructive = false,
  style = {}
}) {
  const [hover, setHover] = useState(false);
  const interactive = !!onClick;
  return /*#__PURE__*/React.createElement("div", {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '11px 14px',
      minHeight: 50,
      cursor: interactive ? 'pointer' : 'default',
      background: interactive && hover ? 'var(--glass-hover-bg)' : 'transparent',
      transition: 'background 0.12s ease',
      WebkitTapHighlightColor: 'transparent',
      ...style
    }
  }, leading && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
      color: destructive ? 'var(--color-destructive)' : 'var(--glass-text-primary)'
    }
  }, leading), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 500,
      color: destructive ? 'var(--color-destructive)' : 'var(--glass-text-strong)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 12,
      fontWeight: 400,
      color: 'var(--glass-text-secondary)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, subtitle)), trailing && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      flexShrink: 0,
      color: 'var(--glass-text-secondary)'
    }
  }, trailing), showChevron && /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    style: {
      color: 'var(--glass-text-secondary)',
      flexShrink: 0,
      opacity: 0.6
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M9 6l6 6-6 6",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}

/**
 * List — a grouped glass container that hairlines its ListItem children.
 */
function List({
  children,
  inset = true,
  style = {}
}) {
  const items = React.Children.toArray(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: inset ? 'var(--radius-2xl)' : 0,
      overflow: 'hidden',
      background: 'var(--glass-card-bg)',
      border: inset ? '1px solid var(--glass-card-border)' : 'none',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      ...style
    }
  }, items.map((child, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderTop: i === 0 ? 'none' : '1px solid var(--glass-divider)'
    }
  }, child)));
}
Object.assign(__ds_scope, { ListItem, List });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ListItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Pagination.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Pagination — page navigation control for AIVA OS.
 * Glass prev/next buttons flanking numbered page pills with ellipsis
 * truncation for large ranges. Controlled or uncontrolled.
 */
function Pagination({
  count,
  page,
  defaultPage = 1,
  onChange,
  siblingCount = 1,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultPage);
  const cur = page !== undefined ? page : internal;
  const go = p => {
    if (p < 1 || p > count || p === cur) return;
    if (page === undefined) setInternal(p);
    onChange && onChange(p);
  };

  // build page list with ellipses
  const pages = [];
  const range = (a, b) => {
    for (let i = a; i <= b; i++) pages.push(i);
  };
  const left = Math.max(2, cur - siblingCount);
  const right = Math.min(count - 1, cur + siblingCount);
  pages.push(1);
  if (left > 2) pages.push('…l');
  range(left, right);
  if (right < count - 1) pages.push('…r');
  if (count > 1) pages.push(count);
  const Arrow = ({
    dir,
    onClick,
    disabled
  }) => /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    disabled: disabled,
    "aria-label": dir === 'prev' ? 'Zurück' : 'Weiter',
    style: {
      width: 34,
      height: 34,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: '1px solid var(--glass-card-border)',
      borderRadius: 'var(--radius-lg)',
      background: 'var(--glass-card-bg)',
      color: 'var(--glass-text-strong)',
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.4 : 1,
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      WebkitTapHighlightColor: 'transparent'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    style: {
      transform: dir === 'prev' ? 'rotate(180deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M9 6l6 6-6 6",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      ...style
    }
  }, /*#__PURE__*/React.createElement(Arrow, {
    dir: "prev",
    onClick: () => go(cur - 1),
    disabled: cur <= 1
  }), pages.map((p, i) => typeof p === 'string' ? /*#__PURE__*/React.createElement("span", {
    key: p + i,
    style: {
      width: 28,
      textAlign: 'center',
      color: 'var(--glass-text-secondary)',
      fontFamily: 'var(--font-sans)',
      fontSize: 14
    }
  }, "\u2026") : /*#__PURE__*/React.createElement("button", {
    key: p,
    onClick: () => go(p),
    style: {
      minWidth: 34,
      height: 34,
      padding: '0 6px',
      border: `1px solid ${p === cur ? 'var(--glass-toggle-on-border)' : 'var(--glass-card-border)'}`,
      borderRadius: 'var(--radius-lg)',
      background: p === cur ? 'var(--glass-toggle-on-bg)' : 'var(--glass-card-bg)',
      color: p === cur ? '#fff' : 'var(--glass-text-strong)',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: p === cur ? 600 : 500,
      fontVariantNumeric: 'tabular-nums',
      cursor: 'pointer',
      boxShadow: p === cur ? '0 2px 8px var(--glass-toggle-on-shadow)' : 'none',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      WebkitTapHighlightColor: 'transparent'
    }
  }, p)), /*#__PURE__*/React.createElement(Arrow, {
    dir: "next",
    onClick: () => go(cur + 1),
    disabled: cur >= count
  }));
}
Object.assign(__ds_scope, { Pagination });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Pagination.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Sidebar.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Sidebar — glass navigation rail / drawer for AIVA OS (maps to SplitLayout's
 * side pane). A frosted vertical panel of nav items with optional header and
 * footer slots; the active item gets an accent highlight.
 */
function Sidebar({
  items = [],
  value,
  defaultValue,
  onChange,
  header,
  footer,
  width = 248,
  variant = 'panel',
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue ?? items[0]?.value);
  const active = value !== undefined ? value : internal;
  const select = v => {
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  const Item = ({
    it
  }) => {
    const [hover, setHover] = useState(false);
    if (it.header) return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '14px 12px 6px',
        fontFamily: 'var(--font-sans)',
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: '0.06em',
        textTransform: 'uppercase',
        color: 'var(--glass-text-secondary)'
      }
    }, it.label);
    const isActive = it.value === active;
    return /*#__PURE__*/React.createElement("button", {
      onClick: () => {
        select(it.value);
        it.onSelect && it.onSelect();
      },
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 11,
        width: '100%',
        padding: '9px 11px',
        border: 'none',
        cursor: 'pointer',
        textAlign: 'left',
        borderRadius: 'var(--radius-lg)',
        background: isActive ? 'var(--glass-toggle-on-bg)' : hover ? 'var(--glass-hover-bg)' : 'transparent',
        color: isActive ? '#fff' : 'var(--glass-text-strong)',
        fontFamily: 'var(--font-sans)',
        fontSize: 14,
        fontWeight: isActive ? 600 : 500,
        boxShadow: isActive ? '0 2px 8px var(--glass-toggle-on-shadow)' : 'none',
        transition: 'background 0.15s ease, color 0.15s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        flexShrink: 0,
        opacity: isActive ? 1 : 0.85
      }
    }, it.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, it.label), it.badge != null && /*#__PURE__*/React.createElement("span", {
      style: {
        minWidth: 18,
        height: 18,
        padding: '0 5px',
        borderRadius: 999,
        background: isActive ? 'rgba(255,255,255,0.25)' : 'var(--glass-notif-dot)',
        color: '#fff',
        fontSize: 11,
        fontWeight: 700,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexShrink: 0
      }
    }, it.badge));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width,
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: variant === 'plain' ? 'transparent' : 'var(--glass-panel-bg)',
      borderRight: variant === 'plain' ? 'none' : '1px solid var(--glass-divider)',
      backdropFilter: variant === 'plain' ? 'none' : 'blur(40px)',
      WebkitBackdropFilter: variant === 'plain' ? 'none' : 'blur(40px)',
      ...style
    }
  }, header && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 14px 6px',
      flexShrink: 0
    }
  }, header), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '6px 10px',
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement(Item, {
    key: i,
    it: it
  }))), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 12,
      borderTop: '1px solid var(--glass-divider)',
      flexShrink: 0
    }
  }, footer));
}

/**
 * SplitView — two-pane layout: a fixed Sidebar slot beside a flexible content
 * area. Provide `sidebar` and `children`.
 */
function SplitView({
  sidebar,
  children,
  style = {}
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: '100%',
      width: '100%',
      overflow: 'hidden',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flexShrink: 0,
      height: '100%'
    }
  }, sidebar), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      height: '100%',
      overflow: 'auto'
    }
  }, children));
}
Object.assign(__ds_scope, { Sidebar, SplitView });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Sidebar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/SubHeader.jsx
try { (() => {
/**
 * SubHeader — section header row for grouped content (maps to ArkUI SubHeader).
 * A title with optional secondary/caption text and a trailing action
 * (text button or icon). Use above Lists, grids, and settings groups.
 */
function SubHeader({
  title,
  secondary,
  action,
  onAction,
  icon,
  style = {}
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      padding: '6px 4px 8px',
      width: '100%',
      ...style
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0,
      color: 'var(--glass-text-secondary)'
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 600,
      letterSpacing: '0.04em',
      textTransform: 'uppercase',
      color: 'var(--glass-text-secondary)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, title), secondary && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 12,
      fontWeight: 400,
      color: 'var(--glass-text-secondary)',
      opacity: 0.8
    }
  }, secondary)), action && /*#__PURE__*/React.createElement("button", {
    onClick: onAction,
    style: {
      flexShrink: 0,
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--color-info)',
      padding: '2px 4px',
      WebkitTapHighlightColor: 'transparent'
    }
  }, action));
}
Object.assign(__ds_scope, { SubHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/SubHeader.jsx", error: String((e && e.message) || e) }); }

// components/navigation/TabBar.jsx
try { (() => {
const {
  useState
} = React;
/**
 * TabBar — bottom tab navigation for AIVA OS (maps to ion-tabs).
 * A frosted glass pill of tabs; the active tab tints blue with its label.
 * Each tab takes an icon and label, with an optional numeric badge.
 */
function TabBar({
  tabs = [],
  value,
  defaultValue,
  onChange,
  floating = true,
  style = {}
}) {
  const [internal, setInternal] = useState(defaultValue ?? tabs[0]?.value);
  const active = value !== undefined ? value : internal;
  const select = v => {
    if (value === undefined) setInternal(v);
    onChange && onChange(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'stretch',
      gap: 2,
      padding: 6,
      width: floating ? 'auto' : '100%',
      justifyContent: floating ? 'center' : 'space-around',
      background: 'var(--glass-panel-bg)',
      border: '1px solid var(--glass-panel-border)',
      borderRadius: floating ? 'var(--radius-full)' : 'var(--radius-3xl)',
      boxShadow: 'var(--glass-panel-shadow)',
      backdropFilter: 'blur(40px)',
      WebkitBackdropFilter: 'blur(40px)',
      ...style
    }
  }, tabs.map(t => {
    const isActive = t.value === active;
    return /*#__PURE__*/React.createElement("button", {
      key: t.value,
      onClick: () => select(t.value),
      "aria-label": t.label,
      style: {
        position: 'relative',
        flex: floating ? '0 0 auto' : 1,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 3,
        minWidth: 60,
        padding: '7px 14px',
        border: 'none',
        cursor: 'pointer',
        borderRadius: 'var(--radius-full)',
        background: isActive ? 'var(--glass-card-bg)' : 'transparent',
        color: isActive ? 'var(--color-info)' : 'var(--glass-text-secondary)',
        transition: 'background 0.2s ease, color 0.2s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
      }
    }, t.icon, t.badge != null && /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        top: -5,
        right: -8,
        minWidth: 15,
        height: 15,
        padding: '0 4px',
        borderRadius: 999,
        background: 'var(--glass-notif-dot)',
        color: '#fff',
        fontSize: 9,
        fontWeight: 700,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        border: '1.5px solid var(--glass-panel-bg)'
      }
    }, t.badge)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-sans)',
        fontSize: 10,
        fontWeight: 600,
        letterSpacing: '0.01em'
      }
    }, t.label));
  }));
}
Object.assign(__ds_scope, { TabBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/TabBar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Toolbar.jsx
try { (() => {
/**
 * Toolbar — top navigation bar for AIVA OS (maps to ion-toolbar).
 * A frosted glass bar with leading/trailing slots and a centered or
 * leading title. Use for window title bars and panel headers.
 */
function Toolbar({
  title,
  subtitle,
  leading,
  trailing,
  centerTitle = false,
  variant = 'panel',
  style = {}
}) {
  const bg = variant === 'transparent' ? 'transparent' : 'var(--glass-panel-bg)';
  const border = variant === 'transparent' ? 'none' : '1px solid var(--glass-divider)';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      height: 52,
      padding: '0 14px',
      width: '100%',
      background: bg,
      borderBottom: border,
      backdropFilter: variant === 'transparent' ? 'none' : 'blur(40px)',
      WebkitBackdropFilter: variant === 'transparent' ? 'none' : 'blur(40px)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      flexShrink: 0,
      minWidth: centerTitle ? 60 : undefined
    }
  }, leading), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: centerTitle ? 'center' : 'flex-start',
      textAlign: centerTitle ? 'center' : 'left'
    }
  }, title && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 16,
      fontWeight: 600,
      color: 'var(--glass-text-strong)',
      letterSpacing: '-0.01em',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      maxWidth: '100%'
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 11,
      fontWeight: 500,
      color: 'var(--glass-text-secondary)'
    }
  }, subtitle)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      flexShrink: 0,
      justifyContent: 'flex-end',
      minWidth: centerTitle ? 60 : undefined
    }
  }, trailing));
}
Object.assign(__ds_scope, { Toolbar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Toolbar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/TreeView.jsx
try { (() => {
const {
  useState
} = React;
/**
 * TreeView — collapsible hierarchical tree for AIVA OS (maps to ArkUI
 * TreeView). Files/folders with expand chevrons, indentation, icons, and a
 * selected-row accent. Pass a nested `nodes` array.
 */
function TreeView({
  nodes = [],
  selectedId,
  defaultExpanded = [],
  onSelect,
  style = {}
}) {
  const [expanded, setExpanded] = useState(new Set(defaultExpanded));
  const [internalSel, setInternalSel] = useState(null);
  const sel = selectedId !== undefined ? selectedId : internalSel;
  const toggle = id => setExpanded(p => {
    const n = new Set(p);
    n.has(id) ? n.delete(id) : n.add(id);
    return n;
  });
  const choose = node => {
    if (selectedId === undefined) setInternalSel(node.id);
    onSelect && onSelect(node);
  };
  const Node = ({
    node,
    depth
  }) => {
    const [hover, setHover] = useState(false);
    const hasChildren = node.children && node.children.length > 0;
    const isOpen = expanded.has(node.id);
    const isSel = node.id === sel;
    return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      onClick: () => {
        if (hasChildren) toggle(node.id);
        choose(node);
      },
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        padding: '6px 8px',
        paddingLeft: 8 + depth * 18,
        cursor: 'pointer',
        borderRadius: 'var(--radius-md)',
        background: isSel ? 'var(--glass-toggle-on-bg)' : hover ? 'var(--glass-hover-bg)' : 'transparent',
        color: isSel ? '#fff' : 'var(--glass-text-strong)',
        transition: 'background 0.12s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 16,
        display: 'flex',
        flexShrink: 0,
        justifyContent: 'center'
      }
    }, hasChildren && /*#__PURE__*/React.createElement("svg", {
      width: "12",
      height: "12",
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        transform: isOpen ? 'rotate(90deg)' : 'rotate(0deg)',
        transition: 'transform 0.2s ease',
        opacity: 0.8
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M9 6l6 6-6 6",
      stroke: "currentColor",
      strokeWidth: "2.4",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }))), node.icon && /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        flexShrink: 0,
        opacity: isSel ? 1 : 0.8
      }
    }, node.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        fontFamily: 'var(--font-sans)',
        fontSize: 14,
        fontWeight: isSel ? 600 : 500,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, node.label)), hasChildren && /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateRows: isOpen ? '1fr' : '0fr',
        transition: 'grid-template-rows 0.22s ease'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        overflow: 'hidden'
      }
    }, node.children.map(c => /*#__PURE__*/React.createElement(Node, {
      key: c.id,
      node: c,
      depth: depth + 1
    })))));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 1,
      ...style
    }
  }, nodes.map(n => /*#__PURE__*/React.createElement(Node, {
    key: n.id,
    node: n,
    depth: 0
  })));
}
Object.assign(__ds_scope, { TreeView });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/TreeView.jsx", error: String((e && e.message) || e) }); }

// components/overlays/ActionSheet.jsx
try { (() => {
const {
  useState
} = React;
/**
 * ActionSheet — bottom-anchored glass option list for AIVA OS (maps to ion-action-sheet).
 * iOS-style: a grouped glass card of actions plus a separate "Abbrechen" card.
 * Destructive actions render in red. Render conditionally on `open`.
 */
function ActionSheet({
  open = false,
  onClose,
  title,
  message,
  actions = [],
  cancelText = 'Abbrechen',
  style = {}
}) {
  if (!open) return null;
  const Row = ({
    action,
    first,
    last
  }) => {
    const [hover, setHover] = useState(false);
    return /*#__PURE__*/React.createElement("button", {
      onClick: () => {
        action.onPress && action.onPress();
        onClose && onClose();
      },
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      style: {
        width: '100%',
        border: 'none',
        cursor: 'pointer',
        padding: '16px 18px',
        fontFamily: 'var(--font-sans)',
        fontSize: 17,
        fontWeight: action.destructive ? 600 : 500,
        color: action.destructive ? 'var(--color-destructive)' : 'var(--color-info)',
        background: hover ? 'var(--glass-hover-bg)' : 'transparent',
        borderTop: first ? 'none' : '1px solid var(--glass-divider)',
        transition: 'background 0.12s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, action.label);
  };
  const sheetCard = {
    borderRadius: 'var(--radius-2xl)',
    overflow: 'hidden',
    background: 'var(--glass-overlay-bg)',
    border: '1px solid var(--glass-overlay-border)',
    backdropFilter: 'blur(40px) saturate(180%)',
    WebkitBackdropFilter: 'blur(40px) saturate(180%)',
    boxShadow: 'var(--glass-overlay-shadow)'
  };
  return /*#__PURE__*/React.createElement("div", {
    onClick: () => onClose && onClose(),
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 'var(--z-modal, 30)',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      alignItems: 'center',
      padding: 10,
      gap: 8,
      background: 'var(--glass-scrim)',
      backdropFilter: 'blur(4px)',
      WebkitBackdropFilter: 'blur(4px)',
      animation: 'nx-scrim-in var(--motion-quick, 0.28s) ease both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    style: {
      width: '100%',
      maxWidth: 420,
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      animation: 'nx-sheet-in var(--motion-slow, 0.5s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: sheetCard
  }, (title || message) && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 18px',
      textAlign: 'center',
      borderBottom: '1px solid var(--glass-divider)'
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--glass-text-strong)'
    }
  }, title), message && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 12,
      color: 'var(--glass-text-secondary)',
      marginTop: 3,
      lineHeight: 1.4
    }
  }, message)), actions.map((a, i) => /*#__PURE__*/React.createElement(Row, {
    key: i,
    action: a,
    first: i === 0 && !title && !message,
    last: i === actions.length - 1
  }))), /*#__PURE__*/React.createElement("div", {
    style: sheetCard
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => onClose && onClose(),
    style: {
      width: '100%',
      border: 'none',
      cursor: 'pointer',
      padding: '16px 18px',
      fontFamily: 'var(--font-sans)',
      fontSize: 17,
      fontWeight: 700,
      color: 'var(--glass-text-strong)',
      background: 'transparent',
      WebkitTapHighlightColor: 'transparent'
    }
  }, cancelText))));
}
Object.assign(__ds_scope, { ActionSheet });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/ActionSheet.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Alert.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Alert — iOS-style confirmation dialog for AIVA OS (maps to ion-alert).
 * A compact centered glass card with title, message, and 1–2 stacked
 * or side-by-side buttons. Render conditionally on `open`.
 */
function Alert({
  open = false,
  onClose,
  title,
  message,
  buttons = [{
    label: 'OK'
  }],
  style = {}
}) {
  if (!open) return null;
  const horizontal = buttons.length === 2;
  const BtnRow = ({
    btn,
    idx
  }) => {
    const [hover, setHover] = useState(false);
    const isDanger = btn.role === 'destructive';
    const isPrimary = btn.role === 'primary' || buttons.length === 1;
    return /*#__PURE__*/React.createElement("button", {
      onClick: () => {
        btn.onPress && btn.onPress();
        onClose && onClose();
      },
      onMouseEnter: () => setHover(true),
      onMouseLeave: () => setHover(false),
      style: {
        flex: horizontal ? 1 : undefined,
        width: horizontal ? undefined : '100%',
        padding: '13px 16px',
        border: 'none',
        cursor: 'pointer',
        background: hover ? 'var(--glass-hover-bg)' : 'transparent',
        fontFamily: 'var(--font-sans)',
        fontSize: 16,
        fontWeight: isPrimary ? 700 : 500,
        color: isDanger ? 'var(--color-destructive)' : 'var(--color-info)',
        borderTop: '1px solid var(--glass-divider)',
        borderLeft: horizontal && idx === 1 ? '1px solid var(--glass-divider)' : 'none',
        transition: 'background 0.12s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, btn.label);
  };
  return /*#__PURE__*/React.createElement("div", {
    onClick: () => onClose && onClose(),
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 'var(--z-modal, 30)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 24,
      background: 'var(--glass-scrim)',
      backdropFilter: 'blur(4px)',
      WebkitBackdropFilter: 'blur(4px)',
      animation: 'nx-scrim-in var(--motion-quick, 0.28s) ease both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    role: "alertdialog",
    style: {
      width: '100%',
      maxWidth: 300,
      overflow: 'hidden',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-2xl)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      animation: 'nx-overlay-in var(--motion-slow, 0.5s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 18px 16px',
      textAlign: 'center'
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 17,
      fontWeight: 700,
      color: 'var(--glass-text-strong)'
    }
  }, title), message && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      color: 'var(--glass-text-primary)',
      marginTop: 5,
      lineHeight: 1.45
    }
  }, message)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: horizontal ? 'row' : 'column'
    }
  }, buttons.map((b, i) => /*#__PURE__*/React.createElement(BtnRow, {
    key: i,
    btn: b,
    idx: i
  })))));
}
Object.assign(__ds_scope, { Alert });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Alert.jsx", error: String((e && e.message) || e) }); }

// components/overlays/FAB.jsx
try { (() => {
const {
  useState
} = React;
/**
 * FAB — floating action button for AIVA OS (maps to ion-fab).
 * A round glass button that can expand a stack of secondary mini-actions.
 * Positions itself in a corner by default (position="static" to inline it).
 */
function FAB({
  icon,
  actions = [],
  onClick,
  position = 'bottom-end',
  size = 56,
  open,
  onOpenChange,
  style = {}
}) {
  const [internal, setInternal] = useState(false);
  const isOpen = open !== undefined ? open : internal;
  const setOpen = v => {
    if (open === undefined) setInternal(v);
    onOpenChange && onOpenChange(v);
  };
  const hasActions = actions.length > 0;
  const handleMain = () => {
    if (hasActions) setOpen(!isOpen);else onClick && onClick();
  };
  const corner = position === 'static' ? {} : position === 'bottom-start' ? {
    position: 'fixed',
    left: 24,
    bottom: 24
  } : position === 'top-end' ? {
    position: 'fixed',
    right: 24,
    top: 24
  } : position === 'top-start' ? {
    position: 'fixed',
    left: 24,
    top: 24
  } : {
    position: 'fixed',
    right: 24,
    bottom: 24
  };
  const glass = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexShrink: 0,
    border: '1px solid var(--glass-overlay-border)',
    cursor: 'pointer',
    background: 'var(--glass-overlay-bg)',
    color: 'var(--glass-text-strong)',
    boxShadow: 'var(--glass-overlay-shadow)',
    backdropFilter: 'blur(40px) saturate(180%)',
    WebkitBackdropFilter: 'blur(40px) saturate(180%)',
    WebkitTapHighlightColor: 'transparent'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...corner,
      zIndex: 'var(--z-raised, 10)',
      display: 'flex',
      flexDirection: 'column-reverse',
      alignItems: 'center',
      gap: 12,
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: handleMain,
    "aria-label": "Aktion",
    style: {
      ...glass,
      width: size,
      height: size,
      borderRadius: '50%',
      padding: 0,
      transform: isOpen ? 'rotate(45deg)' : 'rotate(0deg)',
      transition: 'transform 0.3s cubic-bezier(0.34,1.4,0.5,1), background 0.15s ease'
    }
  }, icon || /*#__PURE__*/React.createElement("svg", {
    width: size * 0.4,
    height: size * 0.4,
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 5v14M5 12h14",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round"
  }))), hasActions && actions.map((a, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => {
      a.onPress && a.onPress();
      setOpen(false);
    },
    title: a.label,
    "aria-label": a.label,
    style: {
      ...glass,
      width: size * 0.78,
      height: size * 0.78,
      borderRadius: '50%',
      padding: 0,
      opacity: isOpen ? 1 : 0,
      pointerEvents: isOpen ? 'auto' : 'none',
      transform: isOpen ? 'translateY(0) scale(1)' : 'translateY(12px) scale(0.6)',
      transition: `opacity 0.2s ease ${isOpen ? i * 0.04 : 0}s, transform 0.3s cubic-bezier(0.34,1.4,0.5,1) ${isOpen ? i * 0.04 : 0}s`
    }
  }, a.icon)));
}
Object.assign(__ds_scope, { FAB });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/FAB.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Menu.jsx
try { (() => {
const {
  useState,
  useRef,
  useEffect
} = React;
/**
 * Menu — glass dropdown/context menu for AIVA OS (maps to ArkUI Menu / formMenu).
 * A frosted panel of MenuItems anchored to a trigger; opens on click (or on
 * right-click when contextMenu is set). Supports icons, shortcuts, submenus,
 * dividers, destructive items. Dismisses on outside-click / Escape.
 */
function Menu({
  trigger,
  items = [],
  open,
  onOpenChange,
  placement = 'bottom-start',
  contextMenu = false,
  width = 220,
  style = {}
}) {
  const [internal, setInternal] = useState(false);
  const isOpen = open !== undefined ? open : internal;
  const [coords, setCoords] = useState(null); // {x,y} for context menu
  const wrapRef = useRef(null);
  const setOpen = v => {
    if (open === undefined) setInternal(v);
    onOpenChange && onOpenChange(v);
  };
  useEffect(() => {
    if (!isOpen) return;
    const onDoc = e => {
      if (wrapRef.current && !wrapRef.current.contains(e.target)) setOpen(false);
    };
    const onKey = e => {
      if (e.key === 'Escape') setOpen(false);
    };
    document.addEventListener('mousedown', onDoc);
    document.addEventListener('keydown', onKey);
    return () => {
      document.removeEventListener('mousedown', onDoc);
      document.removeEventListener('keydown', onKey);
    };
  }, [isOpen]);
  const [vert, horiz] = placement.split('-');
  const submenuLeft = horiz === 'end';
  const panelPos = contextMenu && coords ? {
    position: 'fixed',
    top: coords.y,
    left: coords.x
  } : {
    position: 'absolute',
    ...(vert === 'top' ? {
      bottom: 'calc(100% + 6px)'
    } : {
      top: 'calc(100% + 6px)'
    }),
    ...(horiz === 'end' ? {
      right: 0
    } : {
      left: 0
    })
  };
  const onTriggerClick = e => {
    if (contextMenu) return;
    setOpen(!isOpen);
  };
  const onTriggerCtx = e => {
    if (!contextMenu) return;
    e.preventDefault();
    setCoords({
      x: e.clientX,
      y: e.clientY
    });
    setOpen(true);
  };
  const panelStyle = {
    width,
    padding: 6,
    background: 'var(--glass-overlay-bg)',
    border: '1px solid var(--glass-overlay-border)',
    borderRadius: 'var(--radius-xl)',
    boxShadow: 'var(--glass-overlay-shadow)',
    backdropFilter: 'blur(40px) saturate(180%)',
    WebkitBackdropFilter: 'blur(40px) saturate(180%)',
    transformOrigin: (vert === 'top' ? 'bottom' : 'top') + ' ' + (horiz === 'end' ? 'right' : 'left'),
    animation: 'nx-menu-in var(--motion-quick, 0.28s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both'
  };
  const Item = ({
    it
  }) => {
    const [hover, setHover] = useState(false);
    const [subOpen, setSubOpen] = useState(false);
    if (it.divider) return /*#__PURE__*/React.createElement("div", {
      style: {
        height: 1,
        background: 'var(--glass-divider)',
        margin: '5px 8px'
      }
    });
    if (it.label && it.header) return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '6px 11px 4px',
        fontFamily: 'var(--font-sans)',
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: '0.06em',
        textTransform: 'uppercase',
        color: 'var(--glass-text-secondary)'
      }
    }, it.label);
    const hasSub = Array.isArray(it.submenu) && it.submenu.length > 0;
    return /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'relative'
      },
      onMouseEnter: () => {
        setHover(true);
        if (hasSub) setSubOpen(true);
      },
      onMouseLeave: () => {
        setHover(false);
        if (hasSub) setSubOpen(false);
      }
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => {
        if (it.disabled) return;
        if (hasSub) {
          setSubOpen(o => !o);
          return;
        }
        it.onSelect && it.onSelect();
        setOpen(false);
      },
      disabled: it.disabled,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        width: '100%',
        padding: '8px 11px',
        border: 'none',
        cursor: it.disabled ? 'not-allowed' : 'pointer',
        textAlign: 'left',
        borderRadius: 'var(--radius-md)',
        opacity: it.disabled ? 0.4 : 1,
        background: hover && !it.disabled ? 'var(--glass-hover-bg)' : 'transparent',
        fontFamily: 'var(--font-sans)',
        fontSize: 14,
        fontWeight: 500,
        color: it.destructive ? 'var(--color-destructive)' : 'var(--glass-text-strong)',
        transition: 'background 0.12s ease',
        WebkitTapHighlightColor: 'transparent'
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        flexShrink: 0,
        width: 17,
        opacity: 0.85
      }
    }, it.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, it.label), it.shortcut && /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 12,
        color: 'var(--glass-text-secondary)',
        fontVariantNumeric: 'tabular-nums',
        flexShrink: 0
      }
    }, it.shortcut), hasSub && /*#__PURE__*/React.createElement("svg", {
      width: "14",
      height: "14",
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        flexShrink: 0,
        color: 'var(--glass-text-secondary)',
        opacity: 0.7
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: submenuLeft ? 'M15 6l-6 6 6 6' : 'M9 6l6 6-6 6',
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    })), !hasSub && it.checked && /*#__PURE__*/React.createElement("svg", {
      width: "14",
      height: "14",
      viewBox: "0 0 24 24",
      fill: "none",
      style: {
        flexShrink: 0,
        color: 'var(--color-info)'
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "M4 12.5l5 5L20 6.5",
      stroke: "currentColor",
      strokeWidth: "2.6",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }))), hasSub && subOpen && /*#__PURE__*/React.createElement("div", {
      role: "menu",
      style: {
        position: 'absolute',
        top: -6,
        zIndex: 1,
        ...(submenuLeft ? {
          right: '100%'
        } : {
          left: '100%'
        }),
        ...panelStyle
      }
    }, it.submenu.map((s, i) => /*#__PURE__*/React.createElement(Item, {
      key: i,
      it: s
    }))));
  };
  return /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    style: {
      position: 'relative',
      display: 'inline-flex'
    },
    onContextMenu: onTriggerCtx
  }, /*#__PURE__*/React.createElement("span", {
    onClick: onTriggerClick,
    style: {
      display: 'inline-flex'
    }
  }, trigger), isOpen && /*#__PURE__*/React.createElement("div", {
    role: "menu",
    style: {
      ...panelPos,
      zIndex: 'var(--z-overlay, 20)',
      ...panelStyle,
      ...style
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement(Item, {
    key: i,
    it: it
  }))));
}

/**
 * ContextMenu — convenience wrapper: right-click `children` to open `items`.
 */
function ContextMenu({
  children,
  items,
  width,
  style
}) {
  return /*#__PURE__*/React.createElement(Menu, {
    contextMenu: true,
    trigger: children,
    items: items,
    width: width,
    style: style
  });
}
Object.assign(__ds_scope, { Menu, ContextMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Menu.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Modal.jsx
try { (() => {
/**
 * Modal — centered glass dialog for AIVA OS (maps to ion-modal).
 * Dimmed backdrop + a frosted panel with optional title bar and close button.
 * Render conditionally on `open`. Click the backdrop or ✕ to dismiss.
 */
function Modal({
  open = false,
  onClose,
  title,
  children,
  footer,
  width = 440,
  showClose = true,
  dismissOnBackdrop = true,
  style = {}
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    onClick: () => dismissOnBackdrop && onClose && onClose(),
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 'var(--z-modal, 30)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 20,
      background: 'var(--glass-scrim)',
      backdropFilter: 'blur(4px)',
      WebkitBackdropFilter: 'blur(4px)',
      animation: 'nx-scrim-in var(--motion-quick, 0.28s) ease both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    role: "dialog",
    "aria-modal": "true",
    style: {
      position: 'relative',
      width: '100%',
      maxWidth: width,
      maxHeight: 'calc(100vh - 40px)',
      display: 'flex',
      flexDirection: 'column',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-3xl)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      animation: 'nx-overlay-in var(--motion-slow, 0.5s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both',
      overflow: 'hidden',
      ...style
    }
  }, (title || showClose) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      padding: '16px 18px',
      borderBottom: title ? '1px solid var(--glass-divider)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 17,
      fontWeight: 600,
      color: 'var(--glass-text-strong)'
    }
  }, title), showClose && /*#__PURE__*/React.createElement("button", {
    onClick: () => onClose && onClose(),
    "aria-label": "Schlie\xDFen",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: 28,
      height: 28,
      borderRadius: '50%',
      border: 'none',
      cursor: 'pointer',
      background: 'var(--glass-card-bg)',
      color: 'var(--glass-text-primary)',
      flexShrink: 0,
      marginLeft: 'auto',
      transition: 'transform var(--motion-quick, 0.28s) var(--motion-spring-soft, ease), background var(--motion-swift, 0.16s) ease'
    },
    onMouseEnter: e => {
      e.currentTarget.style.transform = 'scale(1.1)';
    },
    onMouseLeave: e => {
      e.currentTarget.style.transform = 'scale(1)';
    },
    onMouseDown: e => {
      e.currentTarget.style.transform = 'scale(0.88)';
    },
    onMouseUp: e => {
      e.currentTarget.style.transform = 'scale(1.1)';
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round"
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 18,
      overflowY: 'auto',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      color: 'var(--glass-text-primary)',
      lineHeight: 1.5
    }
  }, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      justifyContent: 'flex-end',
      padding: '12px 18px',
      borderTop: '1px solid var(--glass-divider)'
    }
  }, footer)));
}
Object.assign(__ds_scope, { Modal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Modal.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Popover.jsx
try { (() => {
const {
  useState,
  useRef,
  useEffect
} = React;
/**
 * Popover — anchored floating glass panel for AIVA OS (maps to ion-popover).
 * Wraps a trigger; clicking it opens a frosted panel positioned relative to
 * the trigger. Click outside or press Escape to dismiss. Controlled or uncontrolled.
 */
function Popover({
  trigger,
  children,
  open,
  onOpenChange,
  placement = 'bottom-start',
  offset = 8,
  width,
  style = {}
}) {
  const [internal, setInternal] = useState(false);
  const isOpen = open !== undefined ? open : internal;
  const wrapRef = useRef(null);
  const setOpen = v => {
    if (open === undefined) setInternal(v);
    onOpenChange && onOpenChange(v);
  };
  useEffect(() => {
    if (!isOpen) return;
    const onDoc = e => {
      if (wrapRef.current && !wrapRef.current.contains(e.target)) setOpen(false);
    };
    const onKey = e => {
      if (e.key === 'Escape') setOpen(false);
    };
    document.addEventListener('mousedown', onDoc);
    document.addEventListener('keydown', onKey);
    return () => {
      document.removeEventListener('mousedown', onDoc);
      document.removeEventListener('keydown', onKey);
    };
  }, [isOpen]);
  const [vert, horiz] = placement.split('-'); // top|bottom , start|end
  const panelPos = {
    position: 'absolute',
    ...(vert === 'top' ? {
      bottom: `calc(100% + ${offset}px)`
    } : {
      top: `calc(100% + ${offset}px)`
    }),
    ...(horiz === 'end' ? {
      right: 0
    } : {
      left: 0
    })
  };
  return /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    style: {
      position: 'relative',
      display: 'inline-flex'
    }
  }, /*#__PURE__*/React.createElement("span", {
    onClick: () => setOpen(!isOpen),
    style: {
      display: 'inline-flex'
    }
  }, trigger), isOpen && /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    style: {
      ...panelPos,
      zIndex: 'var(--z-overlay, 20)',
      width: width || 'max-content',
      minWidth: 160,
      maxWidth: 280,
      padding: 6,
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-xl)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      transformOrigin: vert === 'top' ? 'bottom' : 'top',
      animation: 'nx-menu-in var(--motion-quick, 0.28s) var(--motion-spring, cubic-bezier(0.34,1.4,0.5,1)) both',
      ...style
    }
  }, children));
}

/**
 * PopoverItem — a single tappable row for use inside a Popover menu.
 */
function PopoverItem({
  children,
  icon,
  onClick,
  destructive = false
}) {
  const [hover, setHover] = useState(false);
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      width: '100%',
      padding: '9px 11px',
      border: 'none',
      cursor: 'pointer',
      textAlign: 'left',
      borderRadius: 'var(--radius-md)',
      background: hover ? 'var(--glass-hover-bg)' : 'transparent',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 500,
      color: destructive ? 'var(--color-destructive)' : 'var(--glass-text-strong)',
      transition: 'background 0.12s ease',
      WebkitTapHighlightColor: 'transparent'
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0,
      opacity: 0.85
    }
  }, icon), children);
}
Object.assign(__ds_scope, { Popover, PopoverItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Popover.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Tooltip.jsx
try { (() => {
const {
  useState
} = React;
/**
 * Tooltip — small glass label shown on hover/focus for AIVA OS.
 * Wraps any element; reveals a frosted bubble after a short delay.
 */
function Tooltip({
  label,
  children,
  placement = 'top',
  delay = 350,
  style = {}
}) {
  const [show, setShow] = useState(false);
  const timer = React.useRef(null);
  const enter = () => {
    timer.current = setTimeout(() => setShow(true), delay);
  };
  const leave = () => {
    clearTimeout(timer.current);
    setShow(false);
  };
  const pos = {
    top: {
      bottom: 'calc(100% + 8px)',
      left: '50%',
      transform: 'translateX(-50%)'
    },
    bottom: {
      top: 'calc(100% + 8px)',
      left: '50%',
      transform: 'translateX(-50%)'
    },
    left: {
      right: 'calc(100% + 8px)',
      top: '50%',
      transform: 'translateY(-50%)'
    },
    right: {
      left: 'calc(100% + 8px)',
      top: '50%',
      transform: 'translateY(-50%)'
    }
  }[placement];
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      display: 'inline-flex'
    },
    onMouseEnter: enter,
    onMouseLeave: leave,
    onFocus: enter,
    onBlur: leave
  }, children, show && /*#__PURE__*/React.createElement("span", {
    role: "tooltip",
    style: {
      position: 'absolute',
      ...pos,
      zIndex: 'var(--z-overlay, 20)',
      padding: '6px 10px',
      whiteSpace: 'nowrap',
      pointerEvents: 'none',
      fontFamily: 'var(--font-sans)',
      fontSize: 12,
      fontWeight: 500,
      color: 'var(--glass-text-strong)',
      background: 'var(--glass-overlay-bg)',
      border: '1px solid var(--glass-overlay-border)',
      borderRadius: 'var(--radius-md)',
      boxShadow: 'var(--glass-overlay-shadow)',
      backdropFilter: 'blur(40px) saturate(180%)',
      WebkitBackdropFilter: 'blur(40px) saturate(180%)',
      ...style
    }
  }, label));
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Tooltip.jsx", error: String((e && e.message) || e) }); }

// components/window/Icon.jsx
try { (() => {
/**
 * Icon — minimal Lucide-style SVG renderer for AIVA OS.
 * Pass the raw inner SVG markup (Lucide path data) as `paths`.
 * Replaces the per-template `_ic()` helper so every window draws
 * its chrome icons from one place.
 */
function Icon({
  paths,
  size = 16,
  strokeWidth = 2,
  fill = 'none',
  color = 'currentColor',
  style = {}
}) {
  return React.createElement('svg', {
    width: size,
    height: size,
    viewBox: '0 0 24 24',
    fill,
    stroke: color,
    strokeWidth,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    style,
    dangerouslySetInnerHTML: {
      __html: paths
    }
  });
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/Icon.jsx", error: String((e && e.message) || e) }); }

// components/window/WindowActionBar.jsx
try { (() => {
/** One column button (icon over label) in the action bar. */
function ActionButton({
  label,
  icon,
  variant = 'default',
  onClick
}) {
  const [hover, setHover] = React.useState(false);
  const primary = variant === 'primary';
  const danger = variant === 'danger';
  const muted = variant === 'muted';
  const color = danger ? 'rgba(239,68,68,0.92)' : primary ? 'var(--glass-text-strong)' : muted ? 'var(--glass-text-secondary)' : 'var(--glass-text-primary)';
  let background = 'transparent';
  let border = '1px solid transparent';
  let boxShadow = 'none';
  if (primary) {
    background = hover ? 'var(--glass-active-bg)' : 'var(--glass-hover-bg)';
    border = '1px solid var(--glass-window-bar-border)';
    boxShadow = 'inset 0 1px 0 rgba(255,255,255,0.18)';
  } else if (hover) {
    background = danger ? 'rgba(239,68,68,0.16)' : 'var(--glass-hover-bg)';
  }
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 3,
      padding: '7px 12px',
      borderRadius: 9999,
      border,
      background,
      boxShadow,
      color,
      cursor: 'pointer',
      fontFamily: 'var(--font-sans)',
      transition: 'background 0.12s, color 0.12s'
    }
  }, icon, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 9,
      fontWeight: 500,
      whiteSpace: 'nowrap',
      letterSpacing: '0.1px'
    }
  }, label));
}

/**
 * WindowActionBar — the floating glass pill of column buttons that sits
 * at the bottom-centre of an AIVA OS window's content area. Pass `items`:
 * each is { label, icon, variant?, onClick } or { divider: true }.
 * Variants: 'primary' (filled), 'default', 'danger' (red), 'muted'.
 */
function WindowActionBar({
  items = [],
  style = {}
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 2,
      padding: 5,
      borderRadius: 9999,
      background: 'var(--glass-window-bar-bg)',
      border: '1px solid var(--glass-window-bar-border)',
      backdropFilter: 'blur(72px) saturate(180%)',
      WebkitBackdropFilter: 'blur(72px) saturate(180%)',
      boxShadow: 'var(--glass-window-bar-shadow)',
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, items.map((it, i) => it.divider ? /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      width: 1,
      height: 34,
      background: 'var(--glass-divider)',
      margin: '0 3px'
    }
  }) : /*#__PURE__*/React.createElement(ActionButton, {
    key: i,
    label: it.label,
    icon: it.icon,
    variant: it.variant,
    onClick: it.onClick
  })));
}
Object.assign(__ds_scope, { WindowActionBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/WindowActionBar.jsx", error: String((e && e.message) || e) }); }

// components/window/WindowButton.jsx
try { (() => {
/**
 * WindowButton — the chrome icon button used across AIVA OS window
 * title bars and pane headers (formerly the per-template `.ibtn`).
 * Ghost by default; `active` keeps the hover surface pinned; `danger`
 * is the red close-style treatment.
 */
function WindowButton({
  children,
  onClick,
  title,
  active = false,
  danger = false,
  size = 30,
  radius = 8,
  style = {}
}) {
  const [hover, setHover] = React.useState(false);
  const [pressed, setPressed] = React.useState(false);
  const on = active || hover;
  const color = danger ? hover ? 'rgba(239,68,68,1)' : 'rgba(239,68,68,0.85)' : on ? 'var(--glass-text-strong)' : 'var(--glass-window-icon)';
  const background = danger ? hover ? 'rgba(239,68,68,0.18)' : 'transparent' : on ? 'var(--glass-hover-bg)' : 'transparent';
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    title: title,
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setPressed(false);
    },
    onMouseDown: () => setPressed(true),
    onMouseUp: () => setPressed(false),
    style: {
      width: size,
      height: size,
      borderRadius: radius,
      background,
      border: 'none',
      color,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      flexShrink: 0,
      padding: 0,
      transform: pressed ? 'scale(0.88)' : 'scale(1)',
      transition: pressed ? 'transform var(--motion-instant, 0.1s) ease-out, background 0.12s, color 0.12s' : 'transform var(--motion-quick, 0.28s) var(--motion-spring-soft, cubic-bezier(0.34,1.2,0.5,1)), background 0.12s, color 0.12s',
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { WindowButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/WindowButton.jsx", error: String((e && e.message) || e) }); }

// components/window/WindowControls.jsx
try { (() => {
/**
 * WindowControls — the minimise / maximise / close cluster that sits
 * at the right edge of every AIVA OS window title bar.
 */
function WindowControls({
  onMinimize,
  onMaximize,
  onClose,
  style = {}
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      flexShrink: 0,
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.WindowButton, {
    title: "Minimieren",
    size: 34,
    radius: 10,
    onClick: onMinimize
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    paths: "<path d=\"M5 12h14\"/>",
    size: 14,
    strokeWidth: 2.5
  })), /*#__PURE__*/React.createElement(__ds_scope.WindowButton, {
    title: "Maximieren",
    size: 34,
    radius: 10,
    onClick: onMaximize
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    paths: "<rect width=\"18\" height=\"18\" x=\"3\" y=\"3\" rx=\"2\"/>",
    size: 13,
    strokeWidth: 2.5
  })), /*#__PURE__*/React.createElement(__ds_scope.WindowButton, {
    title: "Schlie\xDFen",
    size: 34,
    radius: 10,
    danger: true,
    onClick: onClose
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    paths: "<path d=\"M18 6 6 18\"/><path d=\"m6 6 12 12\"/>",
    size: 14,
    strokeWidth: 2.5
  })));
}
Object.assign(__ds_scope, { WindowControls });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/WindowControls.jsx", error: String((e && e.message) || e) }); }

// components/window/Window.jsx
try { (() => {
/**
 * Window — the shared glass window shell for AIVA OS. Provides the
 * dense window surface, the three-zone title chrome (leading / centered
 * toolbar / trailing + window controls) and a flex body region. Every
 * app and settings window is composed from this — pass the body
 * (Sidebar, WindowPane, etc) as children.
 *
 * Theming: pass theme="dark" | "light" to scope the window itself, or
 * leave it unset to inherit the ambient .dark class. Reads the
 * --glass-window-* tokens.
 */
function Window({
  theme,
  width = 960,
  height = 600,
  leading,
  toolbar,
  trailing,
  windowControls = true,
  onMinimize,
  onMaximize,
  onClose,
  animate = true,
  rootRef,
  className = '',
  style = {},
  bodyStyle = {},
  children
}) {
  const dark = theme === 'dark';
  const cls = [dark ? 'dark' : '', className].filter(Boolean).join(' ');
  const divider = /*#__PURE__*/React.createElement("div", {
    style: {
      width: 1,
      height: 16,
      background: 'var(--glass-divider)',
      margin: '0 6px'
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    ref: rootRef,
    className: cls || undefined,
    style: {
      position: 'relative',
      width,
      height,
      maxWidth: '100%',
      background: 'var(--glass-window-bg)',
      border: '1px solid var(--glass-window-border)',
      borderRadius: 'var(--radius-3xl)',
      backdropFilter: 'blur(72px) saturate(180%)',
      WebkitBackdropFilter: 'blur(72px) saturate(180%)',
      boxShadow: 'var(--glass-window-shadow)',
      display: 'flex',
      flexDirection: 'column',
      overflow: 'hidden',
      fontFamily: 'var(--font-sans)',
      animation: animate ? 'aiva-window-in 0.35s cubic-bezier(0.16,1,0.3,1)' : 'none',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      padding: '10px 12px',
      flexShrink: 0,
      position: 'relative',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      flexShrink: 0
    }
  }, leading), toolbar && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: '50%',
      transform: 'translateX(-50%)',
      display: 'flex',
      alignItems: 'center',
      gap: 1
    }
  }, toolbar), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      marginLeft: 'auto',
      flexShrink: 0
    }
  }, trailing, windowControls && /*#__PURE__*/React.createElement(React.Fragment, null, trailing && divider, /*#__PURE__*/React.createElement(__ds_scope.WindowControls, {
    onMinimize: onMinimize,
    onMaximize: onMaximize,
    onClose: onClose
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      overflow: 'hidden',
      ...bodyStyle
    }
  }, children));
}
Object.assign(__ds_scope, { Window });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/Window.jsx", error: String((e && e.message) || e) }); }

// components/window/WindowPane.jsx
try { (() => {
/**
 * WindowPane — the inner content card inside an AIVA OS window
 * (content area, properties panel, etc). Rounded glass card with an
 * optional title/actions header and a scrolling body. Reads the
 * --glass-window-pane-* tokens, so it matches the window surface in
 * both dark and light mode.
 */
function WindowPane({
  title,
  header,
  headerActions,
  padded = true,
  style = {},
  bodyStyle = {},
  children
}) {
  const hasHeader = title || header || headerActions;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      borderRadius: 'var(--radius-2xl)',
      background: 'var(--glass-window-pane-bg)',
      border: '1px solid var(--glass-window-pane-border)',
      boxShadow: 'inset 0 1px 0 var(--glass-window-pane-inset)',
      display: 'flex',
      flexDirection: 'column',
      overflow: 'hidden',
      ...style
    }
  }, hasHeader && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 8,
      padding: '12px 16px 10px',
      flexShrink: 0,
      borderBottom: '1px solid var(--glass-divider)'
    }
  }, header || /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      fontWeight: 600,
      color: 'var(--glass-text-strong)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, title), headerActions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 2,
      flexShrink: 0
    }
  }, headerActions)), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: padded ? '6px 8px' : 0,
      ...bodyStyle
    }
  }, children));
}
Object.assign(__ds_scope, { WindowPane });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/WindowPane.jsx", error: String((e && e.message) || e) }); }

// components/window/AppWindow.jsx
try { (() => {
/**
 * AppWindow — the full AIVA OS window scaffold and the single base every
 * app/settings window is built from. Wraps the Window shell with a
 * three-zone body (sidebar · content pane · properties pane), an
 * optional floating action bar, and a responsive layout that lifts the
 * side panes into glass overlays as the window narrows.
 *
 * Provide identity chrome via the leading/toolbar/trailing slots; the
 * sidebar-toggle and properties-toggle buttons are added automatically.
 * Content goes in `children`; `sidebar`, `properties` and `actionBar`
 * are slots.
 */
function AppWindow({
  theme,
  width = 960,
  height = 600,
  leading,
  toolbar,
  trailing,
  windowControls = true,
  onMinimize,
  onMaximize,
  onClose,
  // body
  sidebar,
  sidebarWidth = 240,
  contentTitle,
  contentHeader,
  contentActions,
  contentPadded = true,
  children,
  properties,
  propertiesTitle = 'Eigenschaften',
  propertiesWidth = 240,
  showProperties = true,
  actionBar,
  responsive = true,
  style = {},
  bodyStyle = {}
}) {
  const rootRef = React.useRef(null);
  const modeRef = React.useRef('desktop');
  const [mode, setMode] = React.useState('desktop');
  const [leftOpen, setLeftOpen] = React.useState(false);
  const [rightOpen, setRightOpen] = React.useState(false);
  const [sidebarHidden, setSidebarHidden] = React.useState(false);
  const [propsHidden, setPropsHidden] = React.useState(false);
  React.useEffect(() => {
    const el = rootRef.current;
    if (!responsive || !el || typeof ResizeObserver === 'undefined') return undefined;
    const ro = new ResizeObserver(entries => {
      const w = entries[0].contentRect.width;
      const m = w >= 820 ? 'desktop' : w >= 560 ? 'compact' : 'mobile';
      if (m !== modeRef.current) {
        modeRef.current = m;
        setMode(m);
        setLeftOpen(false);
        setRightOpen(false);
      }
    });
    ro.observe(el);
    return () => ro.disconnect();
  }, [responsive]);
  const hasProps = !!properties && showProperties;
  const overlayMode = mode === 'compact' || mode === 'mobile';
  const toggleSidebar = () => {
    if (mode === 'desktop') setSidebarHidden(h => !h);else {
      setRightOpen(false);
      setLeftOpen(o => !o);
    }
  };
  const toggleProps = () => {
    if (mode === 'mobile') {
      setLeftOpen(false);
      setRightOpen(o => !o);
    } else setPropsHidden(h => !h);
  };
  const closeOverlays = () => {
    setLeftOpen(false);
    setRightOpen(false);
  };

  // ── Composed chrome: identity slots + structural toggles ──────────
  const composedLeading = /*#__PURE__*/React.createElement(React.Fragment, null, leading, sidebar && /*#__PURE__*/React.createElement(__ds_scope.WindowButton, {
    title: "Seitenleiste",
    active: mode === 'desktop' ? !sidebarHidden : leftOpen,
    onClick: toggleSidebar
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    paths: "<rect width=\"18\" height=\"18\" x=\"3\" y=\"3\" rx=\"2\"/><path d=\"M9 3v18\"/>",
    size: 14
  })));
  const composedTrailing = /*#__PURE__*/React.createElement(React.Fragment, null, hasProps && /*#__PURE__*/React.createElement(__ds_scope.WindowButton, {
    title: "Eigenschaften",
    active: mode === 'mobile' ? rightOpen : !propsHidden,
    onClick: toggleProps
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    paths: "<rect width=\"18\" height=\"18\" x=\"3\" y=\"3\" rx=\"2\"/><path d=\"M15 3v18\"/>",
    size: 14
  })), trailing);

  // ── Pane visibility ───────────────────────────────────────────────
  const sidebarInline = sidebar && mode === 'desktop' && !sidebarHidden;
  const sidebarOverlay = sidebar && overlayMode;
  const propsInline = hasProps && (mode === 'desktop' || mode === 'compact') && !propsHidden;
  const propsOverlay = hasProps && mode === 'mobile';
  const overlaySurface = {
    background: 'var(--glass-window-bg)',
    border: '1px solid var(--glass-window-border)',
    backdropFilter: 'blur(72px) saturate(180%)',
    WebkitBackdropFilter: 'blur(72px) saturate(180%)',
    boxShadow: 'var(--glass-window-shadow)',
    display: 'flex',
    flexDirection: 'column',
    overflow: 'hidden',
    transition: 'transform .34s cubic-bezier(.16,1,.3,1), opacity .26s ease'
  };
  return /*#__PURE__*/React.createElement(__ds_scope.Window, {
    theme: theme,
    width: width,
    height: height,
    rootRef: rootRef,
    leading: composedLeading,
    toolbar: toolbar,
    trailing: composedTrailing,
    windowControls: windowControls,
    onMinimize: onMinimize,
    onMaximize: onMaximize,
    onClose: onClose,
    style: style,
    bodyStyle: {
      gap: 8,
      padding: '0 8px 16px 8px',
      position: 'relative',
      minHeight: 0,
      ...bodyStyle
    }
  }, sidebarInline && /*#__PURE__*/React.createElement("div", {
    style: {
      width: sidebarWidth,
      flexShrink: 0,
      height: '100%'
    }
  }, sidebar), sidebarOverlay && /*#__PURE__*/React.createElement("div", {
    style: {
      ...overlaySurface,
      position: 'absolute',
      zIndex: 40,
      ...(mode === 'mobile' ? {
        top: 0,
        left: 0,
        bottom: 0,
        width: 'min(300px,82%)',
        borderRadius: '0 20px 20px 0'
      } : {
        top: 16,
        left: 16,
        bottom: 16,
        width: 254,
        borderRadius: 28
      }),
      padding: '14px 14px 0',
      transform: leftOpen ? 'translateX(0)' : 'translateX(-118%)',
      opacity: leftOpen ? 1 : 0,
      pointerEvents: leftOpen ? 'auto' : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0
    }
  }, sidebar)), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      position: 'relative',
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.WindowPane, {
    title: contentTitle,
    header: contentHeader,
    headerActions: contentActions,
    padded: contentPadded
  }, children), actionBar && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 14,
      left: 0,
      right: 0,
      display: 'flex',
      justifyContent: 'center',
      zIndex: 10,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      pointerEvents: 'auto'
    }
  }, actionBar))), propsInline && /*#__PURE__*/React.createElement(__ds_scope.WindowPane, {
    title: propertiesTitle,
    style: {
      flex: 'none',
      width: propertiesWidth,
      minWidth: propertiesWidth
    },
    bodyStyle: {
      padding: '4px 6px'
    }
  }, properties), propsOverlay && /*#__PURE__*/React.createElement("div", {
    style: {
      ...overlaySurface,
      position: 'absolute',
      zIndex: 30,
      top: 0,
      right: 0,
      bottom: 0,
      width: 'min(320px,86%)',
      borderRadius: '20px 0 0 20px',
      transform: rightOpen ? 'translateX(0)' : 'translateX(112%)',
      opacity: rightOpen ? 1 : 0,
      pointerEvents: rightOpen ? 'auto' : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 16px',
      borderBottom: '1px solid var(--glass-divider)',
      flexShrink: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--glass-text-strong)'
    }
  }, propertiesTitle), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '4px 6px'
    }
  }, properties)), overlayMode && /*#__PURE__*/React.createElement("div", {
    onClick: closeOverlays,
    style: {
      position: 'absolute',
      inset: 0,
      zIndex: 25,
      background: 'var(--glass-scrim)',
      backdropFilter: 'blur(2px)',
      WebkitBackdropFilter: 'blur(2px)',
      opacity: leftOpen || rightOpen ? 1 : 0,
      pointerEvents: leftOpen || rightOpen ? 'auto' : 'none',
      transition: 'opacity .26s ease'
    }
  }));
}
Object.assign(__ds_scope, { AppWindow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/window/AppWindow.jsx", error: String((e && e.message) || e) }); }

// design_handoff_files_window/source/ds-base.js
try { (() => {
// Loads this design system into the template. In a consuming project, point
// base at the bound DS folder relative to this file (e.g. '_ds/<folder>' at
// the project root, '../_ds/<folder>' one level down) — one line to edit.
(() => {
  const base = '../..';
  for (const p of ["styles.css"]) {
    const l = document.createElement('link');
    l.rel = 'stylesheet';
    l.href = base + '/' + p;
    document.head.appendChild(l);
  }
  const s = document.createElement('script');
  s.src = base + '/_ds_bundle.js';
  s.onerror = () => console.error('ds-base.js: failed to load ' + s.src + ' — if this is a consuming project, point the base line in ds-base.js at the bound _ds/<folder> tree relative to this page (e.g. _ds/<folder> at the project root, ../_ds/<folder> one level down); in a fresh design system this can just mean the bundle is not compiled yet');
  document.head.appendChild(s);
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_files_window/source/ds-base.js", error: String((e && e.message) || e) }); }

// design_handoff_os_settings_window/source/ds-base.js
try { (() => {
// Loads this design system into the template. In a consuming project, point
// base at the bound DS folder relative to this file (e.g. '_ds/<folder>' at
// the project root, '../_ds/<folder>' one level down) — one line to edit.
(() => {
  const base = '../..';
  for (const p of ["tokens/fonts.css", "tokens/colors.css", "tokens/typography.css", "tokens/spacing.css", "tokens/glass.css", "styles.css"]) {
    const l = document.createElement('link');
    l.rel = 'stylesheet';
    l.href = base + '/' + p;
    document.head.appendChild(l);
  }
  const s = document.createElement('script');
  s.src = base + '/_ds_bundle.js';
  s.onerror = () => console.error('ds-base.js: failed to load ' + s.src + ' — if this is a consuming project, point the base line in ds-base.js at the bound _ds/<folder> tree relative to this page (e.g. _ds/<folder> at the project root, ../_ds/<folder> one level down); in a fresh design system this can just mean the bundle is not compiled yet');
  document.head.appendChild(s);
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_os_settings_window/source/ds-base.js", error: String((e && e.message) || e) }); }

__ds_ns.DatePicker = __ds_scope.DatePicker;

__ds_ns.GlassCheckbox = __ds_scope.GlassCheckbox;

__ds_ns.GlassRadioGroup = __ds_scope.GlassRadioGroup;

__ds_ns.Rating = __ds_scope.Rating;

__ds_ns.Segment = __ds_scope.Segment;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.Stepper = __ds_scope.Stepper;

__ds_ns.WheelPicker = __ds_scope.WheelPicker;

__ds_ns.AppIcon = __ds_scope.AppIcon;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.GlassButton = __ds_scope.GlassButton;

__ds_ns.GlassCard = __ds_scope.GlassCard;

__ds_ns.GlassToggle = __ds_scope.GlassToggle;

__ds_ns.Banner = __ds_scope.Banner;

__ds_ns.ProgressBar = __ds_scope.ProgressBar;

__ds_ns.Refresher = __ds_scope.Refresher;

__ds_ns.Skeleton = __ds_scope.Skeleton;

__ds_ns.SkeletonText = __ds_scope.SkeletonText;

__ds_ns.Spinner = __ds_scope.Spinner;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.SearchBar = __ds_scope.SearchBar;

__ds_ns.TextArea = __ds_scope.TextArea;

__ds_ns.TextField = __ds_scope.TextField;

__ds_ns.KeyboardKey = __ds_scope.KeyboardKey;

__ds_ns.OnScreenKeyboard = __ds_scope.OnScreenKeyboard;

__ds_ns.LANGS = __ds_scope.LANGS;

__ds_ns.LETTER_ROWS = __ds_scope.LETTER_ROWS;

__ds_ns.HANGUL_SHIFT = __ds_scope.HANGUL_SHIFT;

__ds_ns.NUMBER_ROW = __ds_scope.NUMBER_ROW;

__ds_ns.SYMBOL_LAYERS = __ds_scope.SYMBOL_LAYERS;

__ds_ns.KANA_KEYS = __ds_scope.KANA_KEYS;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Breadcrumbs = __ds_scope.Breadcrumbs;

__ds_ns.Chip = __ds_scope.Chip;

__ds_ns.ListItem = __ds_scope.ListItem;

__ds_ns.List = __ds_scope.List;

__ds_ns.Pagination = __ds_scope.Pagination;

__ds_ns.Sidebar = __ds_scope.Sidebar;

__ds_ns.SplitView = __ds_scope.SplitView;

__ds_ns.SubHeader = __ds_scope.SubHeader;

__ds_ns.TabBar = __ds_scope.TabBar;

__ds_ns.Toolbar = __ds_scope.Toolbar;

__ds_ns.TreeView = __ds_scope.TreeView;

__ds_ns.ActionSheet = __ds_scope.ActionSheet;

__ds_ns.Alert = __ds_scope.Alert;

__ds_ns.FAB = __ds_scope.FAB;

__ds_ns.Menu = __ds_scope.Menu;

__ds_ns.ContextMenu = __ds_scope.ContextMenu;

__ds_ns.Modal = __ds_scope.Modal;

__ds_ns.Popover = __ds_scope.Popover;

__ds_ns.PopoverItem = __ds_scope.PopoverItem;

__ds_ns.Tooltip = __ds_scope.Tooltip;

__ds_ns.AppWindow = __ds_scope.AppWindow;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.Window = __ds_scope.Window;

__ds_ns.WindowActionBar = __ds_scope.WindowActionBar;

__ds_ns.WindowButton = __ds_scope.WindowButton;

__ds_ns.WindowControls = __ds_scope.WindowControls;

__ds_ns.WindowPane = __ds_scope.WindowPane;

})();

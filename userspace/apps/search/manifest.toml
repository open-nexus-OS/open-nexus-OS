# Copyright 2026 Open Nexus OS Contributors
# SPDX-License-Identifier: Apache-2.0
#
# CONTEXT: Search — a DSL app: service-side app search (bundlemgrd ENUMERATE
# does the matching) + launch via the launch authority. The former Rust-crate
# renderer is retired (promote-best: one UI stack).
# Schema: tools/nexus-idl/schemas/manifest.capnp  ·  Docs: docs/packaging/nxb.md

name = "search"
version = "1.0.0"
min_sdk = "1.0.0"
bundle_type = "app"
payload_kind = "ui-program"
abilities = ["search.MainAbility"]
caps = ["nexus.permission.WINDOW", "nexus.permission.ENUMERATE", "nexus.permission.LAUNCH"]
provides = []
resources = []

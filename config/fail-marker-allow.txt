# FAIL markers the QEMU ladder tolerates (scripts/qemu-test.sh, no-fake-green
# gate). One marker prefix per line; '#' starts a comment. Every entry is a
# KNOWN, tracked red — never a hiding place. Remove a line once the proof is
# green again; never add one without the tracking reference.
#
# DSoftBus family (network lanes HOLD until the joint replanning — see
# tasks/IMPLEMENTATION-ORDER.md "Netz-Gruppe"; dsoftbusd announces no ready
# marker and the single-VM QUIC probe has been red since at least 2026-08-25).
SELFTEST: dsoftbus os connect FAIL
SELFTEST: dsoftbus ping FAIL

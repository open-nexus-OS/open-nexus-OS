# Copyright 2026 Open Nexus OS Contributors
# SPDX-License-Identifier: Apache-2.0
#
# CONTEXT: Notes demo-app bundle manifest (RFC-0065 — the v6b demo app launched
# via the abilitymgr lifecycle to prove the full launch handoff).
# Compiled to `manifest.nxb` (Cap'n Proto) by `tools/nxb-pack` and installed into
# the `bundlemgrd` registry; `abilitymgr` resolves the launch ability from here.
# Schema: tools/nexus-idl/schemas/manifest.capnp  ·  Docs: docs/packaging/nxb.md
#
# NOTE: `publisher`/`sig` are zero placeholders (unsigned bring-up bundle); real
# signing lands with the supply-chain pipeline (RFC-0039). `payload.elf` is a
# placeholder until the notes app crate ships its own ELF (TASK-0065 P4/P5).

name = "notes"
version = "1.0.0"
min_sdk = "1.0.0"
bundle_type = "app"
abilities = ["notes.MainAbility"]
caps = ["nexus.permission.WINDOW"]
provides = []
resources = []

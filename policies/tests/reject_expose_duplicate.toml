# expect: ExposeDuplicate
# A NIC-facing (port, proto) has exactly one owner across all subjects.
[[expose."demo.web"]]
port = 8080
proto = "tcp"
cidr_allow = ["10.0.2.0/24"]
rate_per_s = 100
burst = 20
backend = 18080

[[expose."demo.api"]]
port = 8080
proto = "tcp"
cidr_allow = ["10.0.2.0/24"]
rate_per_s = 100
burst = 20
backend = 18081

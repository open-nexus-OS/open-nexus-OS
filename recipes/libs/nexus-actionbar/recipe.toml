[package]
name = "nexus-actionbar"
version = "0.1.0"
description = "Reusable Action Bar + Panels for the Nexus desktop"
license = "Apache-2.0"
repository = "https://example.invalid/nexus-actionbar" # optional

[build]
# Build the library only
command = "cargo"
args = ["build", "--release", "--lib"]
env = { RUSTFLAGS = "", TARGET = "" }

[install]
# No binaries to install; provides a static rlib for other packages (like nexus-launcher).
# If you need to stage headers or resources, add steps here.

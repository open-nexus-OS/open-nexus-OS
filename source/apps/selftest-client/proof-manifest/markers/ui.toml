# Copyright 2026 Open Nexus OS Contributors
# SPDX-License-Identifier: Apache-2.0
#
# CONTEXT: TASK-0055 headless UI/windowd proof markers.
# OWNERS: @ui @runtime
# STATUS: Done
# API_STABILITY: Stable literals once TASK-0055 closes

[marker."windowd: ready (w=64, h=48, hz=60)"]
phase = "end"
proves = "windowd initialized a bounded headless desktop profile"

[marker."windowd: systemui loaded (profile=desktop)"]
phase = "end"
proves = "windowd installed the deterministic base SystemUI/headless layer state"

[marker."windowd: present ok (seq=1 dmg=1)"]
phase = "end"
proves = "windowd composed damaged surface state and produced a minimal present acknowledgement"

[marker."launcher: first frame ok"]
phase = "end"
proves = "launcher queued its first frame and observed present acknowledgement"

[marker."SELFTEST: ui launcher present ok"]
phase = "end"
proves = "selftest observed the launcher present state"

[marker."SELFTEST: ui resize ok"]
phase = "end"
proves = "selftest observed a committed resized surface and present state"

[marker."display: bootstrap on"]
phase = "end"
proves = "visible QEMU scanout bootstrap path was selected by the boot-mode knob"

[marker."display: mode 1280x800 argb8888"]
phase = "end"
proves = "visible bootstrap accepted exactly the fixed 1280x800 ARGB8888 ramfb mode"

[marker."windowd: backend=visible"]
phase = "end"
proves = "windowd selected the visible backend for the TASK-0055C present path"

[marker."windowd: present visible ok"]
phase = "end"
proves = "windowd composed the visible frame and visible-present evidence exists after the guest-side ramfb target is writable"

[marker."display: first scanout ok"]
phase = "end"
proves = "selftest configured ramfb only after windowd visible-present evidence"

[marker."systemui: first frame visible"]
phase = "end"
proves = "the TOML-backed SystemUI desktop shell frame was the visible-present source"

[marker."SELFTEST: display bootstrap guest ok"]
phase = "end"
proves = "legacy TASK-0055B guest-side ramfb bootstrap marker; not emitted by the TASK-0055C visible SystemUI ladder"

[marker."SELFTEST: ui visible present ok"]
phase = "end"
proves = "selftest completed the visible windowd present path with a deterministic SystemUI first frame"

[marker."hidrawd: ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded hidrawd ingest adapter was initialized before live-input proofs"

[marker."hidrawd: device kbd"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the deterministic keyboard source for the visible proof was registered"

[marker."hidrawd: device mouse"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the deterministic mouse source for the visible proof was registered"

[marker."touchd: ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded touch ingest adapter was initialized before touch proof vectors"

[marker."inputd: ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded inputd merge/config/route core was initialized"

[marker."inputd: keymap=de"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the canonical visible proof uses the deterministic de keymap configuration"

[marker."inputd: repeat start code=4"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "repeat scheduling started from a real key press for the canonical A-key proof vector"

[marker."inputd: dispatch windowd cursor=(36,28)"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "inputd drove the existing windowd pointer authority to the deterministic visible cursor target"

[marker."systemui: imed show"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded IME/SystemUI show hook fired only after verified text-focus transition"

[marker."systemui: imed hide"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded IME/SystemUI hide hook fired only after verified focus exit"

[marker."SELFTEST: input keymap de ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed the deterministic de keymap resolution through inputd"

[marker."SELFTEST: input cursor ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed the real hidrawd/inputd/windowd cursor and click-visible path"

[marker."SELFTEST: input touch ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed the bounded touchd/inputd normalized touch sequence"

[marker."SELFTEST: input repeat ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic repeat scheduling through the inputd path"

[marker."windowd: present scheduler on"]
phase = "end"
proves = "windowd v2a present scheduler path was activated by real frame-indexed present state"

[marker."windowd: input on"]
phase = "end"
proves = "windowd v2a input routing path was activated by real pointer/focus/keyboard delivery state"

[marker."windowd: focus -> 1"]
phase = "end"
proves = "windowd updated focus to the deterministic launcher surface after a routed click"

[marker."launcher: click ok"]
phase = "end"
proves = "launcher observed a routed click and updated its double-buffered demo state"

[marker."SELFTEST: ui v2 present ok"]
phase = "end"
proves = "selftest observed windowd v2a scheduler evidence after a real present tick"

[marker."SELFTEST: ui v2 input ok"]
phase = "end"
proves = "selftest observed windowd v2a focus and input delivery evidence"

[marker."windowd: input visible on"]
phase = "end"
proves = "windowd visible-input path was activated after routed pointer/focus/click state changed"

[marker."windowd: full-window color visible"]
phase = "end"
proves = "windowd rendered a full-window colored visible-input proof scene, not a partial placeholder surface"

[marker."windowd: cursor move visible"]
phase = "end"
proves = "windowd rendered deterministic cursor pixels after a routed pointer move"

[marker."windowd: hover visible"]
phase = "end"
proves = "windowd rendered deterministic hover affordance after routed pointer hit-test state changed"

[marker."windowd: focus visible"]
phase = "end"
proves = "windowd rendered deterministic focus affordance after click-driven focus transfer"

[marker."launcher: click visible ok"]
phase = "end"
proves = "bottom-left proof square visibly changed state after a routed click delivery"

[marker."windowd: keyboard visible"]
phase = "end"
proves = "right-side proof square visibly changed state after routed keyboard delivery"

[marker."SELFTEST: ui visible input ok"]
phase = "end"
proves = "selftest observed full-window, cursor, hover, focus, click, and keyboard-visible evidence from windowd authority"

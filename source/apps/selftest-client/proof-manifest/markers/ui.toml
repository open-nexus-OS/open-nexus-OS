# Copyright 2026 Open Nexus OS Contributors
# SPDX-License-Identifier: Apache-2.0
#
# CONTEXT: TASK-0062 GPU-only display proof markers (Phase 6).
# OWNERS: @ui @runtime
# STATUS: In Progress (GPU-only migration)
# API_STABILITY: Stable literals once TASK-0062 Phase 6 closes
#
# Architecture: GPU-only path. windowd is the sole display owner — it creates the
# framebuffer VMO and sends it to gpud via OP_SET_FRAMEBUFFER_VMO. gpud provides
# scanout (ATTACH_BACKING + SET_SCANOUT) on demand. No fbdevd, no ramfb, no
# handoff from another service — one owner, one path.

[marker."windowd: ready (w=1280, h=800, hz=120)"]
phase = "end"
proves = "windowd initialized the fixed visible desktop profile"

[marker."windowd: backend=gpu"]
phase = "bringup"
proves = "windowd is the sole display owner — creates framebuffer VMO, hands off to gpud for scanout"

[marker."display: mode 1280x800 argb8888"]
phase = "end"
proves = "GPU-only display path accepted exactly the fixed 1280x800 BGRA8888 virtio-gpu mode"

[marker."windowd: compose ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "windowd completed first-frame composition into gpud scanout VMO"

[marker."windowd: backend=visible"]
phase = "end"
proves = "windowd selected the visible backend for the GPU present path"

[marker."windowd: present ok (seq=1 dmg=1)"]
phase = "end"
proves = "windowd composed damaged surface state and produced a minimal present acknowledgement"

[marker."windowd: present visible ok"]
phase = "end"
proves = "windowd composed the visible frame into gpud scanout VMO"

[marker."windowd: present scheduler on"]
phase = "end"
proves = "windowd v2a present scheduler path was activated"

[marker."display: first scanout ok"]
phase = "end"
proves = "observer-only selftest saw first-scanout readiness after windowd visible-present evidence"

[marker."systemui: first frame visible"]
phase = "end"
proves = "TOML-backed SystemUI desktop shell frame was the visible-present source"

[marker."SELFTEST: ui v2 present ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed the v2 present state in GPU path"

[marker."SELFTEST: ui visible present ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "observer-only selftest completed service-owned visible present path in GPU-only mode"

[marker."windowd: cursor svg loaded"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "Mocu SVG cursor was rasterized and available for composition"

[marker."windowd: wallpaper visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "wallpaper source was loaded and visible in composed frame"

[marker."windowd: text target visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "text proof target was rendered in composed frame"

[marker."windowd: icon target visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "icon proof target was rendered in composed frame"

[marker."windowd: effects on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "shadow/blur/glass effects were activated in compositor"

[marker."windowd: effect blur ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "backdrop blur effect rendered correctly"

[marker."SELFTEST: ui v3 effect ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed effects-path evidence in GPU path"

[marker."launcher: first frame ok"]
phase = "end"
proves = "launcher queued its first frame and observed present acknowledgement"

[marker."SELFTEST: ui launcher present ok"]
phase = "end"
proves = "selftest observed the launcher present state"

[marker."SELFTEST: ui resize ok"]
phase = "end"
proves = "selftest observed a committed resized surface and present state"

[marker."windowd: input on"]
phase = "end"
proves = "windowd v2a input routing path was activated"

[marker."windowd: focus -> 1"]
phase = "end"
proves = "windowd updated focus to the deterministic launcher surface after a routed click"

[marker."windowd: input visible on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "input-visible flag set after first live input batch delivery"

[marker."windowd: full-window color visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "full-window compositor fill color is visible in the composed frame"

[marker."windowd: cursor move visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "cursor position change was reflected in the composed visible frame"

[marker."windowd: hover visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "hover affordance was visible on the routed interactive target"

[marker."windowd: focus visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "focus affordance was visible on the focused proof panel"

[marker."windowd: keyboard visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "keyboard interaction affordance was visible"

[marker."windowd: wheel visible"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "wheel interaction indicator was visible"

[marker."launcher: click ok"]
phase = "end"
proves = "launcher reported click event on the routed cursor <-> interactive-panel path"

[marker."launcher: click visible ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "launcher click proof was confirmed as visible in the composed frame"

[marker."SELFTEST: ui v2 input ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed v2 input evidence"

[marker."SELFTEST: ui visible input ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed visible-input proof evidence"

[marker."SELFTEST: ui visible wheel ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed wheel interaction evidence"

[marker."SELFTEST: ui v2b assets ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed v2b asset evidence (cursor, wallpaper, text, icon visible)"

[marker."hidrawd: ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the bounded hidrawd ingest adapter was initialized"

[marker."hidrawd: device kbd"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the deterministic keyboard source was registered"

[marker."hidrawd: device mouse"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "the deterministic mouse source was registered"

[marker."hidrawd: virtio-input mmio ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "hidrawd opened a real virtio-input MMIO owner path"

[marker."hidrawd: virtio-input keyboard ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "hidrawd classified at least one real virtio-input device as keyboard"

[marker."hidrawd: virtio-input pointer ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "hidrawd classified at least one real virtio-input device as pointer"

[marker."hidrawd: virtio-input raw event seen"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "real virtio-input device delivered non-empty raw events"

[marker."hidrawd: ingress adapter ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "hidrawd normalized real raw input into bounded HID batch contract"

[marker."touchd: ready"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "bounded touch ingest adapter was initialized"

[marker."inputd: ready"]
phase = "end"
proves = "bounded inputd merge/config/route core was initialized"

[marker."inputd: keymap=de"]
phase = "end"
proves = "canonical inputd startup path selected deterministic de keymap"

[marker."inputd: repeat start code=4"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "repeat scheduling started from a real key press"

[marker."inputd: dispatch windowd cursor=(36,28)"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "inputd drove windowd pointer authority to deterministic cursor target"

[marker."inputd: live pointer route on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "inputd routed a real normalized pointer batch into windowd-visible state"

[marker."inputd: live keyboard route on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "inputd routed a real normalized keyboard batch into windowd-visible state"

[marker."systemui: imed show"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "bounded IME/SystemUI show hook fired after verified text-focus transition"

[marker."systemui: imed hide"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "bounded IME/SystemUI hide hook fired after verified focus exit"

[marker."SELFTEST: input keymap de ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic de keymap resolution through inputd"

[marker."SELFTEST: input cursor ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed real hidrawd/inputd/windowd cursor path"

[marker."SELFTEST: input touch ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed bounded touchd/inputd normalized touch sequence"

[marker."SELFTEST: input repeat ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic repeat scheduling through inputd"

[marker."windowd: clipping on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "windowd enabled clipped proof-panel composition"

[marker."windowd: scroll on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "windowd applied deterministic scroll translation to proof-panel"

[marker."windowd: font rasterized"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "windowd rasterized the Inter font for the proof-panel text target"

[marker."windowd: text wrapping on"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "windowd completed text-shaping/wrapping for supported textual proof targets"

[marker."SELFTEST: ui v3 scroll ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic v3 scroll-target evidence in visible lane"

[marker."SELFTEST: ui v3 ime ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic IME/text-input path evidence"

[marker."SELFTEST: ui v3 filter ok"]
phase = "end"
emit_when = { profile = "visible-bootstrap" }
proves = "selftest observed deterministic v3 filter-update evidence"

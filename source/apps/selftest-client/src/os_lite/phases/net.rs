//! Phase: net (extracted in Cut P2-11 — local IP / os2vm detection, ICMP ping, DSoftBus OS transport incl. QUIC subset markers).

use crate::os_lite::context::PhaseCtx;

#[allow(dead_code)]
pub(crate) fn run(_ctx: &mut PhaseCtx) -> core::result::Result<(), ()> {
    Ok(())
}

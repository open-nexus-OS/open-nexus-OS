# source/apps/selftest-client/.arch-allowlist.txt
#
# Architecture-gate baseline allowlists for selftest-client.
# Maintained as part of TASK-0023B Phase 3 (Cut P3-03) per RFC-0038 refinement (7).
#
# Each section is consumed by scripts/check-selftest-arch.sh.
# All paths are relative to source/apps/selftest-client/src/.
#
# Cut P4-04 emptied the [marker_emission] allowlist when emit sites migrated
# to `crate::markers::M_<KEY>` constants generated from `proof-manifest.toml`.
# Rule 3 in `scripts/check-selftest-arch.sh` is now allowlist-free: the only
# files that may carry marker literals are `markers.rs` and
# `markers_generated.rs` (the latter produced by `build.rs`).

[mod_rs_fn]
# `os_lite/mod.rs` hosts the dispatch `pub fn run()` which is the OS entry
# point (called from `main.rs::os_entry` via
# `nexus_service_entry::declare_entry!`). Documented in ADR-0027's "Decision"
# section as the canonical entry-point pattern; not extracted to a separate
# file because that fn IS the aggregator's reason to exist (12 phase calls
# in declarative order).
os_lite/mod.rs

[size_500]
# `smoltcp_probe.rs` is bounded smoltcp-over-virtio bring-up debug code,
# cfg-gated by feature `smoltcp-probe` (off by default per Cargo.toml).
# Kept under selftest-client for bisect convenience; tracked for Phase 4
# cleanup if it survives the manifest migration.
os_lite/net/smoltcp_probe.rs
# `display_bootstrap.rs` owns the single visible-bootstrap coordinator for
# ramfb scanout setup, proof-lane rendering, and the interactive live observer
# loop. RFC-0054 extends that same coordinator with inputd-backed live redraws;
# the file remains intentionally centralized until a later selftest split.
os_lite/display_bootstrap.rs
# `phases/exec.rs` grew past 500 LoC with the phased vmo/exec syscall proofs
# (CopyPlan, SMP soft-realtime track, 2026-07). The phase is one coherent
# ladder; splitting it mid-track would blur the proof ordering. Revisit when
# the SMP track's follow-ups land.
os_lite/phases/exec.rs

[justfile_require_env]
# TASK-0023B P4-10 — Rule 6 escape hatch.
# Each entry is a `just` recipe name (no leading colon) that is permitted
# to mention REQUIRE_* literals in its body. Currently empty: the canonical
# `ci-os-*` recipes consume REQUIRE_* via `nexus-proof-manifest list-env
# --profile=<name>` (the manifest is the SSOT). New entries here MUST come
# with a comment explaining why the recipe cannot use a manifest profile.

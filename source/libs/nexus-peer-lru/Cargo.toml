[package]
name = "nexus-peer-lru"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
# no_std compatible, minimal LRU for discovery peers

[dev-dependencies]

[lib]
path = "src/lib.rs"

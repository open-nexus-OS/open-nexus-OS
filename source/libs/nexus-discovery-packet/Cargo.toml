[package]
name = "nexus-discovery-packet"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
# no_std compatible, no external dependencies needed for packet parsing

[dev-dependencies]

[lib]
path = "src/lib.rs"

[package]
name = "nexus-service-entry"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[features]
default = []
alloc-log = ["nexus-log"]

[dependencies]
nexus-abi = { path = "../nexus-abi" }
nexus-log = { path = "../nexus-log", optional = true, features = ["sink-userspace"] }
nexus-sync = { path = "../nexus-sync" }

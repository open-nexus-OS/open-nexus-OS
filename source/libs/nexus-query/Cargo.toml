# Copyright 2026 Open Nexus OS Contributors
# SPDX-License-Identifier: Apache-2.0

[package]
name = "nexus-query"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "nexus_query"
path = "src/lib.rs"

[features]
default = ["std"]
# Host conveniences; the engine itself is no_std+alloc over the `Kv` trait.
std = []

[dependencies]

[package]
name = "nexus-log"
version = "0.1.0"
edition = "2021"
license.workspace = true

[features]
default = []
sink-userspace = ["dep:nexus-abi"]
sink-kernel = []

[dependencies]
nexus-abi = { path = "../nexus-abi", optional = true }

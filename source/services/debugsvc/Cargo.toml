[package]
name = "debugsvc"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[features]
default = ["std"]
std = []
os-lite = ["nexus-service-entry", "nexus-abi"]

[package.metadata.nexus-service]
stack_pages = 0

[dependencies]
nexus-service-entry = { path = "../../libs/nexus-service-entry", optional = true }
nexus-abi = { path = "../../libs/nexus-abi", optional = true }

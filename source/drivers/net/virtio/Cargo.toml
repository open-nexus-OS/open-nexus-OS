[package]
name = "net-virtio"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
nexus-hal = { path = "../../../libs/nexus-hal" }
# Gate 3: the shared virtio-mmio transport HAL — the register map + device-init
# handshake + queue programming live here now, not copy-pasted per driver.
nexus-virtio = { path = "../../../libs/nexus-virtio" }

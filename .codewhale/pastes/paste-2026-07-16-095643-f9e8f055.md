evaluiere: ╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 SMP Production-Grade + pinched (Parallel Interaction Net Combination Hybrid Evaluator)                                                                                                                       ↑

 Context

 SMP v1 (TASK-0012/0012B, RFC-0021/0022) ist Done — aber nur als Gerüst: Sekundär-Harts booten via SBI HSM und parken dann für immer in spin_loop() (core/kmain.rs:627-646). Scheduler, Syscalls, IPC, PLIC un↑ Timer sind hart auf den Boot-Hart begrenzt (trap.rs:236-239 → ENOSYS auf Sekundär-Harts). Ziel dieses Tracks: echte Multi-Core-Task-Ausführung production grade (Apple-Qualität, nicht minimal), darauf ein Userspace-Parallelismus-Fundament, und darauf pinched — ein systeminterner Compute-Broker mit deklarativem Job-Graph-Interface.
                                                                                                                                                                                                              ↑
 Entschiedene Architektur (User-Entscheidungen):
 - pinched = Hybrid-Broker: deklaratives Job-Graph-Interface (partition→map→reduce), zentrales Kern-/QoS-Budget wie GCD; Execution-Backends austauschbar. v1-Backend = natives deterministisches Work-Stealing↑(TASK-0276-konform). v2-Backend = minimaler eigener Interaction-Net-Evaluator (Lafont Interaction Combinators; im Code/Docs NIE Produktnamen wie „HVM2"/„Bend" — Legal-Regel).
 - pinched ist für App-Devs unsichtbar: KEIN Eintrag in dsl_services.capnp, KEINE svc.compute-DSL-Surface. Nur System-Services und unser SDK nutzen es intern (z. B. eine SDK-Rasterize-Funktion, die hinten dran pinched benutzt). Der Dev sieht nur „it just works"-SDK-Funktionen.
 - Einsatzkriterium: nur wo WIR entscheiden, dass Latenz unkritisch ist und echte Verbesserung messbar ist (Batch/„User wartet"-Pfade). Frame-Hotpaths bleiben unberührt.                                     ↑
 - Erster Workload: SVG-Tessellation/Raster (userspace/ui/svg/, Icon-Bakes) — embarrassingly parallel, cachebar, nicht frame-kritisch.
 - GPU: Nein. GPU bleibt Render-Domäne (virtio-gpu/virgl, thin gpud). GPU-Compute/dsoftbus/Server = spätere Backends hinter demselben Job-Graph-Interface, out of scope.                                      ↑

 Governing Policies (Plan ist danach ausgerichtet): TASK-0277 (Lock-Hierarchie, deterministische Result-Proofs, keine Lock-free-Experimente v1, bounded everything), TASK-0276 (deterministische Threadpools, workers=1≡N-Beweise, EIN geteilter workpool), TASK-0283 (PerCpu<T>), TASK-0288 (Latenz-Budgets/Stress), docs/architecture/smp-ipi-rate-limiting.md (Korrektheits-IPIs = nie droppen; Best-effort = throtteln), Fake-Proof-Marker-Doktrin (Counterfactual-Marker), RFC-0021/0022 (kein Marker-Drift).                                                                                                                        ↑

 Master-Sequenzregel: Jeder Schritt lässt den Kernel unter SMP=1 UND SMP=2 bootbar mit allen bestehenden Markern grün. Nach jedem boot-bewiesenen Paket: Commit-Vorschlag (User committet selbst).            ↑

 Validierte Zusatzfakten: QEMU 11.0.1 installiert → -cpu max hat Sstc/stimecmp. Höchste Syscall-ID = 45. trap.S nutzt bereits sscratch = per-Hart-Trap-Stack-Top (sauberer Anker für A1). TaskTable::wake (task/mod.rs:974) enqueued global trotz Doc-Kommentar „home-CPU" — TASK-0042-Delta ist dem Code voraus, Ledger reconcilen.
                                                                                                                                                                                                              ↑
 ---
 PHASE A — Kernel: echte SMP-Ausführung                                                                                                                                                                       ↑

 Kritische Dateien: source/kernel/neuron/src/core/{trap.rs,kmain.rs,smp.rs}, sched/mod.rs, arch/riscv/trap.S, hal/plic.rs, mm/{page_table.rs,address_space.rs}, timer.rs.                                     ↑

 A0. PerCpu<T> + IRQ-safe Spinlock (S) — TASK-0283/0277                                                                                                                                                       ↑

 - Neu sync/percpu.rs: PerCpu<T> ([UnsafeCell<T>; MAX_CPUS], !Send, Accessor with_current mit IRQ-off-Guard + CPU-Index-Assert).                                                                              ↑
 - Neu sync/spin_irq.rs: SpinIrqLock<T> = spin::Mutex + sstatus.SIE save/clear/restore. Einziger erlaubter Lock-Typ in trap-erreichbaren Pfaden.
 - Proof: Host-Tests (kein QEMU-Verhalten geändert).                                                                                                                                                          ↑

 A1. Per-Hart-Identität via tp — Heuristik töten (M, Risiko #1: trap.S)                                                                                                                                       ↑

 - sscratch wird Pointer auf per-Hart HartLocal { trap_stack_top, cpu, hart, resume_frame, tick_deadline, … } (static HART_LOCALS: [_; MAX_CPUS] in smp.rs); tp trägt denselben Pointer in S-Mode.            ↑
 - trap.S: csrrw t0, sscratch, t0 → ld sp, 0(t0) (trap_stack_top = Feld 0); nach Frame-Save mv tp, t0. Trap-Exit restauriert User-tp aus Frame (unverändert).
 - Boot-Hart: tp+sscratch in init_boot_hart_state() (kmain.rs:655) VOR install_runtime; Sekundär: in __secondary_hart_rust vor Trap-Vektor-Install.                                                           ↑
 - cpu_current_id() = tp lesen + Range-Check gegen HART_LOCALS; Fallback nur noch als gezählter Counterfactual-Counter (KINIT: cpuid fallback (n=…)), Selftest asserted 0. Heuristik (cpu_from_stack_pointer) nach einem grünen Zyklus löschen.                                                                                                                                                                            ↑
 - Marker: KSELFTEST: cpuid tp ok (+ Poison-tp-Counterfactual). sscratch-Formatwechsel + Konsumenten in EINEM Commit.
                                                                                                                                                                                                              ↑
 A2. Kernel-State konkurrenzfähig: KernelShared hinter EINEM Lock + per-CPU-Scheduler (XL, Risiko #2)
                                                                                                                                                                                                              ↑
 Ownership-Tabelle (per TASK-0277 deklariert):
 - Per-CPU: Runqueues + current → static RUNQUEUES: [SpinIrqLock<CpuRunQueues>; MAX_CPUS] (cross-CPU lockbar für wake/steal).                                                                                 ↑
 - Shared, EIN BKL: SpinIrqLock<KernelShared> = TaskTable, ipc::Router, AddressSpaceManager (+ASID), WaitsetTable, FenceTable, TimerTable. Begründung: Syscall-Context borrowt heute alles simultan; ein Lock = null Ordering-Bugs, jeder Zwischenstand boot-beweisbar. Split (mm zuerst, dann Router) = expliziter TASK-0288-Follow-up.                                                                                     ↑
 - Immutable nach Boot: SyscallTable, HAL → &'static.
 - Leaf-Atomics: PT_STATIC_POOL_NEXT → AtomicUsize; ASID-Allocator + ASID-Quarantäne (Reuse erst nach TLB-Epoch, A5); VMO-Arena-Cursor atomic (Defense); Kernel-Heap-Lock → SpinIrqLock.                      ↑
 - Lock-Hierarchie (normativ, in docs/architecture/16-rust-concurrency-model.md): BKL → runqueue[i] (aufsteigend) → Leaf-Atomics. Nie BKL unter Runqueue-Lock.
                                                                                                                                                                                                              ↑
 Migrationsfolge (jeder Sub-Schritt bootet grün, Sekundär-Harts parken noch):
 1. A2a: KernelState-Shared-Felder → KERNEL: Once<SpinIrqLock<KernelShared>>; KernelHandles/TRAP_RUNTIME wird Adapter der lockt; Boot-Hart-Gate bleibt vorerst.                                               ↑
 2. A2b: Kein Guard über sret: Switch-Pfade kopieren TrapFrame+satp nach HartLocal.resume_frame, droppen Guard, dann switchen. idle_loop, exit_current, preempt_current_user_task auditieren.
 3. A2c: Atomics/Quarantäne (PT-Pool, ASID) vor jedem Zweit-Hart-mm-Zugriff.                                                                                                                                  ↑
 4. A2d: Alte „U-mode ⇒ unique borrow"-SAFETY-Kommentare + unsafe impl Send/Sync for KernelHandles löschen.
 - Marker: KSELFTEST: kernel lock contention bounded ok (2-Hart-Lock-Ping, exakte Zählung).                                                                                                                   ↑

 A3. Sekundär-Harts in den Scheduler; Runqueues selftest→runtime (L)                                                                                                                                          ↑

 - sched/mod.rs: globale Queues sterben als Runtime-Pfad; enqueue(pid,qos) routet nach home_cpu (Fallback CPU0); selftest_* werden Wrapper über dieselben Strukturen (RFC-0021-Marker behalten Bedeutung). SMP=1: alles auf Queue 0 → byte-identische Reihenfolge (Host-Test: Pick-Order-Parität alt vs. neu für fixes Enqueue-Skript).
 - fn cpu_main(cpu) -> !: eigene Queue → bounded steal (A8) → Timer/IRQ-Backstop (Boot-Hart, wie heute) → WFI-Idle (SIE=0; wenn leer && kein resched pending { wfi }; SIE=1 — kein Lost-Wakeup).              ↑
 - kmain_secondary: nach mark_cpu_online bounded Warten auf RUNTIME_READY: AtomicBool (Boot-Hart setzt nach Selftests), dann cpu_main.
 - Sekundär-ENOSYS-Gate löschen (trap.rs:237/1163): alle Harts servieren Syscalls unter BKL.                                                                                                                  ↑
 - Interim-TLB-Sicherheit bis A5: Sekundär-activate() = voller lokaler sfence.vma bei JEDEM AS-Switch + ASID-Quarantäne ⇒ kein Stale-Window (Same-AS-Threads kommen erst Phase C, gated auf A5).
 - Marker: KSELFTEST: smp exec cpuN ok (Dispatch-Counter nur in cpu_main mit tp-Identität geschrieben), smp exec reject offline ok, SMP=1: smp exec skipped (smp=1).                                          ↑
 - Trap: UART/kfold/liveness sind single-writer — Sekundär-Output nur Counter + seltene Marker (TASK-0277 Rule 5).
                                                                                                                                                                                                              ↑
 A4. Cross-Core-Wake + IPI + Präemption auf allen Harts (M)
                                                                                                                                                                                                              ↑
 - TaskTable::wake (+ alle scheduler.enqueue-Caller: wake_parent_waiter, waitset/fence-Wakes, spawn_inner): enqueue_on_cpu(home_cpu); wenn fremder Hart → smp::request_resched(target) (Evidence-Kette request→send→trap→ack bleibt). Home-CPU v1 = vom Spawner erben.                                                                                                                                              ↑
 - Jeder Hart armt eigenen SBI-Timer in cpu_main + re-armt im S_TIMER-Trap; preempt_current_user_task läuft auf jedem Hart gegen eigene Queue. Timer-Cap-Delivery/IRQ-Backstop bleibt Boot-Hart (v1).
 - Marker: KSELFTEST: cross wake ipi ok (strukturell: Queue[1]-Landung + Evidence exakt +1), cross wake counterfactual ok (forced IPI-send-fail), preempt cpuN ok.                                            ↑

 A5. TLB-Shootdown (L, korrektheitskritisch) — nach A6/A7 einplanbar, VOR Phase C                                                                                                                             ↑

 - Generation-basiert, allokationfrei: TLB_EPOCH: AtomicU64 + TLB_MAIL: [TlbMailbox{requested,acked,scope}; MAX_CPUS].                                                                                        ↑
 - Initiator (unmap/perm-downgrade/AS-teardown/Quarantäne-Flush): lokal sfence → epoch++ → pro Online-Hart scope setzen (koaleszieren: widen zu ALL), IPI (Korrektheitsklasse, kein Throttle) → bounded spin auf acked>=epoch (Budget, Überschreitung = panic fail-closed). Initiator wartet mit SIE=1 (kann fremde Shootdowns bedienen); hält BKL — sanktioniert NUR weil Responder lock-frei aus S_SOFT-Trap ackt (Mailbox atomics-only). Debug-Assert: S_SOFT-Handler asserted BKL-not-held-by-self.
 - Responder-Reihenfolge im S_SOFT: (1) TLB-Mailbox → sfence per scope → ack, (2) resched.                                                                                                                    ↑
 - Danach: A3-Interim-Flush liften, ASIDs bei Epoch-Advance freigeben.
 - Marker: KSELFTEST: tlb shootdown ok / tlb shootdown counterfactual ok / SMP=1 skipped.                                                                                                                     ↑

 A6. PLIC per-Hart-Kontexte (M)                                                                                                                                                                               ↑

 - hal/plic.rs: BOOT_HART_S_CONTEXT → s_context(hart) = 2*hart+1 (virt-Layout); init aller Online-Kontexte (threshold=0, enable cleared) vom Boot-Hart; claim/complete auf eigenem Kontext. enable_source(irq,↑cpu) mit Default Boot-Hart — irq_bind-Semantik (Syscall 36) v1 unverändert, Binding-Table merkt sich cpu für Phase B.
 - Marker: KSELFTEST: plic ctx cpuN ok, plic isolation ok (Source für ctx1 nicht in ctx3 pending).                                                                                                            ↑

 A7. Per-Hart-Timer + optional SSTC (S–M)                                                                                                                                                                     ↑

 - HartTimers → TimerTable, EINE Instanz im BKL (per-CPU-Split = TASK-0288-Follow-up bei Bedarf). Per-Hart-Tick-Counter in HartLocal; Marker KSELFTEST: smp per-hart ticks ok.                                ↑
 - SSTC: Detect via DTB isa-extensions; wenn da → stimecmp direkt statt SBI-Trap; Fallback SBI. Marker KINIT: timer sstc vs timer sbi.
                                                                                                                                                                                                              ↑
 A8. Runtime-Work-Stealing, bounded + affinity-respecting (S)
                                                                                                                                                                                                              ↑
 - selftest_try_steal in cpu_main-Idle-Pfad promoten (alle QoS-Klassen), STEAL_MAX_PER_TICK=1, min-steal-interval 1ms via HartLocal.last_steal_ns, Victim-Queues via Tier-2-Lock aufsteigend. Gestohlener Task: home_cpu = Dieb (explizite Migration); Phase B ersetzt Prädikat durch Affinity-Maske.                                                                                                                        ↑
 - Marker: KSELFTEST: runtime steal ok (Runnable-Set-Konservierung: dispatched == enqueued), runtime steal rate ok (3 Versuche → exakt 1 erlaubt).
                                                                                                                                                                                                              ↑
 A9. SMP=4 Default + Proof-Ladder (S)
                                                                                                                                                                                                              ↑
 - scripts/qemu-launcher.sh:200: -smp "${SMP:-4}" für interaktives just start; Test-Profile passen SMP explizit; SMP=1-Determinismus-Profil bleibt First-Class-Gate.
 - justfile ci-os-smp: SMP=4 strict + SMP=1-Parität (SMP=2 nightly). scripts/qemu-test.sh: alle neuen Marker mit SMP=1-Skip/Counterfactual-Variante (nie „Marker fehlt == pass").                             ↑
 - TASK-0288-Closure: KSELFTEST: runtime timer budget ok / runtime ipi budget ok / runtime stress ok (fixes N Tasks × M Runden über 4 Harts; Budgets: max IPIs, max Steals, 0 Runnable-Loss, 0 cpuid-Fallbacks).
                                                                                                                                                                                                              ↑
 Reihenfolge: A0 → A1 → A2a-d → A3 → A4 → A6 → A7 → A5 → A8 → A9. Nach jedem Schritt: Boot-Beweis SMP=1+SMP=2 (später +SMP=4), Commit-Vorschlag.
                                                                                                                                                                                                              ↑
 ---
 PHASE B — SMP-v2-ABI: Affinity + QoS-Budgets (TASK-0042)                                                                                                                                                     ↑

 - B1 (M): EIN gemultiplexter SYSCALL_SCHED = 46 (Ops SET/GET_AFFINITY, SET/GET_SHARES, SET_LATENCY_HINT; Vorbild SYSCALL_TASK_QOS=15). Validierung: Maske ∩ online ≠ 0, shares clamp [1,1000]; cross-task nur↑mit Manage-Cap (execd). Task-Felder affinity: u8, shares: u16 (inherit on spawn, reset on exec).
 - B2 (M): Scheduler respektiert Affinity (enqueue clampt home_cpu in Maske; Steal-Prädikat lehnt out-of-mask ab); shares = deterministischer Slice-Multiplikator innerhalb QoS-Klasse.                       ↑
 - B3 (S): nexus_abi::sched-Wrapper.
 - B4 (M): execd-Wiring recipes/sched/{affinity.toml,qos.toml}, Marker execd: affinity set (svc=… mask=…).                                                                                                    ↑
 - B5 (M): Per-Task-IPI-Budget + globaler Best-effort-Limiter (smp-ipi-rate-limiting Phase 2).
 - B6 (M): Proofs: Host-Unit-Tests + SELFTEST: affinity applied ok (selftest-client pinnt sich auf cpu1, GET bestätigt last-ran-cpu==1), qos shares ratio ok, ipi_flood_reject ok.                            ↑

 ---                                                                                                                                                                                                          ↑
 PHASE C — Userspace-Parallelismus-Fundament (hard-dependent auf A5, B1)
                                                                                                                                                                                                              ↑
 - C1 (M): Thread-Spawn: Kernel akzeptiert schon spawn(entry, stack, as_handle, …). Fehlt: (a) SYSCALL_AS_SELF = 47 (eigenes AsHandle), (b) nexus_abi::thread_spawn(entry, arg, stack) — VMO+guard-paged Stack, gp durchreichen, Trampoline mit exit-on-return, (c) TLS: v1 keins, tp = user-definierter Thread-Context-Pointer (dokumentiert).                                                                              ↑
 - C2 (M, unterschätzt): Userspace-Runtime thread-safe: nexus-alloc Bump-Cursor → CAS-Loop (never-free bleibt); nexus-ipc/nexus-service-entry Globals auditieren. Policy: Services bleiben single-threaded Recv-Loops; Threads NUR für Compute via workpool (workpool besitzt alle Thread-Erzeugung).                                                                                                                   ↑
 - C3 (L): source/libs/nexus-workpool/ per TASK-0276: fixe workers=N (≤MAX_CPUS), bounded Job-Queue (Semantik von nexus-driverkit::SubmitRing, 32 Slots), API pool.run(partition: DeterministicPartition, reduce: CanonicalReduce); Koordination via fence-Syscalls (41-43), kein Busy-Spin; submit-on-full = dokumentierter Reject. Proofs: Host workers=1 ≡ workers=4-Gleichheitsmatrix; QEMU SELFTEST: workpool determinism ok + workpool bounded ok.
 - C4 (S): Worker setzen Self-Affinity (ein Worker pro CPU, deterministisch).                                                                                                                                 ↑

 ---                                                                                                                                                                                                          ↑
 PHASE D — pinched v1: systeminterner Compute-Broker + SVG-Workload
                                                                                                                                                                                                              ↑
 Sichtbarkeit: KEIN dsl_services.capnp-Eintrag, KEINE App-Permission, KEIN effect_host-Arm. pinched ist reine System-Infrastruktur; Apps erreichen es nie direkt.
                                                                                                                                                                                                              ↑
 - D1 (M): Service-Skelett source/services/pinched/{main.rs, os_lite.rs, std_server.rs, wire.rs} nach Standard-Anatomie (nexus-service-entry, KernelServer, host/OS-Split). Route nur als System-Route (init/execd-Wiring wie andere Service-zu-Service-Verbindungen; NICHT in der App-facing SERVICE_ROUTES-Provisionierung — prüfen ob Tabelle einen system-only-Marker braucht oder Wiring rein deklarativ über execd-Routen läuft).
 - D2 (M): Job-Graph-Wire-Protokoll (tools/nexus-idl/schemas/pinched.capnp + manueller no_std-Codec oder wire.rs): Job = { kind, partition-Spec, QoS-Klasse, Input-VMO, Output-VMO }. Datenpfad zero-copy via VMO (Muster: TASK-0295 OP_READ_VMO/CAP_MOVE). Ergebnis-Fence signalisiert Completion; Client wartet via waitset/fence (kein Async-Runtime nötig).
 - D3 (M): Broker-Kern: nimmt Jobs an, führt sie auf seinem nexus-workpool aus (workers per-CPU gepinnt via B, QoS-Klasse des Jobs → Worker-QoS via SYSCALL_SCHED), bounded Queue mit Backpressure (Reject, ni↑ Hang), deterministic canonical reduce. Ergebnis-Cache (content-keyed, bounded) für wiederholte Bakes.
 - D4 (M): Erster Kunde: SVG-Tessellation/Raster. userspace/ui/svg/tessellate.rs-Pipeline partitionierbar machen (per Shape/Tile, deterministische Tile-Reihenfolge beim Reduce); SDK-Funktion (z. B. in der Icon-Bake-/Asset-Schicht) bekommt pinched-Pfad hinter Feature-Gate; Fallback = heutiger Inline-Pfad (fail-open auf lokal, nie auf Warten).
 - D5 (S): Latenz-Kriterium als Doktrin dokumentieren (docs/dev/… + Task-Ledger): pinched nur für „User wartet"-Batch-Pfade; Frame-Hotpaths verboten.                                                         ↑
 - Proofs: SELFTEST: pinched determinism ok (Digest workers=1 vs N identisch), pinched bounded ok (Queue-Full-Reject), pinched svg speedup ok — Speedup-Beweis als Result-Proof: gleiche Pixel-Digest UND Dispatch-Counter beweisen Multi-Hart-Ausführung (Timing nur informativ loggen, nicht gaten; QEMU-MTTCG-Zeiten schwanken).                                                                                    ↑

 ---                                                                                                                                                                                                          ↑
 PHASE E — pinched v2: minimaler Interaction-Net-Evaluator (eigenes Backend)                                                                                                                                  ↑

 Scope bewusst minimal: Evaluator als Backend beweisen, KEIN Sprach-Frontend/Compiler.

 - E1 (L): Neue Lib source/libs/nexus-inet/ (no_std, forbid(unsafe) soweit möglich): Interaction-Combinator-Netz (Node-Typen ERA/CON/DUP + Zahlen-Leaf), Arena-basierter Node-Store (bounded, recycelnd — Bump-only reicht nicht), Redex-Queue. v1 der Reduktion: gelockte Redex-Deques auf nexus-workpool-Workern (TASK-0277: keine Lock-free-Experimente; atomares Port-Linking = expliziter späterer Schritt mit eigenem Proof-Budget). Konfluenz ⇒ workers=1≡N per Konstruktion — Host-Gleichheitsmatrix als Beweis.
 - E2 (M): pinched-Integration: Job-kind net-eval; Netz-Einschreibung deklarativ über kleines Builder-/Serialisierungsformat im Wire-Protokoll.
E3 (M): Beweis-Workload: kanonisches paralleles Baum-Reduktions-Benchmark (z. B. Baum-Summe/Map über 2^k-Baum). Marker: SELFTEST: inet determinism ok (Normalform-Digest workers=1 vs N), inet bounded ok rena-Erschöpfung = Reject, kein OOM), inet parallel exec ok (per-Worker-Reduktions-Counter > 0 auf ≥2 Harts).
 - E4 (Dokumentiert, nicht gebaut): Ehrlicher erster echter Kunde später = DSL-Compiler/Typechecker-Passes (symbolisch, baumförmig); GPU-/dsoftbus-Backends = Future Scope hinter demselben Job-Graph-Interface.

 ---
 Task-Ledger-Mapping (updaten statt duplizieren)

 - TASK-0012/0012B: A3/A4/A8 → v1 nach A8 komplett markieren.
 - TASK-0247: A1, A2 (trap-runtime ownership handoff), A3, A4, A6, A7, A9 (virtioblkd/packagefs-Teile out of scope).
 - TASK-0277: A0 + A2-Lock-Hierarchie eintragen, von Draft heben.
 - TASK-0283: A0 (DoD: PerCpu für Scheduler/HartLocal).
 - TASK-0288: A9-Closure-Marker + Stress-Fixtures; per-CPU-TimerTable als Follow-up notieren.
 - TASK-0042: Phase B komplett + C4; stales „Stage 2"-Delta reconcilen (wake() ist im Code noch global).
 - TASK-0276: C3 (workpool = sanktionierte Implementierung), Policy-Anker für D/E.
 - NEU anzulegen: TASK-xxxx pinched v1 (Broker+SVG) und TASK-xxxx nexus-inet v2, mit Latenz-Doktrin (D5).

 Top-Risiken

 1. A1 trap.S/sscratch-Format — falscher Offset brickt jeden Trap; atomar landen, SMP=1 zuerst, Fallback-Counter als Tripwire (asserted 0).
 2. A2b Guard über sret — Deadlock „genau einmal, später"; per Konstruktion verhindern (resume via HartLocal-Copy; -> !-Funktionen nehmen keine Guards).
 3. A5 Wait-under-BKL — nur sanktioniert weil S_SOFT-Responder lock-frei ackt; Debug-Assert + dokumentierte harte Invariante.
 4. A3 UART/kfold-Contention — Sekundär-Logging nur Counter + seltene Marker.
 5. C2 Bump-Allocatoren unter Threads — stille Heap-Korruption; thread_spawn nur via workpool, erst nach nexus-alloc-CAS.

 Verifikation

 - Pro Schritt: just build + Boot-Beweis via scripts/qemu-test.sh mit SMP=1 und SMP=2 (ab A9 auch SMP=4), alle Alt-Marker + neue Marker inkl. Counterfactuals grün. Boot-Logs: neuestes build/logs/manual--<ts>/uart.log (latest-Symlink stale). Build+Boot in EINEM Launcher-Call (RUN_TIMEOUT=0-Falle beachten).
 - Host: cargo test für percpu/lock/scheduler-Paritäts-/workpool-/inet-Gleichheitsmatrix.
 - Phase D/E: Selftest-Marker wie oben; Speedup als Result-Proof (Digest + Dispatch-Counter), Timing informativ.